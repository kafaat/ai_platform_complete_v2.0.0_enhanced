{
  "base_url_env": "SUPERVISOR_AGENT_BASE_URL",
  "entrypoint": "services/supervisor-agent/routers/health.py",
  "identity_path": "/runtime-identity",
  "note": "Deterministic health probe plus immutable runtime identity binding for certification. تأكيدات المِسبار مقيسة من `services/supervisor-agent/routers/health.py:22`، ولا تُترك فارغة: `response_assertions: []` يُسقِط تحقّق الخطّة نفسها (`response_assertions must be non-empty`)، ويُنتِج مسباراً يقيس أنّ الخدمة **حيّة** لا أنّها تُجيب بما تعاقدت عليه — وهو تماماً ما وُجِدت هذه الخطط لتجاوزه.",
  "openapi_path": "/openapi.json",
  "probes": [
    {
      "dependency_class": "compute-only",
      "deterministic": true,
      "expected_status": 200,
      "method": "GET",
      "path": "/healthz",
      "probe_id": "supervisor-agent-health",
      "response_assertions": [
        {
          "field": "status",
          "equals": "alive"
        },
        {
          "field": "service",
          "equals": "supervisor-agent"
        }
      ]
    }
  ],
  "schema_version": "1.0",
  "service": "supervisor-agent",
  "source_service": "services/supervisor-agent"
}
