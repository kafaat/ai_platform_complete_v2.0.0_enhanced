"""خطُّ التوصية المحروس — عائلةٌ واحدة تحت `shared/` لأنّها القطعةُ التي تنسخها كلُّ الصور.

`AI-RUNTIME-WIRING-01`: كانت في `services/ai_agronomist/` حيث لا تستطيع صورةُ المنصّة —
المستهلكُ الإنتاجيّ الوحيد الممكن — استيرادَها. النقلُ لا يوصل شيئاً بذاته؛ يجعل التوصيلَ
ممكناً في الحاوية لا في الاختبار وحده. قشورُ `services/ai_agronomist/*` تُعيد التصدير.
"""
