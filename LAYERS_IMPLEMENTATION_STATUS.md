# هل نُفّذ نموذج الطبقات الـ15 في الكود؟ — الإجابة الصادقة

تحقّقت طبقةً طبقةً مقابل الكود الفعلي. الإجابة المختصرة: **نعم جزئيّاً —
معظم الطبقات مبنيّة كوحدات، لكن ليست كلّها مربوطة بالمنسّق الموحّد.**

## ثلاثة مستويات للتنفيذ (مهمّ التمييز)
1. **مبنيّ كوحدة**: الكود موجود في ملفّ.
2. **مربوط بالمنسّق**: يدخل compose_field_state / المسار الموحّد.
3. **حيّ إنتاجيّاً**: يُستدعى عبر endpoint حيّ (لا شيء بعد — لا endpoint).

## الجدول الكامل
| # | الطبقة | مبنيّ كوحدة | مربوط بالمنسّق |
|---|--------|-------------|----------------|
| 1 | هوية الحقل | ✅ | ✅ (مكان/إقليم) |
| 2 | **نيّة المزارع** | ❌ | ❌ |
| 3 | التربة | ✅ | ✅ |
| 4 | الفينولوجيا (مرحلة/Kc/GDD) | ✅ | ✅ |
| 5 | الاستشعار (9 مؤشّرات) | ✅ | ✅ |
| 6 | الطقس | ✅ | ✅ |
| 7 | IoT/المستشعرات | ✅ sensor_intake | ❌ |
| 8 | الاقتصاد | ✅ market_analyzer/farm_ledger | ❌ |
| 9 | الأصناف (cultivar) | ✅ | 🔸 جزئي (salt_tolerance فقط) |
| 10 | المعرفة المجتمعيّة | ✅ | ✅ (anwa) |
| 11 | التقويم الزراعي | ✅ | ✅ |
| 12 | التاريخ | ✅ farm_memory/timeline | 🔸 جزئي (سلسلة طلب فقط) |
| 13 | الملاحظات الميدانيّة | ✅ | ✅ |
| 14 | **تكاليف المدخلات** | ❌ | ❌ |
| 15 | حوكمة القرار | ✅ guardrails | ✅ (hook) |

## الأرقام الصريحة
- **مبنيّ كوحدة**: 13/15 (مفقود: نيّة المزارع، تكاليف المدخلات)
- **مربوط بالمنسّق بالكامل**: 9/15
- **مربوط جزئيّاً**: 2 (الأصناف salt_tolerance فقط، التاريخ سلسلة طلب فقط)
- **غير مربوط رغم وجوده**: 3 (IoT، الاقتصاد، + الجزئيّان)
- **حيّ إنتاجيّاً**: 0 (لا endpoint للمنسّق)

## الفجوات الحقيقيّة (تنفيذ ناقص)
1. **طبقتان غير مبنيّتين**: نيّة المزارع (الهدف: إنتاج/توفير/مخاطرة)، تكاليف
   المدخلات المفصّلة (بذور/سماد/عمالة لكلّ مرحلة).
2. **الاقتصاد مبنيّ لكن خارج المنسّق**: market_analyzer/farm_ledger موجودان،
   لا يدخلان القرار → "إجهاد متوسّط + تكلفة ريّ عالية + سعر منخفض = لا تدخّل"
   لا يُحسب بعد.
3. **IoT مبنيّ لكن غير مربوط**: sensor_intake موجود، لا يُعاير الاستشعار.
4. **الأصناف جزئيّة**: يُقرأ salt_tolerance من بطاقة المحصول فقط، لا ملفّ صنف
   كامل (drought/heat/مدّة الدورة) يُعدّل العتبات.
5. **التاريخ جزئي**: farm_memory/timeline مبنيّان، لكن compose_field_state لا
   يُحفَظ فيهما (request-scoped) → لا ذاكرة موسميّة للحالة الموحّدة.

## الخلاصة الصريحة
**هل نُفّذ الكود؟** نعم لمعظم الطبقات كوحدات (13/15)، ونعم لـ9 منها مربوطة
بالمنسّق. لكن النموذج الكامل (15 طبقة تدخل القرار) **لم يكتمل**: طبقتان غير
مبنيّتين، و4 مبنيّة لكن غير مربوطة/جزئيّة، و0 حيّ إنتاجيّاً.

## ملاحظة صدق
لم أزعم تنفيذاً كاملاً ولم أنكر الموجود. التمييز الحاسم: **"مبنيّ" ≠ "مربوط" ≠
"حيّ"**. وهم القدرة هنا حقيقي — الاقتصاد وIoT والتاريخ مبنيّة وقد توحي الوثائق
بفعّاليّتها، لكنّها لا تدخل المسار الموحّد الحيّ. تحقّقت من كلّ بند بالكود
(مثلاً الأصناف: salt_tolerance فقط لا ملفّ كامل — جزئي بصدق).
