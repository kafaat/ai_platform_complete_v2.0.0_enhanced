# إصلاح فشل حاوية RAG — التشخيص الكامل

راجعتُ كلّ أسباب فشل RAG، ووجدتُ سبباً خفيّاً إضافيّاً لم يكن مُصلَحاً.

## كلّ أسباب فشل RAG وحالتها
| # | السبب | النوع | الحالة |
|---|-------|------|--------|
| ① | sentence-transformers الثقيل | بناء | ✓ مُزال (سابقاً) |
| ② | المرآة الصينيّة (timeout) | بناء | ✓ PyPI الرسمي (سابقاً) |
| ③ | langchain_community.Qdrant مهمَل | تشغيل | ✓ QdrantVectorStore (سابقاً) |
| ④ | langchain-community<0.4 ضيّق | بناء | ✓ <1.0 مرن (سابقاً) |
| ⑤ | **pypdf مفقود** | **تشغيل** | ✓ **مُضاف الآن** |

## السبب الخفيّ الجديد (⑤)
الكود يستخدم `PyPDFLoader` (السطر 139) لتحميل ملفّات PDF. هذا الـloader
**يستورد pypdf بشكل كسول (lazy)** — أي:
- البناء ينجح (الاستيراد من langchain_community يمرّ)
- **لكن وقت التشغيل، عند أوّل تحميل PDF، يفشل**: `ImportError: pypdf not found`

هذا نوع خبيث من الفشل: لا يظهر في البناء، يظهر فقط عند الاستخدام الفعلي.

**الإصلاح**: أضفتُ `pypdf>=4.0.0,<6.0` لـrequirements.

## التحقّق الشامل (كلّ استيراد ↔ حزمة)
فحصتُ كلّ استيرادات RAG، وكلّها الآن مغطّاة:
- langchain/community/qdrant/ollama/text-splitters/core ✓
- httpx, python-jose, python-multipart, aiofiles ✓
- **pypdf** (كان مفقوداً) ✓

## التحقّق
- 584/584 roadmap (+6) · 0 خطأ
- RAG main.py يُترجم · كلّ التبعيّات الخفيّة موجودة

## ملاحظة صدق
كنتَ محقّاً أن RAG قد يفشل لأسباب أخرى. الأسباب ①-④ كانت مُصلَحة، لكن ⑤
(pypdf) **كان لا يزال مفقوداً** — وهو فشل وقت تشغيل خفيّ (لا يظهر في البناء).
لم أكتشفه إلّا بفحص استخدام PyPDFLoader في الكود. لم أختبر التثبيت حيّاً (بيئتي
بلا pip) — لكنّ المطابقة (استيراد ↔ حزمة) صحيحة بنيويّاً. ستتأكّد عند البناء
على جهازك. لو ظهر فشل آخر، أرسل آخر 40 سطراً من:
`docker compose -f docker-compose.v9.yml build sahool-local-ai-rag --progress=plain`
