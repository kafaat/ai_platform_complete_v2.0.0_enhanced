# تدقيق مسار الإنتاج (Production-Path Audit)

نفّذتُ التدقيق الذي أوصت به المراجعات: جدول رسمي يكشف ما هو مبنيّ/مُختبَر/
مربوط/حيّ. الهدف كشف "وهم القدرة" (capability illusion) — وحدات موثّقة لكن
ميّتة في مسار الإنتاج.

## أوّلاً: تصحيح ادّعاءات المراجعات (تحقّق بالكود)
المراجعات ناضجة لكنّها **بالغت في "الغياب"**. الموجود فعلاً:
| ادّعت المراجعة أنّه مفقود | الواقع في الكود |
|---------------------------|------------------|
| "لا ذاكرة حقل/digital twin" | `farm_memory.py` (timeline + snapshots) موجود |
| "لا مقارنة مواسم" | `multi_season_analytics.py` موجود |
| "لا arbitration matrix" | `temporal_arbitration.py` (TemporalArbiter) موجود |
| "السوق خارج النظام" | `engines/market_analyzer.py` موجود |
| "لا timeline" | `api/field_timeline.py` موجود |

**فالفجوة ليست "غياب" بل "ربط"** — كما في كلّ المراجعات السابقة.

## ثانياً: جدول التدقيق الرسمي
| القدرة | مبنيّ | مُختبَر | مربوط بالمنسّق | حيّ (endpoint) |
|--------|-------|---------|----------------|----------------|
| Canonical State | ✅ | ✅ roadmap | ✅ | ❌ |
| Coordinator (المسار) | ✅ | ✅ roadmap | ✅ | ❌ |
| 9 مؤشّرات + تقويم | ✅ | ✅ roadmap | ✅ | ❌ |
| Farm Memory (ذاكرة) | ✅ | ✅ | 🔸 منفصل | 🔸 |
| Multi-Season (مقارنة) | ✅ | 🔸 | ❌ | ❌ |
| Temporal Arbiter | ✅ | ✅ | ❌ | 🔸 |
| Market Analyzer | ✅ | 🔸 | ❌ | ❌ |
| Field Timeline | ✅ | ✅ | 🔸 منفصل | 🔸 |

## ثالثاً: الفجوات الحقيقيّة (مرتّبة، كلّها "ربط/تشغيل")
1. **لا endpoint حيّ للمنسّق** (الأهمّ): run_field_intelligence مبنيّ ومُختبَر
   لكن لا POST /field-intelligence/analyze يستدعيه. → integration-ready لا
   production-active.
2. **المصادر محقونة (mock)**: soil_fn/sensing_fn... للاختبار، لا محوّلات HTTP
   حيّة بعد.
3. **الحالة الموحّدة request-scoped**: compose_field_state لا يُحفَظ في
   farm_memory/timeline → لا ذاكرة موسميّة للحالة (رغم وجود بنية الذاكرة).
4. **السوق + البذور خارج المسار**: market_analyzer وcultivar موجودان لكن لا
   يدخلان compose_field_state — رغم أنّهما يغيّران عتبات القرار (البذور
   تُعدّل salt/heat tolerance؛ السوق يجعل "لا تدخّل" قراراً اقتصاديّاً صحيحاً).
5. **arbitration matrix غير صريحة كاملة**: حلّ تعارض الملوحة-NDVI موجود، لكن
   لا مصفوفة أسبقيّة شاملة (salinity vs heat vs phenology vs economics).

## رابعاً: ترتيب التأثيرات الصحيح (من المراجعة، أتبنّاه)
| المصدر | نوع التأثير |
|--------|-------------|
| الاستشعار | واقع مُلاحَظ (observed) |
| التربة | قيد بنيوي (structural) |
| الطقس | ضغط مستقبلي (future) |
| GDD/Kc | توقيت بيولوجي |
| البذور | مُعدّل تفسير (عتبات) |
| السوق | قيد اقتصادي |
| anwa | قرينة موسميّة (prior، لا حاكم) |

## الخلاصة الصريحة
النظام **عبر "الاكتمال المنطقي" بقوّة**، لكنّه لم يعبر **"الاكتمال التشغيلي"**
بالكامل. القدرات موجودة ومُختبَرة منطقيّاً؛ الفجوة في: endpoint حيّ + محوّلات
HTTP + حفظ الحالة + دمج السوق/البذور + مصفوفة أسبقيّة صريحة.

**كلّها فجوات تكامل/تشغيل، لا فجوات علم أو بناء.** أرخص وأقلّ خطراً من إعادة
البناء بكثير.

## ملاحظة صدق
لم أبالغ كالمراجعات (التي ادّعت غياب موجود) ولم أهوّن. تحقّقت من كلّ قدرة
بالكود: الذاكرة والمقارنة والتحكيم والسوق **مبنيّة لكن غير مربوطة بالمنسّق
الجديد**. "وهم القدرة" حقيقي هنا: الوثائق توحي بفعّاليّتها، لكنّها لا تدخل
المسار الموحّد الحيّ. هذا التدقيق يكشفه بصدق.
