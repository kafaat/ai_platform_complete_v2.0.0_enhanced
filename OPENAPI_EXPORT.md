# تصدير OpenAPI — النتيجة

وثّقتُ واجهات الخدمات الـ14 على مستويين (قيمة فوريّة + spec كامل لبيئتك).

## ما أنجزتُه الآن (offline)
### خريطة المسارات الساكنة — 123 مسار، 14 خدمة
مُولّدة بفحص `@app.route` في كلّ خدمة (بلا تشغيل):
- `docs/openapi/API_MAP.md` — خريطة مقروءة لكلّ المسارات
- `docs/openapi/ROUTE_INVENTORY.json` — نفس البيانات للأدوات

توزيع المسارات:
| الخدمة | مسارات |
|--------|--------|
| raster-service | 31 |
| auth | 16 |
| odoo-bridge | 10 |
| supervisor-agent / video | 8 |
| guardrails / tts | 7 |
| soil / vegetation | 6 |
| actuator/agriai/edge/local-ai-rag | 5 |
| weather (stub) | 4 |

## ما تشغّله أنت (spec كامل)
`export_openapi.py` — يستدعي `app.openapi()` لكلّ خدمة (بلا uvicorn) ويولّد
`<service>.openapi.json` كامل (schemas/parameters/responses):
```bash
python3 export_openapi.py
```
آمن: لو فشل استيراد خدمة (حزمة ناقصة) يتخطّاها ويسجّل السبب، لا يتوقّف.

## لماذا التقسيم؟
- الخريطة الساكنة: **قيمة فوريّة** بلا تثبيت (فحص نصّي للكود)
- spec الكامل: يحتاج استيراد الخدمات فعليّاً (rasterio/redis ثقيلة) → بيئتك

## فائدة الـspec الكامل
Swagger UI/Redoc · توليد عملاء · اختبارات العقود · تدقيق أمني للمسارات المكشوفة

## التحقّق
- 626/626 roadmap (+4) · export_openapi.py يُترجم
- 123 مسار موثّق عبر 14 خدمة (فحص فعلي للكود)

## ملاحظة صدق
الخريطة الساكنة **مُولّدة فعليّاً** بفحص الكود (123 مسار حقيقي، لا مُختلَق).
سكربت التصدير **لم أشغّله** (بيئتي بلا fastapi) — لكنّه مُترجَم ومنطقه صحيح
(importlib + app.openapi() نمط قياسي). ستحصل على spec كامل بتشغيله. الخريطة
الساكنة قد تفوت مسارات مُضمَّنة عبر include_router (سجّلتُ عددها لكن لا
تفاصيلها) — سكربت التصدير يلتقطها كاملةً.
