# SAHOOL FastAPI requirements
# ============================
# Minimal pinned deps. PyJWT for HS256 (dev). pydantic for type validation.

fastapi==0.136.3
uvicorn[standard]==0.30.6
pydantic==2.13.4
PyJWT==2.8.0
python-multipart==0.0.9   # for form data

# Open-Meteo integration
httpx==0.27.0

# FIX (إقلاع الحاوية): api/main.py يستورد trial_engine→numpy؛ وzones_kmeans/
# core.spatial يستوردان numpy/scipy. غيابها ⇒ ModuleNotFoundError وتعطّل المنصّة.
numpy>=1.24.0
scipy>=1.10.0
PyYAML>=6.0  # FIX: core/crop_cards/loader.py + core/learning/calibration_loop.py يستوردان yaml
