fastapi==0.136.3
uvicorn[standard]==0.30.6
pydantic==2.13.4
httpx==0.27.0
python-multipart==0.0.18
# معالجة الراستر الفعليّة (ثقيلة — تُثبّت في بيئة التشغيل):
rasterio==1.3.0
numpy==1.26.0
redis==5.0.0
# قاعدة البيانات — مطلوب لجلب هندسة الحقل (geometry) من جدول fields
# وحفظ أصول الراستر (raster_assets). غيابه ⇒ db_persist يعود لوضع DB-less.
asyncpg==0.29.0
# تخزين كائنات اختياري (S3/MinIO) لـCOGs — مطلوب فقط عند ضبط S3_*؛
# مسار file:// يعمل دونه (الاستيراد محروس داخل object_store.py):
boto3==1.34.0
# Enterprise geospatial analytics/export (optional but enabled in production images):
pyarrow==16.0.0
shapely==2.0.0
geopandas==0.14.0

# DB fallback for tenant ownership and layer hydration
asyncpg==0.29.0
