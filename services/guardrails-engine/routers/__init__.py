"""حزمة راوترات guardrails-engine (تفكيك محفوظ السلوك).

كلّ وحدة هنا تُصدّر ``router = APIRouter()`` وتُضمَّن تلقائيّاً عبر
``router_registry.register_routers(app)`` (بلا prefix — المسارات تبقى كما هي).
نُقلت إليها مُعالِجات ``main.py`` حرفيّاً (``@app`` → ``@router``)؛ التبعيّات المشتركة
(المحرّك/النماذج/المساعِدات/توكن الخدمة) تبقى في ``main`` وتُشار إليها عبر ``main.X``.
"""
