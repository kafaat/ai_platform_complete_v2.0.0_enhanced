# SAHOOL sam2-inference — خادم استدلال SAM2 (GPU/CUDA).
#
# ملاحظة حرجة (صدق): torch بنسخة CUDA يُثبَّت في Dockerfile من فهرس CUDA الرسميّ
# (cu121) أو يأتي ضمن قاعدة pytorch/pytorch CUDA. لا تُثبّت torch من PyPI القياسيّ
# هنا (سيجلب بناء CPU). كذلك sam2 يُثبَّت من مستودع facebookresearch/sam2 (انظر
# Dockerfile) لأنّه غير منشور على PyPI باسم مستقرّ.
#
# pip-audit: شغّله على صورة الـGPU النهائيّة لديك (لا يمكن تثبيت torch هنا بلا GPU).

# خادم الويب (خفيف، يُترجَم مسار 503 بلا المكتبات الثقيلة).
fastapi==0.136.3
uvicorn[standard]==0.30.6
pydantic==2.13.4
httpx==0.27.0

# الجغرافيا/الرَّستر (قراءة COG عبر /vsicurl، تحويل قناع→مضلّع).
rasterio==1.3.10
shapely==2.0.6
numpy==1.26.4

# torch / sam2 يُثبَّتان في Dockerfile (CUDA) — انظر التعليقات أعلاه.
# torch==2.4.1  (CUDA cu121 — من Dockerfile، ليس من هنا)
# sam2          (من github.com/facebookresearch/sam2 — من Dockerfile)
