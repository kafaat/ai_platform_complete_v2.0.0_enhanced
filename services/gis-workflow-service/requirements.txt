# gis-workflow-service — رِندرِر خرائط النشر (الشريحة A). تبعيّات معزولة عن بقيّة الخدمات.
# pip-audit نظيف عند الإضافة (matplotlib 3.11.0 / numpy 2.x — لا ثغرات معروفة).
matplotlib>=3.9,<4
numpy>=1.24
