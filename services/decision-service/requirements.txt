# fastapi/pydantic مثبَّتان على إصدار المنصّة (`services/sahool-platform/api/requirements.txt`):
# كانت هذه الخدمةُ الوحيدةَ على 0.115.6/2.10.4، وstarlette 0.41.3 التابعةُ لها تحمل ستَّ ثغراتٍ
# معروفة (PYSEC-2026-248/249/1941/1942/2280/2281) لا تزول إلّا بالترقية. مقيس قبل/بعد:
# جناحُ الخدمة 147 ناجحة · 134 متخطّاة (تحتاج PostgreSQL) على الزوجَين سواءً.
fastapi==0.136.3
uvicorn==0.34.0
httpx==0.28.1
pydantic==2.13.4

asyncpg==0.30.0

# مخزن nonce لتأكيد هويّة العامل (main.py:_claim_worker_assertion_once). كان
# يُستورَد كسولاً بلا إعلان، فالحاوية تُقلِع خضراء ثمّ يفشل أوّل طلب مربوط بالهويّة
# بـModuleNotFoundError يظهر «مخزن الإعادة غير متاح» — DECISION-SERVICE-REDIS-DEPENDENCY-GAP-01.
redis>=5.0.0
