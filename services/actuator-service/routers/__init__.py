"""حزمة راوترات actuator-service (سقالة التفكيك).

كلّ وحدة هنا تُصدّر ``router = APIRouter()`` وتُضمَّن تلقائيّاً عبر
``router_registry.register_routers(app)`` (بلا prefix — المسارات تبقى كما هي).
نُقلت المُعالِجات حرفيّاً من ``main.py`` (نمط تفكيك المنصّة المحفوظ-السلوك): التبعيّات
الأمنيّة والمسارات والأوامر مطابقة بايت-ببايت؛ الرموز المشتركة تبقى في ``main`` وتُشار
إليها عبر ``main.X``.
"""
