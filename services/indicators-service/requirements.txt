# indicators-service خدمة صحّيّة رفيعة (fastapi+os فقط في main.py) —
# أكّد حارس عقد الحاوية أنّ asyncpg/nats-py/redis/prometheus-client/pydantic غير مُستخدَمة
# مباشرةً (pydantic تأتي متعدّية عبر fastapi).
fastapi==0.136.3
uvicorn[standard]==0.30.6

httpx==0.28.1
