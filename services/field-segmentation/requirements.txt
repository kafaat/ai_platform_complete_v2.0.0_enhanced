# SAHOOL field-segmentation — تبعيّات خفيفة قصداً.
# لا torch / لا أيّ مكتبة ML هنا: المساران auto/hybrid عقدُ خدمة فقط حتى يُوصَل
# نموذج حقيقيّ (SAM2/GeoSAM) في بيئة بأوزان+GPU. المسار اليدويّ منطق صرف.
fastapi==0.136.3
uvicorn[standard]==0.30.6
httpx==0.27.0
pydantic==2.13.4
