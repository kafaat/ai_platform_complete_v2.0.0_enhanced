{
  "$comment": "WS-C.1b/c boundary guard — vapour-pressure/ET0/GDD formula kernels live ONLY in the Weather Engine (services/weather-service/*). Enforced by scripts/ci/weather_engine_formula_guard.py. Zero-Legacy is now LOCKED: temporary_legacy_allowlist is empty and must STAY empty — every platform ET0 kernel has been migrated to consume the Weather Engine product (C.1b delegation + C.2 BFF). Re-adding an entry here fails CI by design; implement any new formula in services/weather-service and consume its product.",
  "canonical": "services/weather-service/vapor_pressure.py",
  "svp_fingerprint": [
    "0.6108",
    "17.27"
  ],
  "temporary_legacy_allowlist": []
}
