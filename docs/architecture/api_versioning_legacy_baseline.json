{
  "$comment": "أساس مُجمَّد لقائمة السماح القديمة. api_versioning_policy_guard كان **كاشف انحراف لا بوّابة سياسة**: يقارن القائمة المُلتزَمة بالمُعاد حسابها، وعلاجه المُوثَّق «أعِد التوليد» — فكلّ مسار غير مُصدَّر جديد يُقبَل بالالتزام به. هذا الأساس يمنع **النموّ**: القائمة تتقلّص ولا تنمو. تقلّصت 749 ⇒ 232 بتصحيح المُصنِّف لا بإعفاء، ثمّ 232 ⇒ 231 بأوّل شريحة منتَج حقيقيّة، ثمّ 231 ⇒ 230 باستبعاد بنيويّ لملفّات الاختبار، ثمّ 230 ⇒ 229 بتصنيف `/runtime-identity` بنيةً تحتيّة لا عملَ تجاريّاً، ثمّ 229 ⇒ 226 بشريحة الوكيل A الأولى (عقد MCP الذرّي عبر خمس خدمات)، ثمّ 226 ⇒ 225 بشريحة الوكيل A الثانية (عقد `/products` العابر للخدمات)، ثمّ 225 ⇒ 222 بشريحة الوكيل A الثالثة (أوامر actuator-service). ثمّ 222 ⇒ 221 بشريحة الوكيل D الأولى (field-segmentation، أُعيد بناؤها ثلاث مرّات بعد تعارضات متتالية مع اندماج ثلاث شرائح للوكيل A). ثمّ 221 ⇒ 213 بشريحة الوكيل B الأولى (guardrails-engine + supervisor-agent). ثمّ 213 ⇒ 205 بشريحة الوكيل C الأولى (raster-service imagery search). ثمّ 205 ⇒ 113 بإصلاح عمى تصنيفيّ ثالث: تركيب `APIRouter(prefix=...)` مع نصّ الديكوريتر (99 موضع). ثمّ 113 ⇒ 107 بشريحة الوكيل D الثانية (edge-inference + knowledge-graph). ثمّ 107 ⇒ 103 بشريحة الوكيل D الثالثة (tts-service + local-ai-rag). ثمّ 103 ⇒ 95 بشريحة الوكيل D الرابعة (ai_agronomist). ثمّ 95 ⇒ 89 بشريحة الوكيل D الخامسة (rag-retrieval + agriai-engine).",
  "gap": "API-VERSIONING-GUARD-IS-A-MIRROR-01",
  "measured_on": "permanent-compatibility-contract (branched from origin/main after PR #734 merged, 2026-07-31)",
  "ceiling": 0,
  "measurement": "**صفر** مسار `legacy_unversioned_business` — الدَّين القابل للهجرة أُغلِق بالكامل. السبعة التي كانت تُحسَب ديناً صُنِّفت بعقد مركزيّ صريح (`docs/architecture/permanent_compatibility_contract.json`، مفتاحه method + مسار مُطبَّع) إلى فئتين مُسمّاتين مرئيّتين: `permanent_health_compat_alias` (٤) و`permanent_compatibility_gateway` (٣). السقف ٠ يعني: أيّ مسار غير مُصدَّر جديد لا يطابق العقد يُسقِط CI فوراً؛ وإضافة اسم بديل دائم تتطلّب تعديل ملفّ العقد صراحةً (adjudication مرئيّ). سجلّ الشرائح في `to_close`.",
  "known_misclassified_count": 0,
  "to_close": "أُغلِق شطر التصنيف في 2026-07-29: _classify صار يعرف مقطع الإصدار في البادئة (^(?:/api)?/v[0-9]+) بدل /v1/ وحده. **الشريحة الأولى (2026-07-30):** sam2-inference `POST /predict` ⇒ `POST /v1/predict` (232 ⇒ 231) — استدعاء واحد متتبَّع (`SEGMENTATION_INFERENCE_URL` من `services/field-segmentation/main.py`)، حُدِّث في docker-compose.v9.yml/v9.gpu.yml/fixed.yml + `scripts/ci/v9_gpu_contract_gate.py` + اختباري tests_v9. **تصحيح الجرد ذاته (2026-07-30):** `collect()` في scripts/ci/api_versioning_policy_guard.py كان يمسح services/**/*.py بلا استبعاد بنيويّ لملفّات الاختبار، فصنّف `GET /probe` داخل services/sahool-platform/tests/test_correlation_middleware.py:18 كمسار عمل غير مُصدَّر حقيقيّ — false positive وحيد في الجرد كلّه (تحقّق شامل عبر كل التصنيفات الأربعة الأخرى، لا نظير له). أُضيف استبعاد بنيويّ (`/tests/` في المسار، أو البادئة `tests/`، أو اسم الملفّ يبدأ بـ`test_`) بدل حذف السطر يدويّاً من الناتج المولَّد (231 ⇒ 230). **تصحيح تصنيف ثانٍ (2026-07-30):** `GET /runtime-identity` (sahool-platform · soil-service · weather-service) كان يُصنَّف `legacy_unversioned_business` رغم أنّ CLAUDE.md يجمعه صراحةً مع `/healthz`/`/readyz`/`/metrics` كمسار بنية/provenance، وأنّ `platform_route_ownership_guard`'s الحارس المستقلّ يصنّفه بنية تحتيّة فعلاً (`test_p0_platform_route_ownership_guard.py:91`) — تناقض بين حارسين. المسار محكوم بعقد مُجمَّد (`docs/architecture/platform_route_placement_contract.json`)، ومُستهلَك في `functional_probe_runner.py`'s `identity_path` عبر ثلاثة ملفّات probe، ومُختبَر بحضوره الحرفيّ في اختبارَي الشهادة (`test_runtime_build_identity_attestation.py`، `test_platform_route_placement_guard.py`) — نقله فعليّاً (لا تصنيفه) كان سيمسّ سلسلة التحقّق الحيّ/الشهادة المحظور لمسها. الإصلاح تصنيفيّ بحت: أُضيف `/runtime-identity` إلى قائمة `infra` في `_classify()` — لا تغيير مسار، لا تغيير ملفّ (230 ⇒ 229، وweather-service خرجت كليّاً من القائمة إذ كان هذا مسارها الوحيد). المتبقّي 229 حقيقيّاً — كلّ واحد شريحته. **شريحة الوكيل A الأولى (2026-07-30، MCP protocol):** عقد ذرّي واحد عبر خمس خدمات mcp_servers هُوجِر معاً إذ لا معنى لهجرة نصفه: `GET /mcp/v1/tools` (4 ملفّات: generic_context_server.py:133 · sentinel_hub_server.py:198 · weather_server.py:84 · wofost_server.py:50) ⇒ `GET /v1/mcp/tools`؛ `POST /mcp/v1/tools/call` (5 ملفّات: الأربعة أعلاه + market_server.py:577) ⇒ `POST /v1/mcp/tools/call`؛ `GET /mcp/v1/tools/list` (market_server.py:545 فقط) ⇒ `GET /v1/mcp/tools/list`. الهدف `/v1/mcp/...` لا `/mcp/v1/...`: مصنِّف الحارس (`_VERSIONED = ^(?:/api)?/v[0-9]+`) يتطلّب مقطع الإصدار **في البادئة**، وسابقة الخدمات المستقلّة في هذا المستودع (sam2-inference `/v1/predict`، soil-service `/v1/soil/...`) بادئة `/v1/` عارية لا `/api/v1/` — فحُوفِظ على بنية المسار الدلاليّة (`mcp/tools[...]`) مع نقل مقطع الإصدار إلى الصدارة. **كل مستدعٍ حرفيّ حُدِّث:** `services/supervisor-agent/mcp_client.py:65,97` (GET/POST) · `services/mcp_servers/shared/streamable_http.py:101,103` (GET/POST) · docstring توضيحيّ في `services/sahool-platform/core/mcp/independent_servers.py:4` · مراجع حيّة في `docs/MARKET_SYSTEM.md` (رسم ASCII + مثال curl) و`docs/api/BACKEND_FRONTEND_COVERAGE.md` (جدول) و`sahool-brain/runbooks/full-stack-activation.md` (مثال curl) · اختبارات `tests_v9/test_mcp_functional.py` (docstrings + نداءات TestClient) و`tests_v9/test_mcp_servers.py` و`tests_v9/test_end_to_end.py` (عناوين http://localhost:809x). **لم تُمسّ عمداً** أربعة تقارير تدقيق تاريخيّة مؤرَّخة تصف حالة كود سابقة (`docs/audits/NATIVE_LIVE_VERIFICATION.md`، `docs/audits/CRITICAL_REVIEW_RESPONSE.md`، `docs/audits/CODE_REVIEW_REPORT.md`، `docs/CRITICAL_REVIEW_ROUND3_2026-06.md`) — سجلّ تاريخيّ لا مرجع API حيّ، تعديلها يُزيّف ما وُصِف وقتها. **مُلاحَظ خارج النطاق (لم يُصلَح):** `MCP_SENTINEL_HUB_URL`/`MCP_WEATHER_URL`/`MCP_WOFOST_URL`/`MCP_MARKET_URL` (docker-compose.*.yml + `services/supervisor-agent/main.py:72-76`) تحمل قيمة `.../mcp/v1` كبادئة base_url، بينما `mcp_client.py` يستدعي بمسار مطلق `/v1/mcp/tools...` — httpx (`Client._merge_url`) **يُلحِق** لا يستبدل، فالمسار الفعليّ الصادر مضاعَف (`/mcp/v1/v1/mcp/tools`) — عِلّة سابقة الوجود لهذه الشريحة، غير مرتبطة بنقل المسار، خارج نطاق الوكيل A، تستحقّ فجوة مستقلّة. الأثر: 229 ⇒ **226** (3 عقود فريدة أُغلِقت)، والجرد الخام: legacy_unversioned_business المواضع 247 ⇒ 237، والعقود الفريدة 229 ⇒ 226. **شريحة الوكيل A الثانية (2026-07-30، عقد `/products` العابر للخدمات):** `GET /products` كان يظهر مرّتين بنصّ (method, path) واحد في خدمتين مختلفتين تماماً — `services/mcp_servers/market_server.py:631` (منتجات MCP للسوق الزراعيّ، منفذ 8094) و`services/odoo-bridge/routers/catalog.py:70` (منتجات ERP عبر Odoo، منفذ 8126) — فالحارس يُوحِّدهما في عقد فريد واحد في القائمة رغم اختلاف الخدمة والدلالة. هذا العقد مُصرَّح للوكيل A حصراً بلمس ملفّ `odoo-bridge` (نطاق الوكيل C عادةً) لسطر واحد فقط. كلاهما ⇒ `GET /v1/products`. **كل مستدعٍ حرفيّ حُدِّث:** `tests_v9/test_mcp_functional.py:232` (نداء `TestClient` + نصوص التوثيق) · `docs/MARKET_SYSTEM.md` (رسم ASCII + مثال curl لمنفذ 8094 + مقتطف Dart توضيحيّ) · `docs/ODOO_INTEGRATION.md` (مثال curl لمنفذ 8126) · `docs/api/BACKEND_FRONTEND_COVERAGE.md` (صفّ جدول) · `docs/capability-registry/domains/farm_management.yaml:793` (مرجع قدرة) · `docs/openapi/ROUTE_INVENTORY.json:84` (لقطة مسارات ثابتة). كما حُدِّث `config/platform_catalog_overrides.yml` (قرار حوكمة يدويّ التصنيف `service_scoped_semantics` لتكرار `/products` عبر خدمتين — أُعيد ربطه بالمسار الجديد `/v1/products` مع الإبقاء على نصّ الحكم ذاته، إذ الفرق الدلاليّ بين منتجات ERP ومنتجات MCP لم يتغيّر، فقط المسار). **مُلاحَظ:** لا سجلّ nginx ولا docker-compose يوجّه به `/products` كمسار خاصّ (بحث شامل، صفر نتائج). الأثر: 226 ⇒ **225** (عقد فريد واحد أُغلِق رغم موضعين، لتطابق نصّ method+path). الجرد الخام: legacy_unversioned_business المواضع 237 ⇒ 235، والعقود الفريدة 226 ⇒ 225. **شريحة الوكيل A الثالثة (2026-07-30، أوامر actuator-service — نطاق حذر مرتفع):** `POST /command` و`GET /commands` (`services/actuator-service/routers/commands.py:24,69`) و`GET /idempotency/metrics` (`services/actuator-service/routers/metrics.py:17`) ⇒ `/v1/command`·`/v1/commands`·`/v1/idempotency/metrics`. هذا نطاق «حذر مرتفع» بنصّ الخطّة (تشغيل أجهزة فيزيائيّة) — بحث شامل عبر الشجرة كاملةً (`frontend/src`، `mobile/sahool_app/lib`، `nginx/*.conf`، `docker-compose*.yml`، كلّ خدمة أخرى) لم يجد **أيّ** مستدعٍ حرفيّ خارج اختبارات الخدمة نفسها؛ المسار محميّ أصلاً خلف `FEATURE_MANUAL_ACTUATOR_COMMANDS` (معطَّل افتراضيّاً) و`Depends(main._verify_token)` + `main._authorize_device_control`. **كل مستدعٍ حرفيّ حُدِّث:** `services/actuator-service/test_actuator_safety.py:163`، `services/actuator-service/test_actuator_router_decomposition_guard.py:36-37` (`_CRITICAL_ROUTES`)، `docs/capability-registry/domains/irrigation.yaml:420,423,431` (مرجع يدويّ، لا مولِّد سكربت يكتبه). **تصويب عمليّاتيّ اكتُشف أثناء هذه الشريحة:** `docs/api/BACKEND_FRONTEND_COVERAGE.md` يُعلِن صراحةً في رأسه «لا تُحرَّر يدويّاً — تُولَّد بـ`scripts/ci/endpoint_ui_coverage_gate.py --report`»، لكنّ شريحتَي الوكيل A الأولى والثانية عدَّلتاه يدويّاً (خطأً) بدل تشغيل المولِّد — لم يُكتشَف فوراً لأنّ CI (`endpoint-ui-coverage-gate`) يفحص عقد التغطية الملزِم لا تطابق الملفّ المولَّد حرفيّاً، فلم يفشل. صُحِّح في هذه الشريحة بتشغيل المولِّد الفعليّ (أنتج 262 سطراً تغيّرت — الملفّ كان بائتاً أصلاً قبل شريحتَي الوكيل A أيضاً، لا بسببهما فقط). **درس مُسجَّل:** أيّ ملفّ يُعلِن عن نفسه أنّه مولَّد يُعامَل ككذلك حتى لو لم يظهر في `verify_all_generated.py` (الذي يكتشف فقط سكربتات بعلم `--check` مذكورة في workflow — هذا السكربت يُستدعى بلا `--check` في `ci.yml:17`، فهو خارج مدى المكنسة رغم كونه مولِّداً حقيقيّاً). **سلسلة تبعيّات أطول من الشريحتين السابقتين:** إعادة توليد `endpoint_ui_coverage_gate.py --report` + مسارات actuator المُهاجَرة استلزمت تسلسلاً إضافيّاً اكتشفه `verify_all_generated.py` تدريجيّاً عبر خمس دورات: `runtime_contract_generator.py --generate` ⇒ `runtime_verification_harness.py --generate` ⇒ `compose_runtime_target_resolver.py --generate` + `integration_runtime_governance_closure.py --generate` + `path3_runtime_readiness_closure.py --generate` + `runtime_evidence_ingestion.py --generate` ⇒ إعادة `integration_runtime_governance_closure.py --generate` مرّة أخرى (تغيّر مُدخَله بعد الجولة السابقة) — كلّ جولة تلت `git add -A` ثمّ إعادة بناء حزمة الإصدار، حتى استقرّ `verify_all_generated.py` على rc=0. الأثر: 225 ⇒ **222** (3 عقود فريدة أُغلِقت، مطابقة لعدد المواضع إذ لا تكرار عبر خدمات لهذه الثلاثة). الجرد الخام: legacy_unversioned_business المواضع 235 ⇒ 232، والعقود الفريدة 225 ⇒ 222. **شريحة الوكيل D الأولى (2026-07-30، field-segmentation):** `POST /segment` ⇐ `POST /v1/segment` (`services/field-segmentation/main.py:390`)، خدمة أحادية الملفّ بمسار عمل وحيد. المستهلك الحقيقيّ الوحيد ليس عميلاً مكتوباً بمسار ثابت بل بروكسي sahool-platform الشفّاف (`services/sahool-platform/api/routers/service_proxy.py`، `@router.api_route(\"/api/segmentation/{path:path}\")`) الذي يُمرِّر `path` حرفيّاً دون إعادة كتابة بادئة. كلّ مستدعٍ حُدِّث: `services/field-segmentation/test_segmentation.py` (22 موضع نداء + اختبار تزييف جديد)، `frontend/src/services/api.ts`+`api.test.ts`، `scripts/e2e/segmentation_platform_live_gate.py`، `tests_v9/test_segmentation_remaining_ui_live_gate_20260703.py`، `docs/SAM2_DEPLOYMENT.md`. **أُعيد بناء الفرع ثلاث مرّات:** فُتِح PR #711 أصلاً على `7563239b` (229⇒228). اندمج #712 (229⇒226) ⇒ إعادة بناء من `69572be7` (226⇒225). قبل الدفع اندمج #715 (226⇒225) ⇒ إعادة بناء ثانية من `160b0798` (225⇒224). قبل الدفع أيضاً اندمج #716 (225⇒222، شريحة الوكيل A الثالثة actuator) ⇒ إعادة بناء ثالثة من `4d4f2d90` (222⇒221 الصحيح النهائيّ)، بنفس الرقعة المصدريّة في كلّ مرّة، إعادة توليد كاملة من الصفر في كلّ مرّة. **فجوة ثانية أُغلِقت ضمن نفس الشريحة (`CAPABILITY-LINKER-SCANS-AGENT-WORKTREES-01`):** أثناء إعادة البناء الثانية، `capability_linker.py --apply` كشف عن مسح خام لنظام الملفّات (`ROOT.rglob`) لا يستبعد `.claude` — فالتقط أدلّة worktree الوكلاء الأربعة المتوازية وكتب مسارات `.claude/worktrees/...` في `capabilities/registry/capabilities.json` المُلتزَم، مُسقِطاً `capability-registry` على CI الحقيقيّ. أُصلِح بإضافة `.claude` إلى `EXCLUDED_DIRS` (نفس نمط `_is_test_file`)، مع اختبار تزييف جديد `test_discover_files_excludes_claude_worktree_directories` في `tests/architecture/test_capability_linker.py`. **الدرس الحاسم لبقيّة الوكلاء:** حين يعمل أكثر من وكيل بالتوازي على الجرد المولَّد المشترك، الدمج يجب أن يكون **متسلسلاً واحداً تلو الآخر** — لا افتراض ثبات الأساس بين الفتح والدمج، فقد يتكرّر التعارض أكثر من مرّة لنفس الـPR إن تأخّر دمجه بينما تندمج شرائح إخوة. الأثر: 222 ⇒ **221** (عقد فريد واحد أُغلِق). **شريحة الوكيل B الأولى (2026-07-30، guardrails-engine + supervisor-agent):** ٢٢١ ⇒ **٢١٣** (8 عقود فريدة). النطاق الأصليّ المُسنَد للوكيل B كان يشمل sahool-platform أيضاً (107 موضع)، لكن 99 منها كانت مُصدَّرة فعلاً عبر `APIRouter(prefix=\"/v1/...\")` — عمى تصنيفيّ لا يراه الحارس (يقرأ نصّ الديكوريتر فقط)، فتُرِكت بلا لمس صراحةً. المُغلَق فعليّاً: `services/guardrails-engine/routers/validation.py` (`POST /validate`، `POST /approve/{workflow_id}`، `GET /workflow/{workflow_id}` ⇐ `/v1/*` — الراوتر بلا `prefix=` ومُركَّب مباشرةً فنصّ الديكوريتر هو المسار الفعليّ)، و`services/supervisor-agent/routers/agent.py` (خمسة مسارات: `/agent/query`، `/agent/optimize`، `/agent/tools`، `/agent/journal/{invocation_id}`، `/agent/actuator-audit` ⇐ `/v1/agent/*`). **كلّ مستدعٍ حرفيّ حُدِّث:** `services/sahool-platform/core/field_intelligence_adapters.py` و`services/supervisor-agent/main.py` (نداءات `GUARDRAILS_URL` المباشرة)، `frontend/src/hooks/useApi.ts` (بوّابة `/api/guardrails/v1/validate` — nginx's `/api/guardrails/` تمرير شفّاف بلا rewrite)، ثلاث تشكيلات nginx (`nginx/nginx.fixed.conf`، `nginx/nginx.v9.conf`، `frontend/nginx.conf` — إعادة كتابة `/api/agent/` إلى مسار `.../v1/agent/` غير جذريّ، فعقد البوّابة الخارجيّ `/api/agent/*` لم يتغيّر)، حارس تفكيك guardrails-engine، مرجع قدرة، وستّة مواقع اختبار نداء مباشر. **إعادة بناء واحدة من `origin/main` بعد اندماج PR #711** (نفس نمط شرائح الوكيل D) — الفرع الأصليّ كان مبنيّاً على `7563239b` القديم، فأُعيد بناؤه بالكامل من `b0067e41` (بعد اندماج PR #711 وإصلاح `CAPABILITY-LINKER-SCANS-AGENT-WORKTREES-01`). تعارض محتوى حقيقيّ واحد (لا تعارض مصنوعات فقط) ظهر أثناء إعادة التطبيق: `tests_v9/test_api_versioning_policy_guard.py` — كلّ شريحة من الوكيل A أضافت اختبار تزييف خاصّاً بها إلى نفس الملفّ؛ حُلّ بدمج ثلاثيّ (`git apply --3way`) ودمج يدويّ بسيط (دمج الدوالّ الأربع دون تداخل منطقيّ فعليّ). **الأثر:** legacy_unversioned_business المواضع والعقود الفريدة كلاهما ٢٢١ ⇒ **٢١٣**. **شريحة الوكيل C الأولى (2026-07-30، raster-service imagery search):** ٢١٣ ⇒ **٢٠٥** (8 عقود فريدة). ثمانية مسارات بحث صور في `services/raster-service/routers/imagery_search.py` — غير مُصدَّرة منذ ما قبل عرف `/v1` في هذه المنصّة — انتقلت إلى `/v1/imagery/*`: `GET /imagery/search/recent`، `GET /imagery/search/season`، `GET /imagery/best`، `POST /imagery/search`، `GET /imagery/search/radar`، `GET /imagery/search/landsat`، `GET /imagery/search/landsat-thermal`، `GET /imagery/dem`. **مستهلكان حقيقيّان وُجِدا بالبحث الشامل** (الستّة الباقية بحث عام بلا مستدعٍ داخليّ — لا سجلّ docker-compose/nginx إذ متغيّرات البيئة تحمل عنوان القاعدة فقط لا جزء المسار): `services/sahool-platform/api/raster_service_client.py:390` (`get_best_imagery_scene`) و`:413` (`search_imagery_scenes`). **كلّ مستدعٍ حرفيّ حُدِّث:** `tests_v9/test_raster_endpoint_auth_coverage.py` (قوائم `PUBLIC_CATALOG`/`SERVICE_ONLY`)، `services/sahool-platform/tests/test_p2_1_imagery_automation_raster_facade_guard.py`، دقّة docstring/تعليق فقط في `imagery_automation.py`/`imagery_providers.py`/`routers/{fields,analysis}.py`، ووثائق تتبّع (`capabilities/registry/capabilities.json`، `docs/capability-registry/domains/{farm_management,gis,satellite}.yaml`، `docs/openapi/{API_MAP.md,ROUTE_INVENTORY.json}`، `docs/audits/ADDITIONAL_PROVIDERS.md`، `skills/sahool-gis/TERRAIN_DEM.md`). `docs/api/BACKEND_FRONTEND_COVERAGE.md` أُعيد توليده عبر المولِّد الفعليّ (`endpoint_ui_coverage_gate.py --report`) لا تحريراً يدويّاً — الوكيل C طبّق درس الوكيل A المُسجَّل صراحةً في هذا الملف نفسه بدل تكراره. **أُعيد بناء الفرع أربع مرّات إجمالاً** عبر الجلسة (الوكيل C أعاد البناء بنفسه ثلاث مرّات تباعاً مع اندماج شرائح الوكيل A: `7563239b`⇒`69572be7`⇒`160b0798`⇒`4d4f2d90`)، ثمّ إعادة بناء رابعة نهائيّة من `b7fd2643` (بعد اندماج PR #711 وPR #713) — بلا أيّ تعارض محتوى حقيقيّ في أيّ مرحلة (الرقعة المصدريّة النهائيّة طُبِّقت بلا نزاع)، فقط تعارضات مصنوعات مولَّدة مُعاد توليدها بالكامل في كلّ مرّة. **الأثر:** legacy_unversioned_business المواضع والعقود الفريدة كلاهما ٢١٣ ⇒ **٢٠٥**. **تصحيح تصنيف ثالث (2026-07-30) — عمى `APIRouter(prefix=...)`:** `_routes()` في `scripts/ci/api_versioning_policy_guard.py` كانت تقرأ نصّ الديكوريتر وحده (`@router.get(\"/plan\")`) بلا تركيب بادئة الراوتر نفسه (`router = APIRouter(prefix=\"/v1/phase9/autonomy\")` في نفس الملفّ) — فمسار مُصدَّر فعلاً على الشبكة (`/v1/phase9/autonomy/plan`) صُنِّف «غير مُصدَّر» لأنّ نصّه الحرفيّ لا يبدأ بمقطع إصدار. هذا هو العمى التصنيفيّ المُوثَّق OPEN في شريحة الوكيل B الأولى أعلاه (99 موضع sahool-platform مُسنَدة للوكيل B تُركت بلا لمس). **بحث شامل عبر المستودع كلّه عن `include_router(..., prefix=...)` — صفر نتائج**: البادئة الوحيدة المُستعمَلة فعليّاً في هذا المستودع هي `APIRouter(prefix=...)` في نفس ملفّ الراوتر (6 ملفّات فقط تستعملها: `phase9_autonomous_farm_os.py`، `phase10_continuous_learning.py`، `phase11_federated_agents.py`، `phase12_marketplace_ecosystem.py`، `routers/gis_cloud_native.py`، `routers/irrigation_engineering.py`)، فالتركيب محليّ الملفّ بحت — لا حاجة لتتبّع `include_router` عبر ملفّات main.py. **الإصلاح:** `_router_prefixes(tree)` جديدة تفحص كلّ `APIRouter(prefix=\"...\")` في شجرة AST للملفّ نفسه وتُخرِج خريطة {اسم المتغيّر: البادئة}؛ `_routes()` تُركِّب البادئة مع نصّ الديكوريتر حين يكون كائن الاستدعاء (`@X.get(...)`) اسماً معروفاً في الخريطة، وتترك مسارات `@app.get(...)` (بلا راوتر) كما هي. **القياس الدقيق:** 99 موضعاً خام انقلب تصنيفه من `legacy_unversioned_business` إلى `versioned` عبر الملفّات الستّة (`phase9`:11 · `phase10`:18 · `phase11`:11 · `phase12`:17 · `gis_cloud_native`:30 · `irrigation_engineering`:12). من بين هذه الـ99، **95 نصّاً فريداً** (method+path) — تكرار داخليّ اثنان: `POST /cycle` يظهر أربع مرّات (`phase9_autonomous_farm_os.py:148`، `phase10_continuous_learning.py:197`، `phase11_federated_agents.py:171`، `phase12_marketplace_ecosystem.py:239`) و`POST /models/register` مرّتين (`phase9_autonomous_farm_os.py:175`، `phase10_continuous_learning.py:269`). من الـ95 هذه، **ثلاثة نصوص بقيت في قائمة السماح** لأنّ خدمة أخرى ما زالت تُعلِن نفس النصّ الحرفيّ بلا إصدار — عقود ذرّية عابرة للخدمات مُوثَّقة مسبقاً في شريحة الوكيل B (`POST /plan` ⇐ `agriai-engine/main.py:311`، `GET /stac` و`GET /stac/collections` ⇐ `raster-service/routers/stac.py:17,25`)؛ سُمِح للوكيل A سابقاً بلمسهما لسبب مماثل لكن الجانب الآخر بقي بلا تحديد، إذ التصنيف كان لا يزال يعرض جانب sahool-platform خطأً كغير مُصدَّر فبدا العقد مُغلَقاً جزئيّاً بالفعل. **الأثر النهائيّ:** 95 - 3 = **92 عقداً فريداً أُغلِق** من قائمة السماح — 205 ⇒ **113**. **البرهان بالتكذيب:** `tests_v9/test_api_versioning_policy_guard.py::test_router_prefix_is_composed_with_route_path` (اختبار وحدة على مصنَّع اصطناعيّ يثبّت `_router_prefixes`/`_routes` معاً) و`test_known_prefixed_sahool_platform_routers_classify_as_versioned` (اختبار تكامل يؤكّد أنّ كلّ مسار في الملفّات الستّة يُصنَّف `versioned` بعد التركيب) — كلاهما فشل باسم المسار المتسرِّب حرفيّاً عند إعادة الإصلاح الأصليّ (نصّ الديكوريتر وحده بلا تركيب)؛ استعادة الإصلاح أعادتهما أخضرَين. **حدّ صدق:** الإصلاح مقصور على `APIRouter(prefix=...)` محليّ الملفّ فقط لأنّه النمط الوحيد المرصود فعليّاً؛ إن استُحدِث مستقبلاً نمط `include_router(..., prefix=...)` عابر للملفّات فلن يراه هذا الإصلاح، ويحتاج حينها امتداداً منفصلاً يتتبّع استدعاءات `include_router` عبر main.py لكلّ خدمة. **شريحة الوكيل D الثانية (2026-07-30، edge-inference + knowledge-graph):** ١١٣ ⇒ **١٠٧** (٦ عقود فريدة، بلا تكرار داخليّ أو عبر خدمات). كلا الملفّين يُعلِنان مسارات بلا `prefix=` (`@app.*` مباشرة)، فنصّ الديكوريتر هو المسار الفعليّ. **edge-inference (`services/edge-inference/main.py`):** `POST /inference/pest-detect:199` ⇐ `POST /v1/inference/pest-detect`، `POST /inference/yield-estimate:261` ⇐ `POST /v1/inference/yield-estimate`، `POST /sync/trigger:327` ⇐ `POST /v1/sync/trigger`. **knowledge-graph (`services/knowledge-graph/main.py`):** `POST /nodes:119` ⇐ `POST /v1/nodes`، `POST /edges:127` ⇐ `POST /v1/edges`، `GET /edges:136` ⇐ `GET /v1/edges`. **مستدعيات حقيقيّة وُجِدت بالبحث الشامل — فقط `pest-detect` و`GET /edges` كانا يُستدعيان خارج ملفّيهما وملفّات اختبارهما**: `mobile/sahool_app/lib/services/api_service.dart:480` (نداء Dio حرفيّ `/api/edge/inference/pest-detect` عبر بروكسي `sahool-platform` الشفّاف `api/routers/service_proxy.py:132` — نفس نمط field-segmentation، المسار يُمرَّر حرفيّاً بلا إعادة كتابة، فتغيّر ما يُرسِله العميل رغم أنّ كود البروكسي نفسه لا يُشفِّر المسار)، `services/video-processor/main.py:270` (نداء خدمة-لخدمة `f\"{EDGE_INFERENCE_URL}/inference/pest-detect\"`)، و`services/ai_agronomist/ai_evidence_runtime.py:623` (نداء خدمة-لخدمة `GET /edges` مع `X-Tenant-Id` مُمرَّر — C2). `yield-estimate` و`sync/trigger` و`POST /nodes` بلا مستدعٍ خارجيّ متتبَّع (مُصنَّفة `internal` في `docs/api/BACKEND_FRONTEND_COVERAGE.md` قبل هذه الشريحة، بلا استهلاك متتبَّع بعدها أيضاً — لا شيء يستدعيها إلّا اختباراتها). **بوّابة nginx (`nginx/nginx.v9.conf`):** `/api/edge/` تمرّ عبر `platform_backend` (بروكسي شفّاف كما ذُكِر، لا حاجة لتعديل nginx نفسه — المسار الخارجيّ `/api/edge/` نفسه ثابت، التغيير فقط في ما يُرسِله العميل بعده). `/api/knowledge-graph/` تمرّ مباشرةً إلى `kg_backend` بشرطة مائلة زائدة (`proxy_pass http://kg_backend/;`) — نمط `/api/agent/` نفسه من شريحة الوكيل B (PR #713): `proxy_pass` عُدِّل إلى `http://kg_backend/v1/;` ليبقى العقد الخارجيّ `/api/knowledge-graph/...` ثابتاً رغم عدم وجود مستهلك خارجيّ متتبَّع لهذه البوّابة تحديداً (بحث شامل في frontend/mobile — صفر نتائج؛ العقد يبقى متّسقاً احتياطاً). `nginx.light.conf`/`nginx.unified.conf` لا يملكان بوّابة `/api/knowledge-graph/`، وبوّابتا `/api/edge/` فيهما تمرّان مباشرةً إلى `edge_backend` بمرور شفّاف (بلا مسار مُلتصِق) — لا حاجة لتعديلهما. **وثائق حُدِّثت:** `RUNBOOK.md:76`، `docs/openapi/API_MAP.md:43-45`، `docs/openapi/ROUTE_INVENTORY.json:46-48` (لقطات ساكنة مُعلَنة «مُولَّدة آليّاً» لكن بلا مولِّد حيّ في `scripts/`، حُدِّثت يدويّاً — نفس القرار المُتَّخَذ في شرائح سابقة لملفّات مماثلة)، `docs/api/BACKEND_FRONTEND_COVERAGE.md` (أُعيد توليده فعليّاً عبر `endpoint_ui_coverage_gate.py --report`، لا تحريراً يدويّاً). **تُركت عمداً — سجلّ تاريخيّ:** `docs/history/EDGE_REVIEW.md` (تقرير مراجعة مؤرَّخ يصف حالة كود سابقة، تعديله يُزيّف ما وُصِف وقتها) و`docs/BEST_PRACTICES_BENCHMARK_2026-06.md` (تقرير مقارنة بأفضل الممارسات مؤرَّخ بذاته P0-P3، نفس فئة التقارير التاريخيّة المتروكة سابقاً في شريحة MCP للوكيل A). **البرهان بالتكذيب:** `tests_v9/test_api_versioning_policy_guard.py::test_edge_inference_and_knowledge_graph_routes_are_versioned` — كسر مقصود (إعادة `@app.post(\"/sync/trigger\")`) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً؛ استعادة الإصلاح أعادته أخضر. **التحقّق:** `verify_all_generated.py` ⇒ صفر انحراف (٥٦ خطوة + ٥٢٣٠ بصمة إصدار) · `pytest -m unit`: ٣٧٥٨ نجح (+١ عن الشريحة السابقة) بلا انحدار · `ruff check .`/`ruff format --check .` نظيفان. **شريحة الوكيل D الثالثة (2026-07-30، tts-service + local-ai-rag):** ٦ مواضع خام انتقلت (٤ tts-service + ٢ local-ai-rag)، لكن العقود الفريدة أُغلِقت أربعة فقط — ١٠٧ ⇒ **١٠٣**. **tts-service (`services/tts-service/routers/tts.py`، `router = APIRouter()` بلا `prefix=`):** `GET /tts/voices:20`، `GET /tts/status:30`، `POST /tts/synthesize:43`، `POST /tts/stream:124` ⇐ `/v1/tts/*` — الأربعة نصوص فريدة أصلاً (لا خدمة أخرى تشاركها)، فأُغلِقت أربعتها. **local-ai-rag (`services/local-ai-rag/main.py`):** `POST /query:419` ⇐ `/v1/query`، `POST /ingest:429` ⇐ `/v1/ingest` — **كلاهما نصّ مُشترَك سابقاً**: `POST /query` مع `ai_agronomist/main.py:359` (خارج نطاق هذه الشريحة)، و`POST /ingest` مع `rag-retrieval/main.py:89` (خارج النطاق أيضاً). موضعا local-ai-rag أُغلِقا من الجرد الخام (١١٠ ⇒ ١٠٤) لكن النصّين `POST /query`/`POST /ingest` بقيا في قائمة السماح لأنّ الخدمتين الأخريين ما زالتا تُعلِنانهما بلا إصدار — ستُغلَقان حين تُهاجَر ai_agronomist ورag-retrieval مستقبلاً (١٠٧ ⇒ ١٠٣: أربعة عقود فقط من ستّة مواضع خام). **مستدعيات حقيقيّة:** `POST /tts/synthesize` ⇐ `bots/telegram/main.py:197` و`agents/notification/agent.py:126` (نداءا خدمة-لخدمة مباشران، حُدِّثا)؛ `frontend/src/services/api.ts:1838` يستدعي عبر `kongApi.post('/tts/synthesize', ...)` بلا بادئة `/api/` — بوّابة nginx.v9.conf's `location /tts/` تمرّر شفّافاً (`proxy_pass http://tts_backend;` بلا مقطع URI)، فعُدِّل proxy_pass إلى `http://tts_backend/v1/tts/;` (نفس نمط `/api/agent/`) فبقي نداء الواجهة **بلا تغيير**. `POST /query` ⇐ `services/supervisor-agent/skills/advisory_skill.py:60` (نداء خدمة-لخدمة، حُدِّث). `nginx.unified.conf`'s `location /api/rag/` — `rag_backend` upstream في هذه الطبولوجيا تحديداً يشير إلى **local-ai-rag** (لا rag-retrieval كما في nginx.v9.conf — نفس اسم upstream، خدمتان مختلفتان تماماً حسب الطبولوجيا، مُوثَّق بتعليق جديد لمنع الخلط مستقبلاً) — عُدِّل `proxy_pass` إلى `http://rag_backend/v1/;`. `POST /tts/voices`·`/status`·`/stream` و`GET /edges`... إلخ بلا مستدعٍ خارجيّ متتبَّع غير اختباراتها. **فجوة حوكمة ثانويّة أُغلِقت ضمن نفس الشريحة:** `config/platform_catalog_overrides.yml` كان يحمل قرارين يدويّين لتصنيف `POST /query`/`POST /ingest` كـ`service_scoped_semantics` (تكرار نصّ عابر خدمتين) — بعد أن أصبح لكلّ نصّ عضو واحد فقط (لا تكرار)، فشل `build_platform_catalog.py` حوكميّاً (`stale decision: no measured duplicate group`)؛ حُذِف القراران (لا أُعيد ربطهما بمسار جديد كما حدث مع `/products` سابقاً، إذ العقد الباقي أحاديّ العضو لا يحتاج قرار تصنيف تكرار أصلاً). اختباران مثبَّتان (pinned) في `tests_v9/test_platform_catalog_gate.py` (عدد المسارات الفريدة، عدد مجموعات التكرار المصنَّفة) حُدِّثا بالقياس الجديد الصحيح (٩٩١⇒٩٩٣ فرادةً [انقسام نصّين مشترَكين إلى أربعة نصوص]، ١٥⇒١٣ مجموعة). **البرهان بالتكذيب:** `tests_v9/test_api_versioning_policy_guard.py::test_tts_service_and_local_ai_rag_routes_are_versioned` — كسر مقصود (إعادة `@app.post(\"/query\")`) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً؛ استعادة الإصلاح أعادته أخضر. **التحقّق:** `verify_all_generated.py` ⇒ صفر انحراف (٥٦ خطوة + ٥٢٣٠ بصمة إصدار) · `pytest -m unit`: ٣٧٥٩ نجح (+١ عن الشريحة السابقة)، صفر انحدار بعد تحديث الثابتين المُثبَّتين · `ruff check .`/`ruff format --check .` نظيفان. **شريحة الوكيل D الرابعة (2026-07-30، ai_agronomist):** ٩ مواضع خام (`@app.*` بلا prefix) ⇐ `/v1/*` — ٨ عقود فريدة أُغلِقت. ١٠٣ ⇒ **٩٥**. **ما انتقل (`services/ai_agronomist/main.py`):** `GET /approvals/pending:125`، `POST /approvals/approve:145`، `POST /approvals/deny:197`، `POST /approvals/resume:243`، `POST /prescription/export-preview:287`، `POST /query:359`، `POST /chat:370`، `POST /explain:381`، `POST /recommend:392`. **`POST /recommend` بقي في القائمة (عقد واحد لا ٩):** نصّه كان مُشترَكاً مع `agriai-engine/main.py:237` (خارج نطاق هذه الشريحة، شريحة لاحقة) — عضو واحد متبقٍّ. **تعارض جديد ظهر عرضاً:** `POST /v1/query` — بعد الهجرة صار ai_agronomist وlocal-ai-rag (شريحة سابقة مباشرة) يُعلِنان نفس النصّ **مصادفةً** (كانا نصّين مختلفين `POST /query` بلا علاقة قبل الهجرتين المستقلّتين، والتقيا على `/v1/query` بعد أن هاجرت كلّ خدمة بمعزل عن الأخرى إلى نفس بادئة الإصدار). أُضيف قرار حوكمة جديد في `config/platform_catalog_overrides.yml` (`service_scoped_semantics`) يوثِّق أنّهما عقدان مختلفان تماماً رغم تطابق النصّ الآن؛ وحُذِف قرار `POST /recommend` القديم (كان يفترض عضوين — أصبح عضواً واحداً بعد الهجرة، فلا حاجة لقرار تكرار). **مستدعيات حقيقيّة وُجِدت بالبحث الشامل:** `frontend/src/hooks/useApi.ts:1982,1994` (`kongApi.get('/api/ai-agronomist/approvals/pending')`، `kongApi.post(\\`/api/ai-agronomist/approvals/${decision}\\`, ...)`) و`frontend/src/sections/ChatbotPage.tsx:217,525` (نفس نمط approvals + `kongApi.post('/api/ai-agronomist/chat', ...)`) — جميعها عبر بوّابة `nginx.v9.conf`'s `location /api/ai-agronomist/` الشفّافة (`proxy_pass http://ai_agronomist_backend/;` بلا مقطع URI) — عُدِّل `proxy_pass` إلى `http://ai_agronomist_backend/v1/;` (نمط `/api/agent/`/`/api/knowledge-graph/`/`/api/rag/`) فبقيت **كلّ** نداءات الواجهة الأربعة بلا أيّ تغيير. اختبارا الواجهة الساكنَين (`ChatbotPage.endpoint.test.ts`، `ChatbotApprovalUi.v58.static.test.ts`) يتحقّقان من نصّ الواجهة نفسها لا من الخلفيّة، فبقيا صحيحَين بلا تعديل. **كلّ مرجع حرفيّ آخر حُدِّث:** `scripts/ci/ai_agronomist_main_decomposition_guard.py` (٤ سلاسل ديكوريتر)، `tests_v9/runtime_activation/test_phase2_ai_gateway_web_binding_static.py` (٤ سلاسل)، `tests_v9/test_gateway_trusted_identity_sec3.py` (قسم SEC-3/SEC-3.1 كاملاً، ١٠ مواضع)، `tests_v9/test_agent_stores_v58_2.py`، `tests_v9/test_ai_approval_endpoints_v58.py`، `tests_v9/test_runtime_evidence_wiring_v62_2.py`، `docs/security/SEC-3.1_approvals_user_role.md` (نصّ حيّ يصف عقد أمنيّ مُطبَّق حاليّاً، لا تقرير تاريخيّ — حُدِّثت المسارات الحرفيّة فقط، أُبقيت أرقام الأسطر البائتة أصلاً L135/L182/L223 ونطاق nginx:308-314 كما كانت، انحراف سابق الوجود غير مرتبط بهذه الشريحة)، `docs/capability-registry/domains/decision.yaml`، `docs/capability-registry/domains/precision.yaml`. `docs/api/BACKEND_FRONTEND_COVERAGE.md` أُعيد توليده عبر `endpoint_ui_coverage_gate.py --report`. `docs/openapi/API_MAP.md`/`ROUTE_INVENTORY.json` لا يذكران ai_agronomist أصلاً (نقص سابق الوجود، خارج النطاق). **البرهان بالتكذيب:** `tests_v9/test_api_versioning_policy_guard.py::test_ai_agronomist_routes_are_versioned` — كسر مقصود (إعادة `@app.post(\"/chat\")`) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً؛ استعادة الإصلاح أعادته أخضر. **التحقّق:** `verify_all_generated.py` ⇒ صفر انحراف (٥٦ خطوة + ٥٢٣٠ بصمة إصدار) · `pytest -m unit`: ٣٧٦٠ نجح (+١ عن الشريحة السابقة)، صفر انحدار · `ruff check .`/`ruff format --check .` نظيفان. **شريحة الوكيل D الخامسة (2026-07-30، rag-retrieval + agriai-engine) — إغلاق كامل نطاق الوكيل D الأصليّ:** ٦ مواضع خام (`@app.*` بلا prefix) ⇐ `/v1/*` — ٦ عقود فريدة أُغلِقت (لا تكرار داخليّ أو خارجيّ عند لحظة الهجرة نفسها). ٩٥ ⇒ **٨٩**. **rag-retrieval (`services/rag-retrieval/main.py`):** `POST /ingest:89` ⇐ `/v1/ingest`، `POST /search:114` ⇐ `/v1/search`. **agriai-engine (`services/agriai-engine/main.py`):** `POST /recommend:237` ⇐ `/v1/recommend`، `POST /simulate:297` ⇐ `/v1/simulate`، `POST /plan:310` ⇐ `/v1/plan`، `POST /replay/verify:326` ⇐ `/v1/replay/verify`. **تعارضان عرضيّان جديدان اكتُشفا فوراً بعد الهجرة** (كلاهما نتيجة تقارب نصّيّ بين شرائح مستقلّة، لا علاقة دلاليّة سابقة): `POST /v1/ingest` (rag-retrieval × local-ai-rag، الأخيرة من شريحة الوكيل D الثالثة) و`POST /v1/recommend` (agriai-engine × ai_agronomist، الأخيرة من شريحة الوكيل D الرابعة) — أُضيف قراران جديدان `service_scoped_semantics` في `config/platform_catalog_overrides.yml` يوثِّقان أنّهما عقود مختلفة تماماً رغم تطابق النصّ عرضاً. **فجوة تصنيف مكتشَفة حديثاً — `POST /plan` ونصّ خام يتّضح أنّه شبح:** القرار القديم لـ`POST /plan` (legacy_bff_facade، canonical_owner: agriai-engine، facade: sahool-platform) افترض عضوين حقيقيّين مُشترَكين — لكن تبيَّن أنّ عضو sahool-platform لم يكن حقيقيّاً قطّ: `phase9_autonomous_farm_os.py`'s راوتر بادئته `/v1/phase9/autonomy` (مُصدَّر فعلاً منذ شريحة الوكيل D الرابعة توضيحاً، لكن الحقيقة أقدم — راجع تصحيح تصنيف ثالث أعلاه)، فمساره الحقيقيّ `/v1/phase9/autonomy/plan` لا `/plan` الخام. **السبب:** `scripts/ci/generate_service_inventory.py` — المولِّد المسؤول عن `route_inventory.generated.json` الذي يقرأه `build_platform_catalog.py` لكشف التكرار — يحمل **نفس** عمى `APIRouter(prefix=...)` المُصلَح في `api_versioning_policy_guard.py` (PR #717، 2026-07-30) لكن **لم يُصلَح هنا قطّ**؛ فسجَّل sahool-platform's phase9 كمالك مسار `/plan` خام زوراً. حُذِف قرار `/plan` (بات `stale decision` بعد هجرة agriai-engine — عضو agriai-engine الحقيقيّ الوحيد اختفى، والعضو الوهميّ المتبقّي لا يُشكِّل تكراراً حقيقيّاً يحتاج قراراً). **هذه فجوة كشف رابعة من نفس عائلة العمى التصنيفيّ في هذه الجلسة** (بعد `/v1/` مقابل `/api/v1/`، استبعاد ملفّات الاختبار، `APIRouter(prefix=...)` — لكن في مولِّد شقيق مختلف تماماً هذه المرّة، لا `api_versioning_policy_guard.py` نفسه) — **لم تُصلَح في هذه الشريحة**، خارج نطاقها المباشر، مُسجَّلة كفجوة كشف مستقلّة تحتاج شريحة تصحيح تصنيف مخصّصة في `generate_service_inventory.py` قبل قياس التكرارات عبره بثقة كاملة. **مستدعيات حقيقيّة وُجِدت بالبحث الشامل:** `GET /v1/search` عبر `services/ai_agronomist/ai_evidence_runtime.py:605` (نداء خدمة-لخدمة عبر `RAG_BASE_URL`، حُدِّث). بقيّة المسارات الخمسة بلا مستدعٍ خارجيّ متتبَّع — agriai-engine بالذات **غير مُستهلَكة عمداً بالكامل** (`wired=False`، قرار معماريّ مسجَّل مسبقاً في هذا المستودع، لا فجوة). **بوّابتا nginx (كلاهما داخليّتان، بلا وصول متصفّح):** `nginx.v9.conf`'s `location /api/rag/` (تشير إلى rag-retrieval هنا تحديداً، لا local-ai-rag كما في nginx.unified.conf — نفس تنبيه التسمية المزدوجة من شريحة الوكيل D الثالثة) عُدِّلت. `nginx.v9.conf`'s و`nginx.unified.conf`'s `location /api/agriai/` عُدِّلتا كلتاهما رغم غياب مستهلك متتبَّع — العقد يبقى متّسقاً احتياطاً. `nginx.light.conf` بلا بوّابتَي `/api/rag/`/`/api/agriai/` أصلاً. **كلّ مرجع حرفيّ آخر حُدِّث:** `tests_v9/test_gateway_trusted_identity_sec3.py` (قسم rag-retrieval كاملاً — ٦ مواضع فاتت الفحص الأوّليّ لهذه الشريحة واكتُشفت عبر `pytest -m unit` الكامل، درسٌ عمليّاتيّ: البحث النصّيّ المُسبَق لا يُغني عن تشغيل السويت الكامل قبل الالتزام)، `tests_v9/test_agriai_engine_features_20260702.py` (٧ مواضع)، `docs/openapi/API_MAP.md`+`ROUTE_INVENTORY.json` (إدخال agriai-engine الوحيد الموجود أصلاً — الوثيقتان لا تذكران rag-retrieval إطلاقاً، نقص سابق الوجود خارج النطاق). `docs/api/BACKEND_FRONTEND_COVERAGE.md` أُعيد توليده فعليّاً. **ثابتان مثبَّتان (pinned) تأثّرا في `tests_v9/test_platform_catalog_gate.py`:** `PINNED_UNIQUE_METHOD_PATH` ٩٩٣ ⇒ **٩٩٢** (صافي: -١ من إغلاقَي `/ingest`/`/recommend` الخامَين الحقيقيَّين، +١ محايد من `/search`/`/simulate`/`/replay-verify`، +١ ثابت من شبح `/plan` — لا يختفي رغم الهجرة)، و`duplicate_groups_classified` ١٣ ⇒ **١٤** (-١ إزالة `/plan` + ٢ إضافة `/v1/ingest`+`/v1/recommend`)؛ اختبار `test_u4` الإضافيّ الذي كان يتحقّق من `facades[('POST','/plan')] == 'agriai-engine'` عُدِّل (لم يعد `/plan` واجهة-حقيقة مُصنَّفة). **البرهان بالتكذيب:** `tests_v9/test_api_versioning_policy_guard.py::test_rag_retrieval_and_agriai_engine_routes_are_versioned` — كسر مقصود (إعادة `@app.post(\"/plan\")`) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً؛ استعادة الإصلاح أعادته أخضر. **التحقّق:** `verify_all_generated.py` ⇒ صفر انحراف (٥٦ خطوة + ٥٢٣٠ بصمة إصدار) · `pytest -m unit`: ٣٧٦١ نجح (+١ عن الشريحة السابقة)، صفر انحدار بعد تحديث الثابتين المُثبَّتين واختبار الواجهة الإضافيّ · `ruff check .`/`ruff format --check .` نظيفان. **بهذه الشريحة يُغلَق نطاق الوكيل D الأصليّ بالكامل** (edge-inference، knowledge-graph، tts-service، local-ai-rag، ai_agronomist، rag-retrieval، agriai-engine — سبع خدمات، خمس شرائح، ٥ PR). **raster-service PR-R2 (2026-07-31، ثاني شرائح خدمة raster-service بعد PR-R1 للتصنيف):** ٨٩ ⇒ **٨١** (٨ عقود فريدة، بلا تكرار). الثمانية: `GET /jobs/{job_id}`، `GET /jobs/{job_id}/result`، `POST /upload/raster`، `POST /upload/drone`، `GET /storage/stats`، `POST /storage/cleanup`، `GET /offline/packs`، `GET /offline/packs/{pack_name}` (services/raster-service/routers/jobs.py+storage.py) ⇐ `/v1/*`. **مستهلك حقيقيّ واحد وُجِد بالبحث الشامل (لا افتراض):** `services/sahool-platform/api/raster_service_client.py:466` (`get_job_result`) يستدعي `/jobs/{job_id}/result` خدمة-لخدمة — حُدِّث في نفس الالتزام إلى `/v1/jobs/{job_id}/result`، ومعه اختبار الواجهة `test_p2_1_imagery_automation_raster_facade_guard.py`. **كلّ مرجع حرفيّ آخر حُدِّث:** `tests_v9/test_raster_endpoint_auth_coverage.py`'s SERVICE_ONLY، `tests_v9/test_fields_put_and_mfa_api_contract_20260626.py`، النداء الوظيفيّ الحقيقيّ الوحيد داخل الخدمة نفسها (`services/raster-service/test_stac_vrt.py`)، حقول `download_url`/`note` الديناميكيّة المُضمَّنة في استجابتَي `/offline/packs`/`/process/batch` (نفس درس `tile_url_template` من بحث PR-R1)، و`docs/openapi/API_MAP.md`. تُركت عمداً: `tests_v9/test_mobile_backend_contract.py` (اختبار غير مُؤكِّد أصلاً — `return` قائمة نتائج لا `assert`، ويمسح مجلّداً شقيقاً غير موجود في هذا المستودع، بائت سابق الوجود) و`tests_v9/test_roadmap_phase23.py`'s فحصان ناعمان (يمسحان `main.py` الذي لم يحمل هذه المسارات قطّ بعد تفكيك الراوترات — بائتان سابقا الوجود، تحقّق مباشر أكّد ذلك قبل الالتزام). تقريران تاريخيّان مؤرَّخان (`FIX_ALL_REPORTED_ISSUES_20260626.md`، `docs/history/PROVIDERS_GAPS_IMPLEMENTATION.md`) لم يُمسّا، نفس سابقة الشرائح الأخرى. **الأثر:** legacy_unversioned_business المواضع والعقود الفريدة كلاهما ٨٩ ⇒ **٨١**. التالي: raster-service PR-R3 (٢٠ مساراً — صور/كتالوج/معالجة، مستهلكان حقيقيّان: `/indices`، `/process/batch`). **raster-service PR-R3 (2026-07-31، ثالث شرائح خدمة raster-service بعد PR-R1 للتصنيف وPR-R2 للمسارات الداخليّة):** ٨١ ⇒ **٦١** (٢٠ عقداً فريداً، بلا تكرار). العشرون: تحاليل (`analysis.py`: `/zones/classify`، `/change/detect`، `/fvc/compute`، `/sar/rvi`، `/terrain/slope`، `/cog/validate`، `/salinity/classify`، `/salinity/calibrate`) + معالجة غير متزامنة (`processing.py`: `/process`، `/raw/process`، `/process/batch`) + واجهة STAC داخليّة (`stac.py`: `/stac`، `/stac/collections`، `/stac/mosaicjson`) + مراقبة (`observability.py`: `/info/{layer_id}`، `/indices`) + سلسلة زمنيّة (`timeseries_routes.py`: `/imagery/timeseries`، `/imagery/timeseries/analyze`، `/imagery/timeseries/parallel`) + حدود GIS (`fields.py`: `/gis/admin-boundaries`) ⇐ `/v1/*`. **مستهلكان حقيقيّان وُجِدا بالبحث الشامل (لا افتراض) — يختلفان عن حالة PR-R2 (صفر مستهلك خارجيّ حقيقيّ):** `services/sahool-platform/api/raster_service_client.py:191` (`get_indices_sync`) و`:448` (`process_indicator_batch`، يقود تجميع مؤشّرات `imagery_automation.py`) — حُدِّثا في نفس الالتزام إلى `/v1/indices` و`/v1/process/batch`، ومعهما اختبار الواجهة `test_p2_1_imagery_automation_raster_facade_guard.py` وdocstrings `imagery_automation.py`. **كلّ مرجع حرفيّ آخر حُدِّث:** `tests_v9/test_raster_endpoint_auth_coverage.py`'s SERVICE_ONLY/PUBLIC_CATALOG، ثلاثة ملفّات اختبار وظيفيّة حقيقيّة تؤكّد وجود الديكوريتر (`test_change_detection.py`، `test_fvc.py`، `test_sar_rvi.py`، `test_field_intelligence_cloud.py`)، حارس CI حقيقيّ (`scripts/ci/raw_data_processing_contract_guard.py`، ليس اختباراً — يفحص وجود `/v1/raw/process` حرفيّاً)، وصف نصّيّ في مهارة LLM غير مُتّصلة فعليّاً (`services/supervisor-agent/skills/remote_sensing_skill.py` ونسختها المكرّرة)، روابط STAC ذاتيّة الإشارة في `cloud_native_catalog.py` (نفس درس `tile_url_template` من بحث PR-R1)، وحقول `note` استشاريّة في `timeseries_routes.py`. `docs/openapi/API_MAP.md` حُدِّث للإدخالات الموجودة فعلاً فيه (تسعة مسارات كانت غائبة أصلاً من الوثيقة قبل هذه الشريحة — نقص سابق الوجود، خارج النطاق، لم يُملأ). تُركت عمداً: فحوص `tests_v9/test_roadmap_phase23.py`'s الناعمة (تمسح `main.py` الذي لم يحمل هذه المسارات قطّ بعد تفكيك الراوترات — بائتة سابقة الوجود، تحقّق مباشر أكّد ذلك) وفحصا مطابقة عنوان-فرعي (`\"/process\" in url`) يبقيان صحيحَين حتماً بغضّ النظر عن بادئة `/v1/`. **الأثر:** legacy_unversioned_business المواضع والعقود الفريدة كلاهما ٨١ ⇒ **٦١**. التالي: raster-service PR-R4 (٢ مساراً فقط — بلاط/عرض `tiles.py`، صفر مستهلك خارجيّ حيّ مؤكَّد بالبحث الشامل). **raster-service PR-R4 (2026-07-31، رابع ورابعة شرائح خدمة raster-service — إغلاق كامل نطاق raster-service الأصليّ بعد PR-R1 للتصنيف وPR-R2/PR-R3 للمسارات):** ٦١ ⇒ **٥٩** (٢ عقد فريد، بلا تكرار). المساران: `GET /tiles/{layer_id}/{z}/{x}/{y}.png` و`GET /layers/{layer_id}/tilejson` (`services/raster-service/routers/tiles.py:22,36`) ⇐ `/v1/tiles/{layer_id}/{z}/{x}/{y}.png`·`/v1/layers/{layer_id}/tilejson`. **صفر مستهلك خارجيّ حيّ مؤكَّد بالبحث الشامل** — تكرار نتيجة بحث PR-R1 نفسها (لا caller ثابت في frontend/mobile/nginx/docker-compose ولا نداء خدمة-لخدمة عبر `raster_service_client.py`)، فالهجرة مقصورة على تحديث المراجع الحرفيّة الداخليّة والوثائقيّة. **رابط ذاتيّ-الإشارة مُحدَّث رغم غياب المستهلك (نفس درس `tile_url_template`/روابط STAC من PR-R1/PR-R3):** `tiles.py:63`'s حقل `tiles` في استجابة `\"static-pregenerated\"` (fallback عند عدم ضبط TiTiler) و`raster_job_orchestration.py:230`'s حقل `tile_url_template` (ميداني ميّت وفق بحث PR-R1، حُدِّث للاتّساق الداخليّ فقط) — كلاهما `f\"/tiles/{layer_id}/...\"` ⇐ `f\"/v1/tiles/{layer_id}/...\"`. **فجوة تصنيف مصاحبة أُصلِحت استباقيّاً (لا بعد فشل CI):** `tests_v9/test_raster_endpoint_auth_coverage.py`'s `_is_layer_scoped(path)` كانت تطابق البادئة الخام `/tiles/{layer_id}`/`/layers/{layer_id}/` — لو تُركت كما هي، كان المساران سيصيران «غير مُصنَّفين» (لا حارس ملكيّة/خدمة، وليسا في PUBLIC_CATALOG/SERVICE_ONLY) فور الهجرة. حُدِّثت لتطابق `/v1/tiles/{layer_id}`/`/v1/layers/{layer_id}/` **قبل** تشغيل السويت، ثمّ بُرهِنت بالتكذيب: عكس ديكوريتر واحد مؤقّتاً عبر `sed` أنتج رسالة الفشل المتوقَّعة حرفيّاً («تصنيف غير معروف»)، ثمّ استُعيد الإصلاح وتأكّد الأخضر. **تنظيف تراكميّ اكتُشف بالمسح الشامل لهذه الشريحة — بقايا من PR-R2/PR-R3 لم تُصلَح وقتها:** `docs/openapi/API_MAP.md`/`ROUTE_INVENTORY.json` (إدخالا tiles.py الأخيران)، `skills/sahool-gis/RASTER_LAYER.md` (`/info/{layer_id}` أيضاً، فاتت PR-R3)، `skills/sahool-gis/TERRAIN_DEM.md` (`/terrain/slope`، فاتت PR-R3)، `docs/architecture/GIS_CLOUD_NATIVE_BEST_PRACTICES_PHASE4.md` (ثلاثة مسارات STAC، فاتت PR-R3)، `docs/architecture/db_ownership.yml` (تعليق `/gis/admin-boundaries`، فاتت PR-R3)، وأربعة ملفّات `docs/capability-registry/domains/*.yaml` (gis.yaml ٤ أسطر، satellite.yaml ١٨ سطراً، irrigation.yaml ٣ أسطر، operations.yaml سطران [`/offline/packs`+`/offline/packs/{pack_name}`، فاتا PR-R2]) — تحقّق نهائيّ بسكربت مسح شامل أكّد صفر مرجع raster-service خام متبقٍّ في أيّ ملفّ `capability-registry/domains/*.yaml`. **تُركت عمداً (فحص مباشر لا افتراض):** `docs/specs/A7_admin_boundaries_spec.md` (رأسه يُعلِن صراحةً «⏳ مسودّة للمراجعة» وملكيّة TBD — مسودّة منفصلة عن التنفيذ الحقيقيّ)، `docs/history/RASTER_SERVICE_REVIEW.md` و`docs/audits/RIV_DURABLE_PRODUCT_IDENTITY_AND_LEASES_REPORT_20260712.md` (سجلّ تاريخيّ مؤرَّخ)، `shared/enterprise_gis/phase8_global_scale.py:124` (قالب اختبار حمل عامّ يذكر `/stac` بلا صلة بـraster-service). **البرهان بالتكذيب:** `tests_v9/test_api_versioning_policy_guard.py::test_raster_service_pr_r4_routes_are_versioned` — كسر مقصود (إعادة الديكوريتر الأوّل إلى مساره الخام) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً؛ استعادة الإصلاح أعادته أخضر. **التحقّق النهائيّ:** `guard.collect()` الحيّ يُظهِر raster-service الآن `Counter({'versioned': 78, 'infra': 3})` — صفر `legacy_unversioned_business` متبقٍّ لهذه الخدمة. **بهذه الشريحة يُغلَق نطاق raster-service الأصليّ بالكامل** (٣٠ مساراً عبر أربع شرائح: PR-R1 تصنيف بلا هجرة، PR-R2 ٨ مسارات داخليّة/تشغيليّة، PR-R3 ٢٠ مساراً صور/كتالوج/معالجة، PR-R4 ٢ مساراً بلاط/عرض). التالي وفق الترتيب المُعلَن سابقاً: soil-service، ثمّ mcp_servers، ثمّ erp-bridge (وodoo-bridge اسمه التاريخيّ لا خدمة مستقلّة) — auth (٢٤ مساراً) يبقى مؤجَّلاً حتى يثبت مستهلك حيّ. **soil-service (2026-07-31، خامس خدمة بعد اكتمال raster-service) — ٥ مسارات غير مُصدَّرة، كلّها service_only:** `POST /soil/decode/modbus` (`services/soil-service/routers/modbus.py:35`)، `GET /soil/readings/{field_id}` و`POST /soil/ingest` (`routers/readings.py:22,37`)، و`POST /soil/suitability` و`GET /soil/soilgrids` (`routers/soil_profile.py:46,57`) ⇐ `/v1/soil/*`. بخلاف raster-service (٣٠ مساراً، أربع شرائح بالملكيّة) لم تُقسَّم هذه الخدمة إذ الخمسة كلّها service_only بلا اختلاف ملكيّة يستحقّ الفصل — هجرة واحدة، PR واحد، مطابقة لنمط الخدمات الصغيرة السابقة (tts-service، edge-inference، knowledge-graph). **مستهلك حقيقيّ واحد وُجِد بالبحث الشامل عبر المستودع كلّه (لا افتراض)، حُدِّث في نفس الالتزام:** `services/sahool-platform/core/field_intelligence_adapters.py:105`'s `fetch_soil_baseline()` يستدعي `/soil/soilgrids` مباشرةً ⇐ `/v1/soil/soilgrids`؛ docstrings الدالّة نفسها وثلاثة تعليقات وصفيّة أخرى في `field_intelligence_card.py`، `soil_climate_sources.py`، و`api/routers/field_intelligence.py` حُدِّثت للدقّة رغم كونها تعليقات لا كوداً منفَّذاً. **الأربعة مسارات الباقية (modbus/readings/ingest/suitability) بلا مستهلك خارجيّ حقيقيّ** — بحث شامل عبر frontend/mobile/nginx/docker-compose لم يجد استدعاءً حرفيّاً؛ frontend's `soilApi` يستدعي مسارات مختلفة تماماً (`/soil/wofost_params/{id}`، `/soil/nitrogen/recommendation`، إلخ — راوترات أخرى في نفس الخدمة، غير هذه الخمسة). بوّابة nginx's `/api/soil/` تمرّ عبر `service_proxy.py`'s `proxy_soil` (نمط field-segmentation/edge-inference — بروكسي شفّاف بلا rewrite، العميل يحدّد المسار النهائيّ لا الوسيط). **مرجع حيّ غير-اختباريّ مُهِمّ اكتُشف ولم يكن ليظهر ببحث نصّيّ سطحيّ:** `runtime-verification/functional_probes/soil-service.json` — خطّة probe فعليّة تُشغَّل حيّاً عبر `functional_probe_runner.py` (لا اختبار pytest) تحمل حقلَي `path: \\\"/soil/suitability\\\"` مرّتين + جملة توضيحيّة في `note` — حُدِّثا كلاهما؛ `functional_probe_runner.py --check` أُعيد تشغيله للتأكّد (`functional_probe_runner_ok plans=3 probes=6`). **كلّ مرجع حرفيّ آخر عبر المستودع حُدِّث:** أربعة ملفّات اختبار وظيفيّة حقيقيّة (`test_modbus_decoder_20260703.py`، `test_soil_science_20260703.py`، `test_services_functional.py` — مُعلَّم `integration` لا يعمل في `-m unit` لكن حُدِّث لنفس انضباط عدم ترك مرجع بائت)، `_CRITICAL_ROUTES` في `services/soil-service/test_soil_router_decomposition_guard.py`، docstring افتتاحيّ في `tests_v9/test_soil_field_tenant_authz.py` (سرد تاريخيّ لثغرة IDOR سابقة، لكن يسمّي المسار الحيّ فحُدِّث للدقّة لا لتزييف السرد)، و`docs/openapi/API_MAP.md`+`ROUTE_INVENTORY.json` (سطرا readings/ingest فقط — الثلاثة الأخرى كانت غائبة أصلاً عن هاتين الوثيقتين قبل هذه الشريحة، نقص سابق الوجود لم يُملأ)، `docs/capability-registry/domains/soil.yaml` (SOIL-001's apis: سطرا suitability/soilgrids)، و`docs/NGINX_ROUTING.md` (صفّ `/api/soil/*` اليدويّ — وصف upstream فيه بائت أصلاً قبل هذه الشريحة إذ الطبولوجيا الفعليّة تمرّ عبر service_proxy لا upstream مباشر، لكن نصّ المسار نفسه حُدِّث). **تُركت عمداً:** `sahool-brain/hot.md`/`log.md`'s سرد تاريخيّ لبناء هذه المسارات أصلاً (وصف جلسة ماضية، تعديله يُزيّف ما وُصِف وقتها)، و`docs/api/BACKEND_FRONTEND_COVERAGE.md` (مُولَّد فعليّاً، أُعيد توليده لا حُرِّر يدويّاً). **عثرة اختبار غير مرتبطة اكتُشفت أثناء البحث، مُوثَّقة لا مُصلَحة:** `test_soil_router_decomposition_guard.py::test_no_app_route_decorators_in_main` يفشل سابقاً على `main` نفسه (قبل أيّ لمس لهذه الشريحة) — `main.py:197`'s `@app.get(\\\"/runtime-identity\\\")` مسار بنية تحتيّة مُعلَن مباشرةً بلا راوتر، بخلاف افتراض الحارس أنّ التفكيك مكتمل بالكامل؛ مُتحقَّق بـ`git stash` على الشجرة النظيفة فأكّد أنّه فشل قائم سلفاً غير ناتج عن هذه الهجرة، خارج نطاقها. **البرهان بالتكذيب:** `tests_v9/test_api_versioning_policy_guard.py::test_soil_service_routes_are_versioned` — كسر مقصود (إعادة `@router.get(\\\"/soil/soilgrids\\\")`) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً، وكسر ثانٍ (إعادة رابط adapters.py الخام) أسقطه أيضاً باسم السطر المتسرِّب؛ استعادة كليهما أعادته أخضر. **التحقّق:** `guard.collect()` الحيّ يُظهِر صفر `legacy_unversioned_business` لـsoil-service (٥٠ `versioned`، ٥ `infra`) · الجرد الخام: `legacy_unversioned_business` ٥٩ ⇒ **٥٤**. **التالي وفق الترتيب المُعلَن مسبقاً:** mcp_servers، ثمّ erp-bridge (وodoo-bridge اسمه التاريخيّ لا خدمة مستقلّة) — auth (٢٤ مساراً) يبقى مؤجَّلاً حتى يثبت مستهلك حيّ. **mcp_servers (2026-07-31، سادس خدمة بعد soil-service) — ٧ مسارات REST سوق غير مُصدَّرة، كلّها Bearer/JWT عبر `Depends(_get_current_user)` (مُصادَقة مستخدم متصفّح لا توكن خدمة، بخلاف soil-service/raster-service):** `GET /suppliers/{supplier_id}` و`POST /procurement` و`GET /procurement/{order_id}` و`POST /sales` و`GET /sales` و`GET /price-history/{category}` و`GET /analytics/{tenant_id}` (كلّها `services/mcp_servers/market_server.py`، أسطر ٦٤٣-٦٩١) ⇐ `/v1/*`. الملفّ يستعمل `@app.<method>` مباشرةً بلا `APIRouter`/`routers/` (بخلاف soil-service/raster-service المُفكَّكين) — نفس نمط تسجيل `GET /v1/products` المُصدَّر مسبقاً في الملفّ نفسه (شريحة الوكيل A الأولى، عقد MCP الذرّي)، فالهجرة تعديل حرفيّ مباشر للديكوريتر بلا اعتبارات تركيب راوتر. **صفر مستهلك خارجيّ حيّ عبر كامل المستودع (بحث شامل، لا افتراض):** frontend/mobile/nginx.v9.conf (لا بوّابة market فيه إطلاقاً)/supervisor-agent (يستدعي فقط عقد بروتوكول MCP `/v1/mcp/tools*` المُهاجَر سابقاً، لا نقاط REST هذه)/odoo-bridge (صفر نداء HTTP فعليّ لهذه المسارات، مجرّد ذكر عابر في تعليق) — صفر نتائج للسبعة كلّها. `nginx.fixed.conf`/`nginx.unified.conf`'s `/api/market/` (غائبة كليّاً من `nginx.v9.conf` الأساسيّ) بروكسي شفّاف بلا rewrite (`proxy_pass http://market_backend/;`) — العميل يحدّد المسار النهائيّ، فلا حاجة لتعديل نغينكس نفسه. **مرجع توضيحيّ (لا حيّ) اكتُشف ولم يُخلَط بمستهلك حقيقيّ:** `docs/MARKET_SYSTEM.md`'s مثال Dart (`lib/services/market_service.dart`) — تحقّق مباشر أكّد أنّ هذا الملفّ **غير موجود** في `mobile/sahool_app/lib/services/` فعليّاً (مثال توضيحيّ في الوثيقة فقط، لا كود حقيقيّ) — حُدِّث للدقّة رغم كونه توضيحيّاً لا وظيفيّاً. **مرجع اكتشاف جانبيّ في `capability-registry`:** `docs/capability-registry/domains/farm_management.yaml`'s قدرتان تحملان أدلّة conservative-linked لـ`GET /analytics/{tenant_id}` و`POST`/`GET /procurement*` — تحقّق مباشر أنّ `mobile_consumers: inventory_screen.dart` المُدرَج في نفس القدرة **لا يستدعي** هذين المسارين فعليّاً (ربط `capability_linker.py` تحفّظيّ heuristic لا دليل نداء حقيقيّ) — حُدِّثت الأسطر الثلاثة للدقّة النصّيّة فقط، بلا تغيير في `mobile_consumers`/`ui_consumers` (المتروكة فارغة/غير مرتبطة كما كانت، إذ لا دليل نداء حقيقيّ يستوجب تعديلها). **مرجعان حيّان غير-كوديَّين حُدِّثا:** `docs/MARKET_SYSTEM.md` (رسم ASCII للبنية + خمسة أمثلة `curl` + مثال Dart) و`docs/UNIFIED_SETUP.md`'s مثال `curl` حيّ لسير عمل التشغيل السريع (`/api/market/procurement` ⇐ `/api/market/v1/procurement`، عبر بروكسي شفّاف). **اختبار تكذيب جديد، مُثبَت بكسره فعليّاً ثمّ استعادته:** `tests_v9/test_api_versioning_policy_guard.py::test_mcp_servers_market_rest_routes_are_versioned` — كسر مقصود (إعادة `@app.post(\\\"/sales\\\")`) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً؛ استعادة الإصلاح أعادته أخضر. **التحقّق:** `guard.collect()` الحيّ يُظهِر صفر `legacy_unversioned_business` لـmcp_servers (١١ `versioned`، ١٤ `infra`) · `verify_all_generated.py` ⇒ صفر انحراف · `pytest -m unit` بلا انحدار. الجرد الخام: `legacy_unversioned_business` ٥٤ ⇒ **٤٧**. **التالي وفق الترتيب المُعلَن مسبقاً:** erp-bridge (وodoo-bridge اسمه التاريخيّ لا خدمة مستقلّة) — auth (٢٤ مساراً) يبقى مؤجَّلاً حتى يثبت مستهلك حيّ. **odoo-bridge/erp-bridge (2026-07-31، سابعة وآخر شريحة هجرة نشطة قبل Auth المؤجَّل) — ٧ مسارات، معظمها Bearer/JWT عبر `main.require_auth` باستثناء الويبهوك:** `GET /erp/provider` و`GET /config` و`GET /logs` و`GET /suppliers` (`routers/catalog.py`، أربعتها `require_auth`) و`GET /readyz/capabilities` (`routers/health.py`، بلا مصادقة — بيانات تشغيلية لا حكم صحة حاوية) و`POST /sync` (`routers/sync.py`، `require_auth`) و`POST /webhook/odoo` (`routers/webhooks.py`، سرّ `X-Webhook-Secret` مشترك لا JWT، يُستدعى من Odoo خارجيّاً لا من كود داخل هذا المستودع) ⇐ `/v1/*`. **قرار تصنيف `/readyz/capabilities`:** رغم موقعه بجوار `/healthz`/`/readyz` (المُصنَّفان `infra`) وتصميمه المُوثَّق صراحةً كمستوى ثالث مستقلّ (docstring health.py)، الحارس صنّفه `legacy_unversioned_business` لأنّه ليس في allowlist البنية الصريح — وبفحص محتواه فعليّاً يُعيد بيانات قدرة ERP التشغيليّة (اسم المزوّد/حالة التهيئة) لا حكماً على صحة الحاوية، فالتصنيف التجاريّ محقّ دلاليّاً أيضاً لا تقنيّاً فقط — لم يُطلَب استثناء تصنيفيّ جديد على غرار `/runtime-identity` (ذاك مدعوم بعقد مُجمَّد صريح في CLAUDE.md، هذا بلا عقد مماثل). **صفر مستهلك خارجيّ حيّ عبر كامل المستودع للستّة الأولى** — frontend/mobile/nginx.v9.conf (لا بوّابة odoo/erp فيه)؛ `nginx.unified.conf`'s `/api/odoo/`+`/api/erp/` بروكسيان شفّافان بلا rewrite؛ `market_server.py`'s `ERP_BRIDGE_URL` مُعرَّف كثابت لكن غير مُستهلَك فعليّاً (لا نداء HTTP)، بيانات ميّتة لا مسار متأثِّر (بلا مقطع مسار مُلحَق أصلاً). **`POST /webhook/odoo` خاصّ:** مُستدعًى من نظام Odoo خارجيّ عبر Automation Rule يُهيَّئه مسؤول بشريّ يدويّاً في واجهة Odoo — لا كود داخل هذا المستودع يستدعيه، فتحديثه اقتصر على مثال التوثيق الإرشاديّ (`docs/ODOO_INTEGRATION.md`) لضمان صحّة الإعداد المستقبليّ لا كسر نداء حيّ قائم فعلاً (لا سبيل للتحقّق من ذلك من داخل المستودع، وهذا حدّ صدق صريح). **مرجع مُضمَّن حيّ في استجابات API — نفس درس `tile_url_template` من raster-service:** `routers/sync.py`'s ثلاث رسائل خطأ استشاريّة (424/503) تُضمِّن نصّاً حرفيّاً `\\\"Check /readyz/capabilities for ...\\\"` يصل للعميل مباشرةً في جسم الاستجابة — حُدِّثت الثلاثة إلى `/v1/readyz/capabilities`. **مستندان حيّان غير مؤرَّخين بحالة تدقيق (فُحِصا وليسا تقارير تاريخيّة مثل نظرائهما في `docs/audits/` الأخرى) حُدِّثا:** `docs/audits/ERPNEXT_SETUP_GUIDE.md` (دليل إعداد بصيغة أمر حاليّ، لا تاريخ في الترويسة) و`docs/audits/ERP_PROVIDER_SWITCH.md` (وصف بناء ميزة، لا طابع تاريخيّ) — كلاهما مثالا `curl`. **`docs/ODOO_INTEGRATION.md` (مؤرَّخ 2026-05-19 لكن بصيغة دليل حيّ لا تقرير — نفس معاملة `MARKET_SYSTEM.md`):** ثمانية أمثلة `curl` + مثال Python لإعداد Odoo Automation Rule. **`docs/UNIFIED_SETUP.md`** مثال `curl` حيّ ضمن سير عمل تشغيل سريع. **تصحيح ترافقيّ اكتُشف في `docs/openapi/API_MAP.md` — `GET /products` بائت سابق الوجود (المسار الحقيقيّ `/v1/products` مُصدَّر منذ شريحة الوكيل A الأولى، لم تُصحَّح الوثيقة يومها):** صُحِّح ضمن نفس تعديل قسم odoo-bridge لتفادي ترك سطر متناقض بجوار الأسطر المُهاجَرة حديثاً. **اختبار تكذيب جديد، مُثبَت بكسره فعليّاً ثمّ استعادته:** `tests_v9/test_api_versioning_policy_guard.py::test_odoo_bridge_erp_routes_are_versioned` — كسر مقصود (إعادة `@router.get(\\\"/erp/provider\\\")`) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً؛ استعادة الإصلاح أعادته أخضر. **التحقّق:** `guard.collect()` الحيّ يُظهِر صفر `legacy_unversioned_business` لـodoo-bridge · `verify_all_generated.py` ⇒ صفر انحراف · `pytest -m unit` بلا انحدار. الجرد الخام: `legacy_unversioned_business` ٤٧ ⇒ **٤٠**، مُوزَّعة بالكامل الآن على ثلاث خدمات فقط: auth (٢٤، مؤجَّل صراحةً حتى إثبات مستهلك حيّ) · sahool-platform (٨، ديون توافقيّة `compat_gateway.py`/`chat_proxy_reference.py` مؤجَّلة عمداً — مُوثَّقة مسبقاً في شريحة Option B) · **video-processor (٨، `routers/streams.py` — اكتشاف جديد لم يُذكَر في التسلسل المُعلَن مسبقاً raster⇐soil⇐mcp_servers⇐erp-bridge، غير مُصرَّح ببدء هجرته بعد؛ يحتاج قرار نطاق مستخدم صريح قبل أيّ لمس).** بهذا تُغلَق كلّ شرائح الهجرة المُعلَنة صراحةً من المستخدم — المتبقّي الثلاثة إمّا مؤجَّل بقرار صريح (auth، sahool-platform) أو ينتظر قراراً جديداً (video-processor). **video-processor (2026-07-31، اكتشاف جديد غير مُعلَن في التسلسل الأصليّ، نُفِّذ بأمر مستخدم صريح بعد اكتشافه في قياس erp-bridge النهائيّ) — ٨ مسارات، كلّها Bearer/JWT عبر `main._get_current_user`:** `POST /streams` و`DELETE /streams/{stream_id}` و`GET /streams/{stream_id}` و`GET /streams` و`POST /streams/{stream_id}/snapshot` و`GET /streams/{stream_id}/snapshot` و`POST /streams/{stream_id}/record/start` و`POST /streams/{stream_id}/record/stop` (كلّها `services/video-processor/routers/streams.py:46-238`) ⇐ `/v1/streams*`. **نمط تسجيل مطابق تماماً لكلّ الشرائح السابقة:** `register_routers(app)` يضمّ الراوتر بلا prefix، فنصّ الديكوريتر هو المسار الفعليّ حرفيّاً. **صفر مستهلك خارجيّ حيّ عبر كامل المستودع (بحث شامل، لا افتراض):** frontend/mobile صفر مرجع حرفيّ؛ لا خدمة أخرى في المستودع تُعرِّف ثابت URL يُشير لـvideo-processor (بخلاف mcp_servers's `ERP_BRIDGE_URL` الميّت — هنا لا يوجد الثابت أصلاً). **فارق مهمّ عن mcp_servers/erp-bridge:** بوّابة `nginx/nginx.v9.conf` (الأساسيّة الحيّة) **تملك فعلاً** `location /api/video/` (بخلاف عدم وجود بوّابة market/odoo في نفس الملفّ) — لكنّها مُقيَّدة صراحةً بنطاقات شبكة داخليّة فقط (`allow 127.0.0.1; allow 10.0.0.0/8; allow 172.16.0.0/12; allow 192.168.0.0/16; deny all;`) مع تعليق صريح في الملفّ يصف video-processor كـ«سطح داخليّ/خدمة-لخدمة» لا واجهة متصفّح. البروكسي شفّاف بلا rewrite (`proxy_pass http://video_backend/;`) فلا حاجة لتعديله. **capability-registry: صفر ربط أصلاً** — video-processor غائب كليّاً من `capabilities/registry/capabilities.json` ومن كلّ ملفّات `docs/capability-registry/domains/*.yaml` (فجوة تغطية قدرات موجودة سابقاً، خارج نطاق هذه الهجرة، لم تُملأ). **تُركت عمداً بعد فحص مباشر:** `docs/audits/PONYTAIL_MCP_STREAMING_KG_EXECUTION_20260625.md`/`MCP_STREAMING_KG_COMPLETION_20260625.md`/`docs/architecture/agentic_rag_mcp_streaming_plan.md` — تحقّق مباشر أكّد أنّها تخصّ مفهوم «streaming» مختلفاً تماماً (بثّ استجابات MCP/RAG، لا بثّ فيديو)، صفر مرجع لـvideo-processor أو `/streams`، لا صلة رغم تشابه الكلمة. **اختبار تكذيب جديد، مُثبَت بكسره فعليّاً ثمّ استعادته:** `tests_v9/test_api_versioning_policy_guard.py::test_video_processor_stream_routes_are_versioned` — كسر مقصود (إعادة `@router.delete(\\\"/streams/{stream_id}\\\")` الخام) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً؛ استعادة الإصلاح أعادته أخضر. **التحقّق:** `guard.collect()` الحيّ يُظهِر صفر `legacy_unversioned_business` لـvideo-processor · `verify_all_generated.py` ⇒ صفر انحراف · `pytest -m unit` بلا انحدار. الجرد الخام: `legacy_unversioned_business` ٤٠ ⇒ **٣٢**، مُوزَّعة الآن على خدمتين فقط كلتاهما مؤجَّلتان بقرار صريح: auth (٢٤) وsahool-platform (٨، ديون توافقيّة `compat_gateway.py`/`chat_proxy_reference.py` مُوثَّقة منذ Option B). **لا شريحة هجرة نشطة متبقّية** بعد هذه الشريحة إلّا بأمر مستخدم صريح جديد. ثمّ ٣٢ ⇐ **٨** بشريحة auth (الأخطر والأخيرة، ٢٤ مساراً تحرس تسجيل الدخول/الجلسة/التفويض على مستوى المنصّة كلّها): تسعة ملفّات راوتر — `email_verify.py` (٤: `/auth/verify`·`/verify/status`·`/verify/confirm`·`/verify/request`)، `invitations.py` (٤: create·list·`/accept`·`DELETE /{invitation_id}`)، `mfa.py` (٣: `/setup`·`/activate`·`/disable`)، `password_reset.py` (٢: `/request`·`/confirm`)، `registration.py` (٢: `/register`·`/change-password`)، `season_edge_sign.py` (١: `/edge-sign`)، `session.py` (٤: `/login`·`/refresh`·`/logout`·`/me`)، `tenants.py` (١: `/tenants`)، `users.py` (٣: list·`/role`·`/deactivate`) — كلّها `/auth/*` ⇐ `/v1/auth/*`. **نفس نمط التسجيل المسطّح** (`register_routers(app)` بلا prefix، نصّ الديكوريتر هو المسار الفعليّ حرفيّاً) المُتّبَع في كلّ شرائح هذه الجلسة. **أعلى مخاطرة مواجَهة حتى الآن — طبقتان إضافيّتان لم تظهرا في أيّ شريحة سابقة:**\n(أ) **بوّابتا nginx الداخليّتان الحرجتان:** `location = /_auth_verify` (يستهدفها `auth_request` من ٧ مواقع مختلفة في `nginx.v9.conf` وحده — vegetation/raster/remote-sensing-workspace ×2 + seasons ×2 — بالإضافة إلى موقع مستقلّ ثانٍ في `frontend/nginx.conf`) و`location = /_auth_edge_sign` (يستهدفها `auth_request` من مسار قبول الموسم `POST /api/v1/seasons/{id}/accept`، `SEASON-RECORD-ENTRY-01` شريحة 3b) — كلاهما `proxy_pass http://auth_backend/auth/verify;` / `.../auth/edge-sign;` حرفيّاً قبل التعديل. عدم تحديثهما كان سيُسقِط JWT verification صامتاً على **كلّ** بلاطة/سطح خلف `auth_request` — فشل مغلق (401) لا اختراق أمنيّ، لكن انقطاع كامل. حُدِّثا في `nginx.v9.conf` و`frontend/nginx.conf` إلى `/v1/auth/verify` و`/v1/auth/edge-sign`. (ب) **موقع `/auth/` العميل المُواجِه في خمسة ملفّات nginx**، يخدم اصطلاحَي عميل مختلفَين بلا كسر أيٍّ منهما: الواجهة (`authApi` قاعدتها `/auth` وتستدعي `.post('/auth/login')` ⇒ مزدوج `/auth/auth/login` على السلك) والعميل المباشر (`/auth/login` مفرد، تُستخدَم في تطبيق الجوّال Flutter عبر `dio` بقاعدة API الكاملة). قبل التعديل: `nginx.v9.conf`/`nginx.fixed.conf`/`frontend/nginx.conf` استخدمت `rewrite ^/auth/auth/(.*)$ /auth/$1 break;` (يُطبِّع المزدوج فقط، يُمرِّر الـURI كما هو لكلا الشكلين)؛ `nginx.unified.conf`/`nginx.light.conf` استخدمتا `proxy_pass http://auth_backend/;` (تجريد شرطة لاحقة، يدعم المزدوج بالمصادفة فقط — تُحقِّق عبر جدول حقيقة يدويّ أنّه **لم يدعم قطّ** الاصطلاح المفرد/المباشر، ثغرة سلوك موجودة سلفاً لا شيء أُدخِلَته هذه الشريحة). الإصلاح المُوحَّد في الخمسة كلّها — قاعدتا `rewrite` بدل واحدة:\n```\nlocation /auth/ {\n    rewrite ^/auth/auth/(.*)$ /v1/auth/$1 break;\n    rewrite ^/auth/(.*)$ /v1/auth/$1 break;\n    proxy_pass http://auth_backend;\n}\n```\nمُتحقَّق بجدول حقيقة يدويّ لكلا المدخلين (`/auth/auth/login` ⇐ القاعدة الأولى تطابق وتُنهي بـ`break` ⇐ `/v1/auth/login`؛ `/auth/login` ⇐ الأولى لا تطابق (تتطلّب حرفيّاً `auth/auth/`) ⇐ الثانية تطابق ⇐ `/v1/auth/login`) ومُتحقَّق أنّه يُبقي `tests_v9/test_auth_rls_and_routing.py::test_nginx_auth_routing_preserves_prefix` أخضر بلا تعديل جوهر التأكيدات (تبحث عن `rewrite\\s+\\^/auth/auth/` حرفيّاً — لا تزال موجودة كالقاعدة الأولى). **لا مُصدِّق nginx محليّاً** (لا ثنائيّ nginx في societal sandbox) — التحقّق اعتمد كليّاً على المراجعة النصّيّة اليدويّة الشاملة بعد كلّ تعديل + حزمة اختبارات التأكيد الساكنة الموجودة (`test_auth_rls_and_routing.py`، `test_nginx_tenant_injection_guard.py`، `test_dev_gateway_raster_tile_auth_guard.py`، `test_season_gateway_nginx_static.py`، `test_season_edge_sign_endpoint_static.py`) كلّها تقرأ وتُطابِق الملفّات كنصّ. **خطر ثالث اكتُشِف بالقراءة لا بالافتراض:** `scripts/ci/endpoint_ui_coverage_gate.py`'s `has_frontend_evidence()` طبّعت تاريخيّاً فقط `/api/v1/auth/*` (اصطلاح sahool-platform المنفصل الخاصّ به) ⇄ `/auth/*`؛ بعد ترقية auth إلى `/v1/auth/*`، ومع إعادة تسمية قاعدة التصنيف `/auth/` ⇒ `/v1/auth/` في `config/endpoint_ui_coverage.json` (لازمة كي تبقى الـ٢٢ مساراً غير المُعفاة مُصنَّفة `farmer`/`admin` مواجِهاً للمستخدم بدل الانحدار الصامت إلى `internal` المُعفى من البوّابة العكسيّة)، كانت ستُنتِج ٢١ «مساراً هارباً» مُزيَّفاً (٢٤ ناقص اثنين مُعفيَين `/edge-sign`/`/me`، ناقص واحد بلا `downstream_surface`) لأنّ التطبيع القديم لا يعرف بادئة `/v1/auth`. الإصلاح: `_AUTH_BACKEND_PREFIX_RE = re.compile(r'^/(?:api/v1|v1)/auth')` تطبّع كلا الاصطلاحين إلى `/auth`. **مُكذَّب فعليّاً:** إعادة المنطق القديم أنتجت فشلاً حرفيّاً بـ٢١ مساراً مُسمّاة؛ استعادة الإصلاح أعادت `PASS — 425 core + 52 إعفاء`. حُدِّثت أيضاً ٢٠ إدخال `core_endpoints` (endpoint فقط؛ evidence بقي كما هو — نصّ استدعاء العميل الحرفيّ `/auth/x` لم يتغيّر، nginx يترجمه) وإدخالا إعفاء (`/auth/edge-sign`، `/auth/me`) إلى `/v1/auth/*`. **مستهلكون حقيقيّون فُحِصوا بالكامل (لا افتراض):** `frontend/src/services/api/auth.ts` (٢٣ استدعاءً مُؤكَّداً) و`mobile/sahool_app/lib/services/api_service.dart` (٨ استدعاءات) **لم يُعدَّلا** — كلاهما يستدعي `/auth/*` الحرفيّ عبر nginx الذي يترجمه الآن؛ تعديلهما كان سيُغيّر عقداً عميلاً لا حاجة لتغييره. `bots/telegram/main.py:350` استدعاء مباشر لـ`AUTH_SERVICE_URL` (افتراضه `http://sahool-auth:8000` — **بلا** nginx بينهما) عُدِّل حرفيّاً `/auth/login` ⇐ `/v1/auth/login`. **اكتشاف رابع بالقراءة الذاتيّة (لم يُذكَر في تقرير الوكيل الخلفيّ):** `frontend/src/config/endpoints.ts`'s `VITE_API_MODE=dev` (منافذ localhost مباشرة بلا بوّابة) كان افتراضه لقاعدة auth `http://localhost:8120` بلا `/v1` — استدعاء `authApi.post('/auth/login')` غير المُعدَّل كان سيصل حرفيّاً `http://localhost:8120/auth/login`، مسار لم يعد قائماً على الخدمة. أُصلِح بتضمين `/v1` في `devDefault` نفسه (`${localHttp(8120)}/v1`) — القاعدة فقط، لا نصّ استدعاء العميل — فتبقى `authApi.post('/auth/login')` كما هي بلا تغيير وتُنتِج `http://localhost:8120/v1/auth/login` الصحيح. **اختبار تكذيب جديد، مُثبَت بكسره فعليّاً ثمّ استعادته:** `tests_v9/test_api_versioning_policy_guard.py::test_auth_service_routes_are_versioned` — كسر مقصود (إعادة `@router.post(\"/auth/login\")` الخام في `session.py`) أسقط الاختبار فوراً باسم المسار المتسرِّب حرفيّاً؛ استعادة الإصلاح أعادته أخضر. **ملفّات اختبار مُحدَّثة** (استدعاءات `TestClient`/curl مباشرة بلا nginx بينها، فتتطلّب المسار الجديد حرفيّاً): `test_auth_e2e.py`، `test_auth_service.py`، `test_security.py`، `test_mfa_hardening_integration_v29_5.py`، زائداً تحديثات نصّيّة/توثيقيّة (لا تأكيدات تنفيذيّة) في `test_auth_rls_and_routing.py`، `test_dev_gateway_raster_tile_auth_guard.py`، `test_season_gateway_nginx_static.py`، `test_season_edge_sign_endpoint_static.py`، `test_nginx_tenant_injection_guard.py`. **سكربتات/شل مُتحقَّق أنّها تعمل عبر البوّابة فعبرت بلا تعديل** (قاعدتها الافتراضيّة `http://localhost` أو `http://localhost:8080` تمرّ عبر nginx الذي يترجم): `scripts/smoke_e2e.py`، `scripts/e2e/live_full_e2e.py`، `scripts/e2e/spatial_flows.py`، `tests_v9/smoke_frontend_activation.sh`، `tests_v9/test_smoke_e2e.py`. **وثائق مُحدَّثة:** `docs/openapi/API_MAP.md`/`ROUTE_INVENTORY.json` (١٢ من ٢٤ المُدرَجة سلفاً — نقص جزئيّ سابق الوجود، لم يُملأ)، `docs/capability-registry/domains/farm_management.yaml` (٢٥ سطراً بما فيها تكرار `edge-sign` في قدرة الموسم)، `satellite.yaml` (٢)، `security.yaml` (٣)، `docs/security/SEC-3.1_approvals_user_role.md`، `docs/runbooks/REAL_ENV_VERIFICATION_RUNBOOK.md`، `docs/api/UI_DEBT_MAP.md` (٦ إشارات في جدول دَين واجهة تاريخيّ لكنّه حيّ يُحرَّر يدويّاً — لا مولِّد). `docs/NGINX_ROUTING.md` أعيدت كتابة قسم `/auth/*` بالكامل — كانت تصف تقنيّة القطع بشرطة لاحقة (`proxy_pass http://auth_backend/;`) وهي أصلاً **لا تطابق** ما في `nginx.v9.conf` الفعليّ (كان يستخدم `rewrite` سلفاً قبل هذه الشريحة) — تصحيح استباقيّ لتوثيق كان زائفاً من قبل هذه الهجرة، وليس فقط تحديث مسار. **تُركت عمداً بعد فحص مباشر (سجلّ تاريخيّ، لا يُزيَّف بأثر رجعيّ):** `docs/audits/CODE_REVIEW_REPORT.md`، `CRITICAL_REVIEW_RESPONSE.md`، `NEW_TESTS_AND_FIXES.md` (روايات جولات مراجعة ماضية مؤرَّخة ضمنيّاً)، `docs/history/*`، و`docs/architecture/brain_deferral_baseline.json` (لقطة مجمَّدة لحارس انحراف مؤجَّل — تعديلها يُزيّف الأساس لا يُصحِّحه). **التحقّق:** `guard.collect()` الحيّ يُظهِر صفر `legacy_unversioned_business` لـauth (٢٤/٢٤ `versioned`) · `endpoint-ui-coverage-reverse-gate` ⇒ PASS (425 core + 52 إعفاء) · `verify_all_generated.py` ⇒ صفر انحراف من أوّل تمريرة (نفس تحسين video-processor: تشغيل كلّ المولِّدات المكتشَفة استباقيّاً) · `pytest -m unit` ⇒ 3771 نجاح، صفر فشل، 26 تخطٍّ، بلا انحدار. الجرد الخام: `legacy_unversioned_business` ٣٢ ⇒ **٨**، كلّها الآن sahool-platform (ديون توافقيّة `compat_gateway.py`/`chat_proxy_reference.py` مُوثَّقة منذ Option B). **auth كانت آخر شريحة هجرة نشطة مُعلَنة في هذه الجلسة** — لا شريحة متبقّية إلّا بأمر مستخدم صريح جديد. ثمّ إحكام حوكميّ للحارس نفسه (لا هجرة مسار جديدة): `api_versioning_policy_guard.py`'s `--check` كان يفرض شرطاً واحداً فقط — `len(current) <= ceiling` (عدّ صرف) — لا مقارنة هويّة. هذا يسمح نظريّاً باستبدال دَين قديم مُقفَل بدَين جديد مختلف تماماً بقي العدد الكلّي ثابتاً أو انخفض: إغلاق مسارَين ثمّ اكتشاف/فتح مسارَين آخرَين مختلفَين كلّياً كان سيمرّ الحارس صامتاً طالما العدد لا يتجاوز السقف — لم يكن يُثبَت أنّ **نفس** المجموعة تتقلّص، فقط أنّ **حجمها** لا ينمو. أُضيف حقل `routes` (المجموعة المُجمَّدة الصريحة للثمانية مسارات المتبقّية، كلّها sahool-platform) وشرط ثانٍ مستقلّ في `--check`: `current_legacy_set ⊆ frozen_routes_set`. أيّ مسار في القائمة الحيّة غير موجود في `routes` المُجمَّدة يُسقِط CI فوراً بالاسم الصريح، بصرف النظر عن العدد الكلّي. **مُكذَّب فعليّاً:** محاكاة استبدال (إزالة مسار مُجمَّد وإضافة مسار وهميّ بديل بنفس العدد الإجماليّ) أنتجت فشلاً حرفيّاً يسمّي المسار الوهميّ الهارب؛ استعادة الحالة الحقيقيّة أعادت `api_versioning_policy_check_ok`. الحقل السابق `ceiling` (عدد صرف) يبقى للتوافق الرجعيّ والعرض السريع، لكنّ الإنفاذ الفعليّ الآن على المجموعة لا العدد. ثمّ ٨ ⇐ **٧** بتصحيح صدق في الجرد لا بهجرة: `POST /api/chat` (`services/sahool-platform/api/chat_proxy_reference.py:109`) كان يُحتسَب ديناً بينما **التطبيق العامل لا يخدمه إطلاقاً** — نفس صنف الإيجابيّة الكاذبة `GET /probe` داخل `test_correlation_middleware.py` المُصلَحة 2026-07-30 باستبعاد بنيويّ. أربعة أقفال بنيويّة مستقلّة تُثبِت عدم التركيب: (١) الملفّ في `api/` لا `api/routers/`؛ (٢) `register_routers()` يُسجّل تلقائيّاً وحدات `api/routers/` وحدها عبر `pkgutil.iter_modules(_routers_pkg.__path__)` (`api/router_registry.py:41`)؛ (٣) صفر استيراد إنتاجيّ في المستودع كلّه (المطابقتان الوحيدتان تعليق عربيّ في `ai_provider_config.py:4` ونصّ docstring داخل الملفّ نفسه — لا `import` واحد)؛ (٤) الملفّ لا يُصدِّر `router` إطلاقاً بل `app = FastAPI(...)` داخل `try/except ImportError` كمثال قابل للتشغيل مستقلّاً (`uvicorn api.chat_proxy_reference:app`)، وdocstring صريح: «هذا ملف مرجعي يوضّح النمط… النواة الحالية لا تتضمّن خادماً». **سابقة مستقلّة قائمة تؤكّد نفس الحكم:** `tests_v9/test_endpoint_auth_coverage.py:60` يستثني هذا الملفّ **بالاسم** منذ قبل هذه الشريحة، بتعليق يصفه «نمط مرجعيّ لـproxy آمن». الاستبعاد بنيويّ في `_UNMOUNTED_REFERENCE_FILES` **مسنود باختبار يُثبِت الحقائق الأربع لا يدّعيها** (`test_chat_proxy_reference_is_structurally_unmounted`) — **مُكذَّب بكسر الأربعة فعليّاً واحداً واحداً:** نسخة داخل `api/routers/` ⇒ فشل · تغيير آليّة المسح التلقائيّ ⇒ فشل مُسمّى («router_registry no longer auto-mounts…») · إضافة `import` إنتاجيّ ⇒ فشل يسمّي الملفّ المستورِد · إضافة `router = …` ⇒ فشل («module now exports `router`»)؛ والاستعادة تُعيده أخضر في الحالات الأربع. فلو رُكِّب الملفّ يوماً يسقط CI ويُجبِر إعادة التقييم بدل بقاء الاستثناء صامتاً.\n\n**والأهمّ — إعادة توصيف السبعة الباقية: ليست ديناً مؤجَّلاً بل عقود توافق دائمة.** فحص مباشر لـ`compat_gateway.py` (المصدر، لا الافتراض) أظهر أنّ السبعة كلّها **أسماء بديلة لمسارات قديمة**: أربعة تُعيد حمولة صحّيّة خالصة `{\"status\": \"ready\", \"service\": …, \"mode\": \"alias\"}` بصفر بيانات عمل (`:26-49`)، وثلاثة تمرير ضيّق موثَّق في المصدر بأنّه «fallback ضيق وآمن… حين تُمرّر بيئات nginx/compose **قديمة** مساراتها إلى المنصّة» (`:105-109`). نقل أيٍّ منها إلى `/v1/` **يُلغي الغرض الذي وُجِدت له** — اسم بديل لمسار قديم يعيش على مسار جديد لا يخدم أحداً، والعميل القديم الذي يستدعي `/api/indicators/readyz` يتلقّى 404 وهو بالضبط الانقطاع الذي أُنشِئ الاسم البديل لمنعه. **فحملة API-VERSIONING-GUARD-IS-A-MIRROR-01 مكتملة موضوعيّاً:** كلّ ما كان قابلاً للهجرة هوجِر (٧٤٩ ⇐ ٧ عبر الجلسة)، والباقي عقد دائم يُوثَّق ولا يُهاجَر. **ملاحظة صدق مؤجَّلة عمداً:** الأربعة الصحّيّة تفوتها بوّابة `infra` لسبب تقنيّ بحت — `_classify()` يطابق `/health` بادئةً أو عضويّة مضبوطة في `{\"/readyz\", …}`، و`/api/…/readyz` **لاحقة لا بادئة**. إعادة تصنيفها `infra` (٧ ⇐ ٣) دفاعها الدلاليّ قويّ ونظيرها `/runtime-identity` أُعيد تصنيفه سابقاً، **لكنّها لم تُنفَّذ هنا**: `/runtime-identity` كان مسنوداً بعقد مُجمَّد صريح في CLAUDE.md (`platform_route_placement_contract.json`) وهذه بلا نظير، وخفض العدد بإعادة تصنيف بلا عقد يُقرأ كتحسين رقم لا كتصحيح. تُترَك لقرار مالك صريح. ثمّ ٧ ⇐ **٠** بإدخال **عقد التوافق الدائم** — لا هجرة مسار ولا إخفاء تصنيفيّ، بل تسمية صادقة لِما أثبتته الشريحة السابقة: السبعة ليست ديناً بل عقود. أُنشئ `docs/architecture/permanent_compatibility_contract.json` مصدراً قانونيّاً وحيداً، مفتاح كلّ إدخال **method + مسار مُطبَّع** (التطبيع يستبدل كلّ معامل بـ`{}` فيُعرَّف شكل المسار لا تسمية معاملاته: `/api/raster/{path:path}` ⇒ `/api/raster/{}`، فإعادة تسمية معامل لا تُسقِط المطابقة ولا تفتح ثغرة). `_classify()` صار `(path, method=None)` يستشير العقد، فأنتج فئتين مُسمّاتين: `permanent_health_compat_alias` (الأربعة الصحّيّة — حمولة `{\"status\": \"ready\", …}` خالصة) و`permanent_compatibility_gateway` (الثلاثة التمريريّة). **ليست `infra`:** الفئتان مرئيّتان في الجرد والـCSV وقائمة السماح المولَّدة (حقل `permanent_compatibility_routes` الجديد) — تُسمّى ولا تُخفى، وهو الفرق الجوهريّ عن إعادة التصنيف التي رُفِضت في الشريحة السابقة. **الإنفاذ ثلاثيّ:** (١) السقف ٠ ⇒ أيّ مسار غير مُصدَّر جديد لا يطابق العقد يُسقِط CI؛ (٢) إضافة اسم بديل دائم تتطلّب تعديل ملفّ العقد صراحةً — adjudication مرئيّ في المراجعة لا قرار صامت؛ (٣) **إنفاذ عكسيّ:** إدخال في العقد بلا مسار حيّ مطابق (عقد بائت) يُسقِط CI كذلك — الاتّجاه الأمامي مضمون بنيويّاً (التصنيف يأتي **من** العقد فلا مسار دائم خارجه) لكنّ العكس لا، وإدخال ميّت يُغطّي سلفاً مساراً يُعاد إدخاله بنفس التوقيع بلا adjudication جديد. **مُكذَّبة الثلاثة فعليّاً.** وبهذا يصير التقرير صادقاً: `legacy_unversioned_business = 0` بدل «سبعة ديون» التي لم تكن ديوناً قطّ.",
  "routes": []
}
