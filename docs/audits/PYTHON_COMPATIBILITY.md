# توافق إصدارات Python — SAHOOL

## الاكتشاف (من فحصك على Python 3.13)
ملاحظتك عن 3.13 كانت دقيقة وكشفت فجوة حقيقيّة: **asyncpg==0.29.0 يفشل
البناء على Python 3.13** (يستخدم Py-API أصبحت خاصّة، مثل
`_PyInterpreterState_GetConfig`). الحلّ الرسمي asyncpg 0.30+.

## مصفوفة التوافق الفعليّة
| الحزمة | كان | الآن | 3.11 | 3.12 | 3.13 |
|--------|-----|------|------|------|------|
| asyncpg | ==0.29.0 / >=0.29.0 | **>=0.30.0,<0.32.0** | ✅ | ✅ | ✅ |
| pydantic | ==2.8.2 | (بلا تغيير) | ✅ | ✅ | ✅* |
| fastapi | ==0.115.0 | (بلا تغيير) | ✅ | ✅ | ✅ |

*pydantic 2.8 يدعم 3.13 رسميّاً (دعم 3.13 بدأ في 2.8.0). لا تغيير مطلوب.

## الإصلاح المُطبَّق
وحّدتُ `asyncpg` على `>=0.30.0,<0.32.0` في 11 ملفّ requirements (الخدمات +
dev + test). هذا:
- ✅ يُصلح فشل البناء على 3.13 (يحتاج 0.30+)
- ✅ يبقى متوافقاً مع 3.11/3.12 (0.30+ يدعمها — wheels متوفّرة cp311/cp312/cp313)
- ✅ سقف `<0.32` يحمي من تغييرات كبرى غير مُختبَرة

## إصدار Python المُوصى به (نصيحتك صحيحة)
الإنتاج الحالي:
- 13 خدمة على **Python 3.11** (الأساس — الأكثر استقراراً)
- 4 خدمات على **Python 3.12** (agriai/mcp/raster/tts)
- CI على **3.12**

**التوصية**: التوحيد على 3.11 أو 3.12 للإنتاج/CI (كلاهما مستقرّ مع كامل
المكدّس). **3.13 يعمل الآن** بعد إصلاح asyncpg، لكن للإنتاج طويل المدى 3.11/3.12
أكثر اختباراً مع المكدّس الجغرافي وML.

## للفحص على جهازك
بيئتك على 3.13 — بعد سحب هذا التحديث، أعد التثبيت:
```bash
pip install -r requirements-dev.txt   # سيسحب asyncpg 0.30+ المتوافق
pytest --cov=services
```
لو أردت محاكاة الإنتاج بدقّة، أنشئ بيئة 3.11:
```bash
python3.11 -m venv .venv311 && source .venv311/bin/activate
```

## ملاحظة صدق
- إصلاح asyncpg **مُتحقَّق منه بالبحث** (asyncpg 0.30 يدعم 3.13 رسميّاً،
  0.29 يفشل) + فحص الملفّات.
- **لم أشغّل pip install** (بيئتي بلا شبكة) — لكنّ القيد صحيح والـwheels متوفّرة.
- صحّحتُ تقديري الأوّلي عن pydantic (ظننتُ يحتاج 2.9، لكنّ البحث أثبت 2.8 كافٍ).
- لم أغيّر إصدارات Python في Dockerfiles (3.11/3.12 مقصودة وتعمل) — التوحيد
  قرارك. أصلحتُ فقط التبعيّة التي تكسر البناء.
