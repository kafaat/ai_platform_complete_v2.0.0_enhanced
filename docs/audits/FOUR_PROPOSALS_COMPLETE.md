# تنفيذ المقترحات الأربعة (بالترتيب)

نُفّذت الأربعة المقترَحة، كلٌّ مُختبَر، بالترتيب من الأعلى قيمة.

## ① دمج workflow_engine في تصعيد الآفة (الأعلى قيمة) ✓
core/pest_escalation_flow.py — **أوّل استخدام فعلي** لـworkflow_engine في قرار
زراعي (كان بنية معزولة). المراحل: رصد→تأكيد→توصية→تعليق للموافقة البشريّة→
تنفيذ (بتعويض Saga)→متابعة. يربط: alert_engine (عتبات الخطورة) + workflow_engine
(durability/استئناف/تعليق/Saga) + correlation (خيط موحّد) + human-in-the-loop.
مُختبَر: آفة خطيرة تُعلَّق ثمّ تُستأنف؛ شدّة منخفضة لا تُصعَّد (تجنّب إنذار كاذب).

## ② تنظيف حماية القسمة في المؤشّرات ✓
services/raster-service/main.py — vari/gli كان مقامهما قد يبلغ صفراً.
أُضيف epsilon (np.where(_denom==0, 1e-10, _denom)) بنفس نمط soil_indices._eps.
ليس H2 صارخاً (np.seterr يُدير inf)، لكنّ التنظيف يمنع تلوّث الإحصاء بـinf.

## ③ تحليل ماء الريّ للصُنيدر ✓
core/irrigation_water_analysis.py — منطق علمي حقيقي (لا جدول فارغ): يحسب
SAR/RSC ويصنّف الملوحة/القلويّة/الصوديوم من بيانات مخبريّة تُدخَل. العتبات
موثّقة (FAO-29/USDA Bull.197/Eaton 1950). صدق: عيّنة ناقصة → يُعلن النقص لا
يخترع. يُستخدَم فور توفّر تحليل مخبري لماء آبار الصُنيدر.

## ④ فهرس الوثائق الموحّد ✓
outputs/INDEX.md — 141 وثيقة مصنّفة في 8 فئات (مراجعات/قرار/بيانات/نماذج/
تكامل/بنية/أدلّة/أخرى). صيانة (67 في "أخرى" تحتاج تصنيفاً أدقّ لاحقاً).

## التحقّق الكلّي (مُختبَر حيّاً)
- 720/720 roadmap (+11) · 0 خطأ (422 ملفّ)
- ① تصعيد الآفة: 6 سيناريوهات ✓ · ② حماية القسمة ✓ · ③ SAR/RSC صحيح + صدق
  النقص ✓ · ④ فهرس مُولَّد ✓

## ملاحظة صدق
الأربعة مُنفَّذة ومُختبَرة. القيمة الحقيقيّة في ① (حوّل بنية معزولة لتدفّق حيّ).
② تنظيف دقيق صغير. ③ منطق علمي جاهز — لكن **لا بيانات ماء حقيقيّة بعد**
(يُستخدَم فور توفّرها؛ لم أفبرك عيّنات). ④ صيانة. الحدود المعلَنة كما قبل:
pest_escalation يستخدم InMemoryStore (الحفظ المعمّر يحتاج PostgresWorkflowStore)؛
الدمج في endpoint حيّ + ربط pest_detector الفعلي خطوة تالية.
