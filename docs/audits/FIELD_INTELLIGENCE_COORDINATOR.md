# الربط الكامل: field_intelligence_coordinator (مسار التنفيذ)

ربطتُ المايسترو بكلّ المصادر وفق المنهجيّة التي أقرّتها المراجعتان. الآن
يوجد **مسار تنفيذ كامل** يجمع المؤشّرات الخمسة في قرار محكوم.

## المنهجيّة المُنفَّذة (الطبقات الخمس)
```
① Collectors      → جمع الحقائق الخام (ممنوع اتّخاذ قرار)
② Normalizers     → تطبيع كلّ مصدر لـSignalInput موحّد
③ Fusion (مايسترو) → compose_field_state → CanonicalFieldState (مرّة واحدة)
④ Policy          → قرار فوق الحالة الموحّدة (لا تفسير خام)
⑤ Guardrails      → القواعد الحاكمة قبل إصدار أيّ توصية
```

## المصادر المربوطة (مدخلات القرار)
| المصدر | الإشارة | الثقة (سقف المصدر) |
|--------|---------|---------------------|
| الاستشعار | ndvi/ndre/ndsi/ndwi | medium (استقرائي) |
| التربة | soil_ec | high (تحليل مخبري) |
| الطقس | heat_risk | medium |
| الميدانيّة | ملاحظة المزارع | low (اجتماعي) |

## القواعد الحاكمة المُطبَّقة
1. **الدمج مرّة واحدة**: compose_field_state ينتج الحالة، والقرار يعمل فوقها
   (policy-over-state) — يمنع double-interpretation bugs.
2. **حلّ التعارض محسوم في المايسترو**: القرار يتبع effective_status (مثلاً
   ملوحة حرجة → soil_remediation، تتجاوز NDVI الإيجابي).
3. **trend > snapshot**: NDVI هابط → إنذار مبكر حتّى لو القيمة طبيعيّة.
4. **لا توصية بلا حَوكمة**: القرار القابل للتنفيذ يمرّ بـguardrails_fn.
5. **الثقة المنخفضة تُرفق صراحةً**: بيانات ناقصة/قديمة → تحذير تحقّق ميداني.
6. **فصل الطبقات**: لا ربط مباشر بين الخدمات (لا spaghetti) — المنسّق نقطة
   الالتقاء الوحيدة.

## الصدق
- المصادر المتعذّرة تُعلَن في missing_signals (لا اختراع)
- المصادر تُحقَن (للاختبار) أو تُجلب HTTP على جهاز التشغيل
- لو فشل مصدر، يُسجَّل بالخطأ لا يُختلق
- حقل سليم → لا إجراء (لا إنذار كاذب)

## التحقّق
- 500/500 roadmap (+8) · 0 خطأ ترجمة · offline 34/0
- 8 اختبارات: المسار الكامل/policy-over-state/زمني/تطبيع ٥ مصادر/صدق/
  حَوكمة/سليم/فصل طبقات

## الحالة المعماريّة الآن
- ✅ اللبنات موجودة (fusion/confidence/trend/provenance)
- ✅ المايسترو يجمعها (CanonicalFieldState)
- ✅ **المسار التنفيذي يربط المصادر→المايسترو→السياسة→الحَوكمة** (هذا التسليم)
- 🔸 يبقى: ربط المصادر الحيّة الفعليّة (HTTP لـweather/soil/raster services)
  + ربط guardrails الحيّ — على جهاز التشغيل (المنطق والتنسيق جاهزان ومُختبَران).

## ملاحظة صدق
المسار كامل ومُختبَر بمصادر محقونة (محاكاة). الربط الحيّ بالخدمات الفعليّة
(نداءات HTTP) وbouّابة guardrails الحيّة يحتاجان الخدمات قائمة على جهازك —
بنيتُ نقاط الحقن (weather_fn/soil_fn/sensing_fn/guardrails_fn) لتوصلها مباشرةً
دون تغيير المنطق. لم أزعم اتّصالاً حيّاً.
