# ربط الاقتصاد + الأصناف + نيّة المزارع بالمنسّق

نفّذتُ ما هو صحيح هندسيّاً من فجوات التدقيق: ربط الطبقات المبنيّة-لكن-المعزولة
بالمنسّق، وإضافة نيّة المزارع. كلّها تكمّل منطق القرار فعليّاً.

## ما نُفّذ
### ١. الاقتصاد (كان مبنيّاً، معزولاً → الآن مربوط)
- `assess_economics()` يعيد استخدام `analyse_market` (تقلّب السعر/المخاطر)
- يحسب نسبة العائد/التكلفة (benefit_cost_ratio)
- **قيد اقتصادي صريح**: عائد < تكلفة → "لا تدخّل قد يكون أصحّ" (economic_caution)
- تقلّب سعري عالٍ → تحذير سوقي في القرار
- يحقّق نقطة المراجعة: القرار = **economically-constrained optimum** لا
  biological optimum فقط.

### ٢. الأصناف (كانت جزئيّة → الآن مربوطة كمُعدّل)
- `load_variety_card` يحمّل ملفّ الصنف الكامل
- تحمّل الجفاف (drought_tolerance) + فئة النضج (maturity_class)
- **مُعدّل عتبة الملوحة** (salt_tolerance_modifier) يُزيح العتبة الحرجة للصنف
- يحقّق نقطة المراجعة: الصنف **interpretation modifier** لا metadata.

### ٣. نيّة المزارع (كانت غير مبنيّة → الآن مبنيّة)
- farmer_objective (maximize_yield|minimize_cost|water_saving|risk_reduction)
- يدخل الحالة الموحّدة كوزن للقرار (decision_weighting)
- يحوّل القرار من علميّ إلى تشغيليّ.

## ترتيب التأثيرات (من المراجعة، مُطبَّق)
الاستشعار (واقع) · التربة (قيد بنيوي) · الطقس (ضغط) · GDD/Kc (توقيت) ·
الصنف (مُعدّل عتبات) · الاقتصاد (قيد) · anwa (قرينة موسميّة لا حاكمة).

## التحقّق
- 522/522 roadmap (+14) · 0 خطأ ترجمة · offline 34/0
- اختبار: عائد 60 / تكلفة 100 → نسبة 0.6 → غير مُجدٍ → caution اقتصادي ✓
- الصنف wheat_local_highland → تحمّل جفاف medium، عتبة ملوحة مُعدّلة ✓

## ما يعيد استخدامه (لا ازدواج)
analyse_market · load_variety_card · CropKcProfile · كلّ لبنات المايسترو.

## ما لم يُنفَّذ (وسبب صادق)
لم أبنِ هذه لأنّها **بنية تحتيّة تشغيليّة لا منطق قرار** — تحتاج جهازك/خدماتك:
1. **تكاليف المدخلات المفصّلة** (بذور/سماد/عمالة لكلّ مرحلة): تحتاج بيانات
   أسعار محليّة فعليّة — أضفتُ المدخل الاقتصادي العامّ (تكلفة/عائد) بدل تخمين
   أرقام لا أملكها. صدق: لا أخترع أسعار سماد يمنيّة.
2. **حفظ الحالة في الذاكرة** (farm_memory persistence): يحتاج قاعدة بيانات
   حيّة على جهازك (PostgreSQL/Timescale) — المنطق جاهز، الحفظ الحيّ لاحق.
3. **endpoint حيّ + محوّلات HTTP**: يحتاج الخدمات قائمة — نقاط الحقن جاهزة.
4. **IoT**: sensor_intake موجود لكن لا أجهزة IoT فعليّة لديك بعد — ربطه الآن
   يبني على فراغ. يُربط عند توفّر المستشعرات.

## ملاحظة صدق
نفّذتُ ما يكمّل **منطق القرار** بإعادة استخدام الموجود (اقتصاد/أصناف/نيّة).
لم أبنِ بنية تحتيّة تشغيليّة (persistence/endpoints/IoT) تحتاج جهازك أو
بيانات لا أملكها — قول ذلك أصدق من بناء واجهات على فراغ. صحّحتُ أسماء الحقول
بقراءة الكود (variety_traits لا traits، maturity_class بالأعلى).
