# تطويرات محرّك الـworkflow — تقييم وبناء

المراجعة اقترحت 4 تطويرات للمحرّك (DSL، Saga، observability، versioning).
وفق المبدأ: تقييم كلٍّ مقابل فجوة الصلابة، بناء الحقيقي لا الكلّ.

## التقييم والقرار
| التطوير | تقييم | قرار |
|---------|-------|------|
| Saga Compensation (rollback) | فجوة صلابة حقيقيّة — يستأنف لكن لا يتراجع | ✅ بُني |
| Observability (traces/stalled) | فجوة تشغيليّة، أساس بسيط | ✅ بُني (خفيف) |
| Versioning | أخطر مستقبلاً، لكن الحدّ الأدنى يكفي الآن | ✅ بُني (كشف عدم تطابق) |
| Workflow DSL | راحة لا صلابة — الـworkflows تُكتب بايثون وتعمل | ⏸ مؤجّل (ليس فجوة) |

## ما بُني (في workflow_engine.py)
**١. Saga Compensation**: compensate اختياريّة لكلّ خطوة؛ عند الفشل مع
compensate_on_failure، تُستدعى تعويضات الخطوات المكتملة **بالترتيب العكسي**
→ status=COMPENSATED. يحلّ: send_sms→reserve→create_order يفشل → يتراجع.
صدق: الخطوة بلا compensate تُترَك (لا ندّعي تعويضاً لم يحدث)؛ توافق خلفي
(بلا الخيار يبقى FAILED قابلاً للاستئناف).

**٢. Observability**: workflow_trace (أثر: حالة + خطوات + توقّف + سبب) +
summarize_workflows (عدد لكلّ حالة + العالقة + needs_attention). نقيّ من
الحالة المحفوظة.

**٣. Versioning**: workflow_version على الحالة؛ استئناف بنسخة مختلفة يُرفَض
بصدق (عدم التطابق مُعلَن) — يمنع كسر workflows قديمة بتعريف جديد.

## ما لم يُبنَ (صدق)
- **DSL**: راحة لا صلابة. الـworkflows تُكتب بايثون الآن وتعمل؛ DSL طبقة فوق
  ما يعمل، ليست فجوة. بناؤه = تحسين تجربة لا معالجة عيب — مؤجّل حتّى تتراكم
  workflows تبرّر الطبقة الإضافيّة.
- **Versioning migration الكامل**: بنيتُ الكشف (يرفض عدم التطابق) لا الترحيل
  (تحويل workflow قديم لتعريف جديد) — لا workflows قديمة بعد لتبرّره. الكشف
  يكفي الآن؛ الترحيل عند الحاجة الفعليّة.

## التحقّق (مُختبَر حيّاً)
- 697/697 roadmap (+6) · 0 خطأ (419 ملفّ)
- Saga تعويض عكسي ✓ · توافق خلفي ✓ · versioning يرفض ✓ · trace ✓ · summary رصد ✓

## ملاحظة صدق
بنيتُ 3 من 4 (الفجوات الحقيقيّة)، وتركتُ DSL صراحةً (راحة لا صلابة — تجنّب
over-engineering مبكّر، نفس مبدأ عدم استنساخ Temporal). كلّها مُختبَرة حيّاً.
الحدّ المعلَن: المحرّك ومُحسّناته بنية نقيّة مُختبَرة؛ الحفظ المعمّر يحتاج
PostgresWorkflowStore (migration v16 جاهز)، والدمج في تدفّق زراعي فعلي خطوة
تالية. لم أبنِ DSL/migration كاملاً لأنّهما يحلّان مشاكل غير موجودة بعد.
