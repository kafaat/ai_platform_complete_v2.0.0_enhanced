# إغلاق البندين الأخيرين من المراجعة الخارجيّة

## ١. supervisor /agent/optimize — body-trust (متوسط)
- كان optimize_farm يمرّر tenant_id=query.tenant_id من الجسم (3 مواضع:
  remote_sensing + crop_model + market) → حامل أيّ توكن ينتحل مستأجراً آخر.
- الإصلاح: trusted_tenant_id = user.get("tenant_id") من التوكن المُتحقَّق
  (نفس نمط process_query). صفر body-trust الآن في الخدمة.

## ٢. MarkdownV2 escaping (موثوقيّة)
- parse_mode=MARKDOWN_V2 عامّ؛ القوالب الثابتة مُهيّأة يدويّاً (تعمل)، لكنّ
  القيم الديناميكيّة كانت تفشل بـ400 مع رموز خاصّة (. - ( ) إلخ).
- الإصلاح: _md2() escape مطبّق على كلّ القيم الديناميكيّة (10 مواضع):
  - إدخال المستخدم (اسم المحصول) — كان الأخطر (مستخدم يكتب أيّ شيء)
  - health_status الديناميكي
  - رسائل الأخطاء str(e) (قد تحوي backtick/backslash يكسر حتّى code span)
- لم أحوّل لـHTML (59 سطر تنسيق) — القوالب الثابتة تعمل؛ المخاطرة كانت في
  الديناميكي فقط، وأُغلقت بـescape موجّه (أقلّ مخاطرة من إعادة كتابة الكلّ).

## التحقّق
- 326/326 · 329 ملفّ يُترجم · YAML 31 خدمة
- optimize: صفر tenant_id من الجسم · 10 قيم ديناميكيّة بـ_md2

## الحالة الأمنيّة النهائيّة (بحسب المراجعة الخارجيّة)
كلّ الثغرات الحرجة (4) والعالية أُغلقت. البندان المتوسّط/الموثوقيّة أُغلقا الآن.
النسخة نظيفة أمنيّاً بحسب ما فُحص بنيويّاً.

## ⚠️ يبقى عليك (لا يُغني عنه الفحص الساكن)
1. **دوّر كلّ الأسرار** — .env شُحن مرّة في النسخة الأولى (محذوف الآن لكن قيمه
   تسرّبت). غيّر JWT_SECRET, DB_PASSWORD, WEBHOOK_SECRET, AGENT_TOKEN, إلخ.
2. **اختبر العزل حيّاً** — PostgreSQL: مستأجران A/B، أكّد صفر تسرّب.
3. **التشغيل الحيّ** — لم أشغّل Docker؛ كلّ التحقّق بنيويّ.
4. الدقّة العلميّة الزراعيّة — تحتاج خبيراً.
