# خطّة ما بعد التشغيل — المراجعة والتنفيذ

## مراجعتي النقديّة للوثيقة
**صحيح فيها (أتّفق):**
- المبدأ الجوهري: بعد التشغيل **نقيس الثقة لا نضيفها** — يتّفق مع كلّ تحذيراتي
- قائمة "تجنّب" (لا AST/DSL/invariants/mock rewrite جديد) — صحيحة تماماً
- إطار القرار الثلاثي (APPROVED/STABLE/INVALID) سليم

**تحفّظات (دقّقتُها):**
- تخلط مصطلح account_id (بنية أخرى) بـtenant_id (SAHOOL) — المعنى ينطبق
- تفترض تشغيلاً حيّاً — لا أستطيع فعله نيابةً عنك (يحتاج postgres/docker)

## ما نُفّذ (الخيار القابل للتنفيذ الوحيد)
الوثيقة عرضت "Runtime Truth Report template جاهز". بنيتُه فعليّاً:
**scripts_v9/runtime_truth_report.py** (+ make truth-report):
- يُشغَّل على جهازك بعد make verify
- يجمع: حقيقة الخدمات (docker ps) + حقيقة RLS (isolation test) + مقارنة
  الواقع بـevidence.json
- **يُبلّغ بصدق عمّا لا يقيسه** (docker/DB غائب → "غير متاح" لا تخمين)
- يحذّر من أخطر فخّ: تشغيل RLS كـsuperuser = silent success
- يفرّق المُثبَت بنيويّاً (7 invariants) عن requires_live (2)
- القسم ٤: إطار القرار — يملؤه الإنسان من القياس (لا يدّعي حكماً لم يقِسه)

## الاستخدام (على جهازك)
```
make up && make migrate && make verify    # التشغيل الحيّ
export RLS_TEST_URL='postgresql://sahool_user:PASS@localhost/sahool'
make truth-report                          # يجمع الحقيقة
# راجع runtime_truth_report.md → اتّخذ القرار (APPROVED/STABLE/INVALID)
```

## ما لم أفعله (التزاماً بحدود الوثيقة)
- ❌ لا AST/DSL/invariants جديدة (الوثيقة نفسها تمنعها)
- ❌ لم أدّعِ قياساً حيّاً لا أملكه — السكربت يُبلّغ بصدق
- ❌ لا "final proof test" E2E (الخيار الآخر) — يحتاج تشغيلاً حيّاً لمعنى حقيقي

## التحقّق
- 388/388 roadmap (+1) · 0 خطأ ترجمة · التقرير مُختبَر (يُبلّغ بصدق)

## ملاحظة صدق ختاميّة
هذه الوثيقة **خطّة مفاهيميّة لما بعد التشغيل**، لا طلب كود. الجزء القابل
للتنفيذ (مولّد التقرير) بنيتُه. البقيّة (جمع الحقيقة، القرار) تحدث **على جهازك
بعد make verify** — لا يمكنني تنفيذها لأنّ التشغيل الحيّ يحتاج بنيتك التحتيّة.
هذا حدّ حقيقي، لا قصور.
