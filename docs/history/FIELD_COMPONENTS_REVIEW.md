# مراجعة عميقة لكلّ ما يتعلّق بالحقل (5 مسارات)

## وكيل ١: حاوية المؤشّرات
- indicators-service = stub موثّق صراحةً (__init__.py فقط، لا main.py).
  المنطق الفعلي في vegetation_real/indicators_real.py + المنصّة
  (compute_all_indicators، NDVI/LAI، _classify، _soil_nitrogen_from_om).
- صادق ومتّسق — لا إصلاح (مرشّح للحذف في تنظيف معماري لاحق، قرارك).

## وكيل ٢: خطّ الأنابيب + جلب صور الأقمار
- raster-service (13 endpoint) مكتمل بنيويّاً:
  - Element84 STAC: Sentinel-2 L2A + Sentinel-1 GRD (رادار يخترق الغيوم)
  - /imagery/search (recent/season/radar) · /process (job_id) · /tiles PNG
  - حساب المؤشّرات (NDVI/EVI/SAVI/NDWI/NDMI/GNDVI/fAPAR/VARI/GLI/TGI)
- معالجة البكسل: rasterio يُحقن في بيئة التشغيل؛ بنية صحيحة + ملاحظة صادقة
  عند غيابه (لا حساب وهمي). ✓ لا إصلاح.

## وكيل ٣: عرض المؤشّرات فوق الخريطة — فجوة صدق وُجدت وأُصلحت
- 🟠 SpatialIndicatorsPage تعرض **بيانات محاكاة كاملة** (genGrid بـMath.random،
  seed=42، bbox وهمي) ولا تتّصل بـ/tiles أو /process الحقيقيّة.
- **الأخطر**: لا تحذير ظاهر — المزارع قد يظنّ "البقعة الضعيفة" حقيقيّة في
  حقله ويتّخذ قرار ريّ/تسميد خاطئ.
- الإصلاح: أُضيف بانر تحذير ظاهر "عرض توضيحي — بيانات محاكاة، لا قراءات
  Sentinel فعليّة، لا تتّخذ قرارات بناءً عليها".
- (الربط بـ/tiles الحقيقيّة تطوير مستقبلي — يحتاج raster مُشغّلاً بـrasterio).

## وكيل ٤: أدوات الرسم + المساحة — خطأ حسابي خطير وُجد وأُصلح
- 🔴 geodesicAreaHa كانت **تُرجع نصف المساحة الصحيحة** (50ha لمربّع 100ha)!
  الصيغة خلطت Shoelace + تكامل lat ثمّ قسمت على 2 خطأً.
- **الأثر الحرج**: المساحة تُستخدم في حساب البذور/الأسمدة/المياه → نصف
  المساحة = نصف المدخلات الموصى بها (ضرر زراعي مباشر).
- الإصلاح: الصيغة الكرويّة الصحيحة (R=6378137 WGS84، spherical excess)،
  متحقّق منها عدديّاً (~100ha)، تطابق ST_Area الخلفي.
- DrawController يستخدم Leaflet حقيقي (react-leaflet) — رسم فعلي صحيح. ✓

## وكيل ٥: استدعاء الحقول + التخزين + الترحيلات — سليم
- endpoints الحقول مكتملة (list, validate-geometry, trueup, prescriptions,
  yield-estimate, timeline, pins, walk-plan) — كلّها بـDepends(get_current_user).
- التخزين: PostGIS GEOMETRY(POLYGON,4326) + v13 trigger compute_field_geometry
  (ST_Area على geography = دقيق كرويّاً، صحيح). المساحة الرسميّة المُخزّنة صحيحة.
- validate_field_geometry قويّ: CRS، تقاطع ذاتي، خارج اليمن، مساحة معقولة.
- ✓ لا إصلاح في الخلفية.

## ملاحظة مهمّة (اتّساق المساحة)
- الخلفية (PostGIS) كانت تحسب المساحة **صحيحة** دائماً.
- الخطأ كان في **العرض اللحظي بالواجهة أثناء الرسم** فقط — لكنّه مهمّ لأنّ
  المزارع يرى رقماً خاطئاً (نصف) قبل الحفظ. الآن متّسقان.

## التحقّق
- 323/323 (+4 field) · 329 ملفّ يُترجم · أقواس JSX متوازنة
- اختبار: صيغة المساحة (~100ha) + تحذير المحاكاة

## ملاحظة صدق
- لم أشغّل الواجهة (لا npm build كامل) — تحقّقتُ من الصيغة عدديّاً (Python)
  وتوازن JSX نصّيّاً. اختبار البناء الكامل على جهازك.
- SpatialIndicatorsPage ما زالت محاكاة — أضفتُ تحذيراً صادقاً بدل إخفائه.
  ربطها بـraster الحقيقي تطوير مستقبلي (يحتاج rasterio مُشغّلاً).
- خطأ المساحة (نصف القيمة) كان الأخطر — أثر زراعي مباشر على المدخلات.
