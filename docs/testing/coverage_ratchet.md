# Coverage Ratchet — `services/` unit tests (SEC-5)

بوّابة تغطية غير-كاسرة (non-breaking ratchet) لوظيفة CI **Unit Tests**. الأرضيّة
تُضبط أسفل المقيس الفعليّ بهامش صغير، وتُرفع تدريجيّاً — لا تنزل أبداً.

## المصدر (source of truth)

- الأمر (نفس CI): `pytest -m unit --cov=services --cov-fail-under=<floor> -q`
- الأرضيّة مضبوطة في `.github/workflows/ci.yml` (خطوة *Coverage gate (fail-under)*).
- **مهمّ (تغيّر في 2026-07-28 — `UNIT-TEST-DORMANCY-01`):** وظيفة CI *Unit Tests* لم تعد
  تعمل في «بيئة دنيا بلا fastapi». `tests_v9/requirements-test.txt` صار يحمل
  `fastapi==0.136.3` و`scipy==1.13.1` و`pillow>=10.0.0` — مثبَّتة على إصدارات الإنتاج —
  فاستيقظ **٤٧٩ اختبار وحدة** كانت تُجمَع وتُتخطّى. القياسات أدناه من هذه البيئة،
  وهي المُلزِمة للبوّابة.

## القياس الحاليّ (2026-07-28)

| البيئة | المقيس | الملاحظة |
| --- | --- | --- |
| **بوّابة CI الحاليّة — بعد وسم الخامدة** | **47%** | المُلزِم — 3722 نجح · 26 تخطٍّ · صفر فشل |
| بعد `UNIT-TEST-DORMANCY-01` وقبل الوسم — تاريخيّ | 46% | 3478 نجح · 26 تخطٍّ |
| الدنيا السابقة (بلا fastapi) — تاريخيّ | 44.55% | 2999 نجح · 369 تخطٍّ — لم تعد بيئة البوّابة |

- **الأرضيّة الحاليّة: `--cov-fail-under=43`** (هامش ~٤ نقاط دون 47%، بقاعدة الرفع
  أدناه). تُرك الهامش كي لا يُحمِّر انخفاض صغير البناءَ.
- **مصدر القفزة 46⇒47 (`TESTS-UNMARKED-DESELECTED-01`):** ٦٩ ملفّاً في `tests_v9` كانت
  **بلا علامة**، و`-m` يستبعد ما لا علامة له من **كلّ** وظيفة ⇒ ٦٥ منها لا تعمل في أيّ
  مكان. قياس في venv نظيفة مطابقة لوظيفة *Unit Tests*: ٤١٥ نجح · ١٣ فشل · صفر تخطٍّ.
  وُسِمت ٥٤ ملفّاً خضراء بالكامل (**٢٤٠ اختباراً**)، وبقيت ١٥ في أساس مُجمَّد يفرضه
  `scripts/ci/test_marker_coverage_guard.py` ويتقلّص ولا ينمو.
- **لماذا لم تقفز الأرضيّة أكثر:** إيقاظ الاختبارات يرفع البسط **والمقام معاً** —
  ملفّات fastapi صارت تُجمَع فتدخل عباراتها في الحساب. المكسب الحقيقيّ ليس رقم
  التغطية بل أنّ ٤٧٩ اختباراً صارت تُنفَّذ في بوّابة الدمج بدل أن تُتخطّى.

> ملاحظة: وظيفة CI منفصلة **Platform Unit Tests** تفرض أرضيّة أعلى (60٪) عبر
> `services/sahool-platform/.coveragerc` على حزمتَي `api/`+`core/`. هذه الوثيقة
> وهذه الأرضيّة (40٪) تخصّان بوّابة `--cov=services` الأوسع فقط ولا تمسّان بوّابة 60٪.

## التفصيل حسب النطاق الحرج (بيئة البوّابة، 2026-07-28)

أبرز الوحدات في كلّ نطاق (النسبة = تغطية الوحدة؛ السهم = قبل إيقاظ الخامدة ← بعده):

- **auth / RLS / authz:**
  `services/sahool-platform/core/authorization.py` 76٪ ← **80٪** ·
  `services/auth/mfa_crypto.py` 97٪ · `services/auth/otp.py` 100٪ ·
  `services/auth/main.py` 3٪ ← **50٪** · `auth/routers/*` 0٪ ← **14–62٪**.
- **field-state:**
  `api/field_state_projection.py` 44٪ ← **85٪** ·
  `api/field_state_gateway.py` 29٪ ← **100٪**.
- **raster:**
  `quality_metrics.py` 97٪ · `change_detection.py` 94٪ · `sar_rvi.py` 94٪ ·
  `fvc.py` 89٪ · `job_store.py` 51٪ ← **61٪** · `cdse_client.py` 66٪ ← **56٪** ·
  `band_math.py` 41٪ · `db_persist.py` 17٪ ← **24٪**.
- **orchestration:**
  `core/internal_orchestrator.py` 48٪ ·
  `supervisor-agent/tool_contracts.py` 44٪ ← **53٪** ·
  `supervisor-agent/main.py` 0٪ ← **37٪**.

> **اقرأ الرقم الكلّيّ بحذر:** 44.55٪ → 46٪ تبدو قفزة نقطة ونصف، لكنّ **المقام
> تضاعف ثلاث مرّات** (20655 عبارة ← 60870): وحدات fastapi صارت تُستورَد فتُحصى.
> المكسب ليس في النسبة بل في أنّ تطبيقات fastapi خرجت من الصفر — و٤٧٩ اختباراً
> صارت تُنفَّذ في بوّابة الدمج بدل أن تُجمَع وتُتخطّى.

`cdse_client.py` وحدها **نزلت** (66٪ ← 56٪) للسبب نفسه: أسطر كانت خارج القياس
دخلته. لا انحدار في اختبار — تغيّر مقام.

## الخطّة المرحليّة (staged targets)

| المرحلة | الأرضيّة | الشرط للانتقال |
| --- | --- | --- |
| ✅ 2026-07 | 40 | المقيس الدنيا 44.55٪. |
| ✅ 2026-07-28 | **42** | المقيس 46٪ بعد إيقاظ ٤٧٩ اختباراً (`UNIT-TEST-DORMANCY-01`). |
| التالية | 45 | حين يستقرّ المقيس ≥ ~49٪ (أضِف تغطية منطق field-state/orchestration). |
| ثمّ | 50 | حين ≥ ~54٪. |
| الهدف | 55 | حين ≥ ~59٪ (يستلزم نقل منطق مسارات fastapi إلى وحدات نقيّة). |

**قاعدة الرفع:** بعد أيّ عمل يرفع المقيس الدنيا، اضبط `--cov-fail-under` على
`floor(المقيس) - ~4` في `ci.yml`، وحدّث جدول «القياس الحاليّ» أعلاه. لا تُنزِل
الأرضيّة أبداً. لا تضبطها فوق المقيس الفعليّ للبيئة الدنيا (يُحمِّر CI).
