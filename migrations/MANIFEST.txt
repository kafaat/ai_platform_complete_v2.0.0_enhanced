# دليل ترتيب الـmigrations (MANIFEST)
#
# المشكلة: الترتيب الأبجدي يضع v10/v11/v12 قبل v9 — خطأ (الاعتماديّات تنكسر).
# الحلّ: ترتيب صريح. كل سطر = ملفّ يُطبَّق بالتسلسل. الأسطر الفارغة و# تُتجاهَل.
#
# C2 FIX: أُكملت الملفّات الناقصة (كان MANIFEST يسرد 11 من 18 — أخطرها
# v9_rls_tenant_isolation الغائب ⇒ قاعدة إنتاج بلا عزل مستأجرين).
# ملفّات .down (التراجع) لا تُدرَج هنا — تُطبَّق يدويّاً عند الحاجة فقط.
#
# الترتيب مبنيّ على الاعتماديّات:
#   init_v8         → الأساس (extensions: postgis, uuid-ossp, pgcrypto + جداول core)
#   v9_foundation   → جداول v9 الأساسيّة (مُشار إليها بـFK لاحقاً)
#   v9_*            → إضافات v9 (auth/market/automation/odoo/new_tables/onboarding)
#   v9_rls_*        → عزل المستأجرين (RLS) — بعد إنشاء الجداول، حرج للأمان
#   v9_append_only  → فرض append-only على الأحداث (سلامة البيانات)
#   v10             → command_store + field_lifecycle
#   v11             → events_bus (يعتمد على pgcrypto من init_v8)
#   v12             → trueup + sharing keys
#   v13             → geospatial core

init_v8.sql
v9_foundation.sql
v9_auth_improvements.sql
v9_new_tables.sql
v9_onboarding.sql
v9_market.sql
v9_automation.sql
v9_automation_persistence.sql
v9_odoo_bridge.sql
v9_edge_idempotency.sql
v9_edge_occurred_at.sql
v9_rls_tenant_isolation.sql
# نُقِل v9_append_only_enforcement.sql إلى نهاية الـMANIFEST: كان يُطبَّق هنا
# (موضع ١٤) قبل إنشاء events (v11) وfield_lifecycle_transitions (v10) و
# lifecycle_temporal_rejections (v9_lifecycle_occurred_at)، فحارس IF EXISTS
# يتخطّاها صامتاً ⇒ السجلّ غير المحميّ (UPDATE/DELETE ممكنان). idempotent فالنقل آمن.
v10_command_store_lifecycle.sql
# FIX: v9_lifecycle_occurred_at يُعدّل field_lifecycle_transitions المُنشأ في
# v10 — لذا يجب أن يأتي بعده (كان قبله ⇒ "relation does not exist").
v9_lifecycle_occurred_at.sql
v11_events_bus.sql
v12_trueup_sharing.sql
v13_geospatial_core.sql
# v14: تخزين الصور (raster_assets / management_zones / zonal_stats) — قبل
# v9_rls_force_all ليُفرَض RLS على جداوله الجديدة.
v14_imagery_storage.sql
# FIX (أمان): يُطبَّق أخيراً — يفرض RLS على كلّ الجداول المُفعّلة (كان 7/26 فقط
# تُفرض، فالباقي يتسرّب لمالك الجدول). يحتاج كلّ الجداول منشأة قبله.
v9_rls_force_all.sql
# v15: تخزين عمليّات offline-first المُزامَنة. يُطبّق RLS بنفسه عبر
# _sahool_apply_tenant_rls، فترتيبه بعد force_all آمن.
v15_offline_synced_operations.sql
# v16: حالة الـworkflow متعدّد الخطوات (durable/resumable) لـworkflow_engine.
# يُطبّق RLS بنفسه (ENABLE + سياسة العزل)، فترتيبه بعد force_all آمن.
v16_workflow_state.sql
# v17: أعمدة WorkflowState الناقصة في v16 (للحفظ المعمّر الكامل).
v17_workflow_state_full.sql
# v18: إصلاح بنيويّ — معرّفات الكيانات نصّيّة لا UUID (events.entity_id،
# field_lifecycle.field_id، trueup_calibrations.field_id، allowed_field_ids).
# يعيد تعريف emit_event بتوقيع p_entity_id TEXT (يُسقط القديم — لا overload).
v18_entity_ids_text.sql
# v19: جدول المزارع (Farms) + fields.farm_id — هرميّة المزرعة→الحقل (تدقيق
# التغطية، الطبقة ١). self-applied RLS، فترتيبه بعد force_all آمن.
v19_farms.sql
# v20: جداول الأتمتة (automation_rules + device_commands_log) التي يتوقّعها
# actuator-service ولم تُنشأ قطّ — يُصلح الأتمتة المعطّلة. self-applied RLS.
v20_automation_tables.sql
# v21: أعمدة MFA (TOTP) على users (mfa_secret/mfa_enabled) — مصادقة ثنائيّة.
v21_mfa.sql
# v22: إدارة المخزون (inventory_items + inventory_batches) — الطبقة ١٠ الغائبة.
# self-applied RLS، فترتيبه بعد force_all آمن.
v22_inventory.sql
# v23: إدارة المعدّات (equipment + equipment_maintenance) — الطبقة ١١ الغائبة.
# self-applied RLS.
v23_equipment.sql
# v24: سجلّ أجهزة IoT (iot_devices) + ابتلاع القراءات (device_telemetry) —
# الطبقة ٤ الغائبة. self-applied RLS.
v24_iot_devices.sql
# v25: الري التشغيلي (irrigation_valves + irrigation_schedules) — الطبقة ٣.
# يعتمد على fields + iot_devices. self-applied RLS.
v25_irrigation.sql
# v26: البيانات المرجعيّة (master_data) + الدورات الزراعيّة (crop_rotations).
# يعتمد على fields. self-applied RLS.
v26_master_data.sql
# v27: فرض صحّة الهندسة على القاعدة (trigger يرفض geom غير صالحة) — الطبقة ٢.
# يعتمد على is_valid_field_geom (v13) + field_boundaries (init_v8).
v27_gis_enforce.sql
# v28: الإعدادات (settings) — منصّة/مزرعة/ريّ/إشعارات، مفتاح/قيمة JSONB مُنطّقة
# بـscope. self-applied RLS.
v28_settings.sql
# v29: سجلّ مستندات (documents) — بيانات وصفيّة فقط (لا blob؛ يحتاج S3/MinIO)
# + storage_ref + تحكّم وصول (RLS + RBAC). يعتمد على fields. self-applied RLS.
v29_documents.sql
# v30: عمود geometry (GeoJSON) + idx_fields_tenant لجدول fields — لرسم/عرض
# الحقول المرسومة على الخريطة (POST/GET /api/v1/fields). يعدّل fields (v9). idempotent.
v30_fields_geometry.sql
# v31: عمود manager (المسؤول عن الحقل) لجدول fields — يكمل تدفّق بيانات الحقل.
v31_fields_manager.sql
# v32: المواسم الزراعيّة (seasons) لكلّ حقل — محاصيل/صنف/ريّ/تواريخ/مراحل/حالة.
# يعتمد على fields. self-applied RLS.
v32_seasons.sql
# v33: إثراء الحقول — field_code + water_source + ownership_type + الكشف الآلي
# للدولة (country) + الإقليم/المحافظة (region) من مركز المضلّع. يعدّل fields. idempotent.
v33_fields_extended.sql
# v34: تنظيم المزرعة — حقول إضافيّة على farms (دولة/منطقة/منطقة زمنيّة/وحدات/
# عملة/وصف/نوع نشاط) لشاشة «إنشاء مزرعة» (Sprint 1b). يعدّل farms (v19). idempotent.
v34_farms_org.sql
# v35: العمليّات الزراعيّة (activities) لكلّ حقل — بذر/تسميد/ريّ/رشّ/تقليم/حصاد/كشف.
# يعتمد على fields. self-applied RLS.
v35_activities.sql
# v36: التنبيهات الزراعيّة (alerts) لكلّ مستأجِر — رطوبة منخفضة/أمطار غزيرة/خطر
# مرض/إجهاد حراريّ/خطر صقيع + خطورة/حالة/إقرار. حقل اختياريّ. self-applied RLS.
v36_alerts.sql
# v37: إثراء متقدّم للحقول — كيمياء التربة (pH/EC/OM/NPK) + المناخ الدقيق
# (ارتفاع/ميل/جهة/منطقة مناخيّة/أمطار) + تفاصيل الملكيّة (مالك/إيجار/سجلّ) — تُملأ
# تدريجيّاً عبر لوحة تفاصيل الحقل (GET/PATCH /api/v1/fields/{id}). يعدّل fields. idempotent.
v37_fields_advanced.sql
# v38: قنوات تسليم التنبيهات — توسعة notification_preferences (v9) بقنوات SMS
# وواتساب + user_ref نصّيّ (هويّة المنصّة) + أرضيّة خطورة دنيا، لإعدادات الإشعارات
# (GET/PUT /api/v1/notifications/preferences). يعدّل notification_preferences. idempotent.
v38_notif_channels.sql
# v39: محاكاة الموسم — أعمدة sim_* على seasons (إنتاج/كتلة حيويّة/GDD/LAI/ماء +
# وقت التشغيل) تحفظ ناتج محاكاة محصوليّة RUE/FAO-56 (POST /api/v1/seasons/{id}/
# simulate). كلّها nullable تقديريّة. يعدّل seasons (v32). idempotent.
v39_season_simulation.sql
# v40: تأكيد البريد/الهاتف — أعمدة verified_email/verified_phone على users
# (تحقّق ناعم عبر OTP في Redis). يعدّل users (v9/v21). idempotent.
v40_verification.sql

# v41: حقل — نموذج الريّ/المياه التفصيليّ (irrigation_type/efficiency/flow/pump/
# well_depth/water_ec) + manager_user_id (ربط منطقيّ بمستخدم). أعمدة متقدّمة
# اختياريّة تُملأ تدريجيّاً عبر PATCH. يعدّل fields. idempotent.
v41_fields_irrigation.sql
# v42: موسم — KPIs زراعيّة (target_yield/plant_density/row_spacing/seed_variety_
# source) أساس التحليلات. nullable تُملأ تدريجيّاً. يعدّل seasons. idempotent.
v42_seasons_kpis.sql
# v43: عمود geom (PostGIS) مفهرس GiST على fields — يُسرّع فحص تداخل الحقول
# (ST_Intersects على فهرس بدل مسح خطّيّ). trigger يزامنه من geometry JSONB.
# يعدّل fields. يتطلّب PostGIS. idempotent.
v43_fields_geom_index.sql
# v44: موسم نشط واحد لكلّ حقل — فهرس فريد جزئي على seasons(field_id) حيث
# status='active'. يضمن ثابت «حقل ⇒ موسم نشط واحد على الأكثر» (يكمّله create_season
# بإغلاق الموسم النشط القديم آليّاً). يعدّل seasons (v32). idempotent.
v44_one_active_season.sql
# v45: فهرس season_id على activities + توثيق العلاقة المنطقيّة (الموسم يخصّ نفس
# الحقل). لا FK صلب (قد يفشل على صفوف يتيمة) — فهرس داعم + تحقّق تطبيقيّ في
# create_activity. يعدّل activities (v35). idempotent.
v45_activity_season_fk.sql
# v46: مزامنة دورة الحياة ↔ سجلّ الأحداث — trigger بعد INSERT على
# field_lifecycle_transitions يستدعي emit_event بنوع 'lifecycle.transitioned'،
# فيُغلِق انحراف الحالة (كان السجلّ يخلو من أحداث الانتقال رغم توقّع المستهلكين
# لها). DB-level فيلتقط كلّ مسارات الكتابة. يعتمد على v10 (field_lifecycle*) +
# v11/v18 (emit_event). idempotent (CREATE OR REPLACE + DROP TRIGGER IF EXISTS).
v46_lifecycle_event_sync.sql
# v47: تقوية سلامة الـSchema — FK مركّب tenant-aware (seasons/activities ↔ fields)،
# تفرّد الحقل (field_code/الاسم لكلّ مستأجِر)، قيود تواريخ الموسم، وترقية
# manager_user_id إلى INTEGER + FK users(id). idempotent + NOT VALID/DO-guard.
v47_schema_integrity.sql
# v48: FK صلب Activity→Season (ON DELETE SET NULL، NOT VALID) يُغلق العلاقة على
# مستوى القاعدة؛ + حوكمة الأحداث الفاشلة (DLQ): عرض v_event_dead_letter ودالّتا
# requeue_dead_letter/requeue_all_dead_letter. يعدّل activities + يضيف عرض/دوالّ.
v48_activity_season_fk_dlq.sql
# v49: تفعيل الربط الحيّ لـ#62/#63 — عمود fields.zone_key (مفتاح الإقليم القانوني)
# + جدول recommendation_outcomes (أزواج توقّع/نتيجة + قبول/نضج) مع RLS. idempotent.
v49_zone_key_recommendation_outcomes.sql
# v50: Workflow مخبري للتربة — جدول soil_lab_tests (دورة حياة عيّنة→مختبر→نتيجة→اعتماد→نشر)
# مع RLS وtrigger updated_at. قواعد الانتقال في core/engines/soil_lab_workflow. idempotent.
v50_soil_lab_tests.sql
# v51: إصلاحات المراجعة (جولة ٢) — توسيع events.entity_type (activity/soil_lab_test)،
# RLS على lifecycle_temporal_rejections، وقصر requeue على مستأجِر السياق. idempotent.
v51_review_round2_fixes.sql
# v52: حقول أغرونوميّة للموسم (نمط FieldView) — maturity/tillage_type/
# actual_yield_kg_ha/notes_ar على جدول seasons. كلّها اختياريّة. idempotent.
v52_season_agronomy_fields.sql
# v53: إسقاط الحالة القانونيّة الموحّدة للحقل (Canonical Field State read model) —
# جدول field_state يُعاد حسابه من المصادر القانونيّة ويُقرأ من كلّ المستهلكين. idempotent.
v53_field_state_projection.sql
# v54: قيمة NDVI الحقيقيّة لكلّ حقل (Stage D) — عمودان على imagery_automation_fields
# تقرأهما الحالة القانونيّة. idempotent (ADD COLUMN IF NOT EXISTS).
v54_imagery_ndvi_value.sql
# v55: تخزين الحقائق الزراعيّة الغنيّة (Stage E) — عمود agronomic JSONB على field_state.
v55_field_state_agronomic.sql
# فرض append-only (immutability) — يُطبَّق أخيراً كي توجد events/field_lifecycle_transitions/
# lifecycle_temporal_rejections فعليّاً وقت تركيب trigger الحظر (كان موضعه الأصليّ يسبق
# إنشاءها فيتخطّاها). idempotent (DROP TRIGGER IF EXISTS) — آمن إعادة التطبيق كلّ إقلاع.
v9_append_only_enforcement.sql
# v56: تغطية RLS ديناميكيّة (catch-all) — يُطبَّق أخيراً بعد إنشاء كلّ الجداول/الأعمدة.
# يمسح كلّ جدول public يحوي tenant_id بلا أيّ سياسة RLS ويطبّق العزل عبر
# _sahool_apply_tenant_rls. يقتصر على «صفر سياسات» تجنّباً لتوسعة الوصول بـOR على
# الجداول ذات السياسات المخصّصة. idempotent (الجدول المُغطّى يصبح ذا سياسة فيُتخطّى).
v56_rls_dynamic_all.sql
# v57: تغطية فهارس tenant_id ديناميكيّة — يُطبَّق أخيراً بعد إنشاء كلّ الجداول/
# الأعمدة. يمسح كلّ جدول public يحوي tenant_id بلا فهرس صدارته tenant_id وينشئ
# له idx_<table>_tenant، فيُغلق مسح RLS التسلسليّ. توأم v56 (التغطية الديناميكيّة).
# يكمّل المصفوفة الثابتة في v9_rls_tenant_isolation. idempotent.
v57_rls_dynamic_indexes.sql
# v58: نموذج بيانات حدود الحقل (#15) — أعمدة المصدر/الجودة على field_boundaries
# (confidence_score/source_type/model_version/review_status) + جدول
# field_boundary_graph للعلاقات المكانيّة بين الحقول (الجيران/الحواف المشتركة)
# لتمكين التحليلات الإقليميّة. يطبّق RLS بنفسه عبر _sahool_apply_tenant_rls
# (المعرّفة في v9_rls_tenant_isolation سابقاً)، فترتيبه بعد v56/v57 آمن. idempotent.
v58_field_boundary_quality.sql
# v59: محرّك الطوبولوجيا للحدود (#15) — دالّة sahool_clean_boundary_geom الحتميّة:
# MakeValid + إزالة الرؤوس المكرّرة + استخراج وإبقاء المضلّع الأكبر مساحةً
# (الحقل، إسقاط الشظايا) + تبسيط حافظ للطوبولوجيا. بلا ML — مضلّعات تشغيليّة.
# تقريب المتر→درجة (÷111320) صالح قرب خطوط عرض اليمن. CREATE OR REPLACE فهي
# idempotent. لا تُحدّث بيانات (تُنشئ الدالّة فقط). تعتمد PostGIS من v58 وما قبلها.
v59_boundary_topology_fn.sql
# v60: لقطات حالة الحقل (field_state_snapshots) — مخبّأ مُشتقّ
# لإعادة بناء state الحقل تزايديّاً: لقطة + إعادة تشغيل ما بعد
# مؤشّرها فقط. ليست مصدر حقيقة — مخزن الأحداث append-only يبقى المصدر.
# يطبّق RLS بنفسه عبر _sahool_apply_tenant_rls (v9). idempotent.
v60_event_snapshots.sql
# v61: عمّاد تزامن تفاؤليّ (row_version) على fields — يتزايد كلّ تحديث، يمكّن
# كشف تعارض تعديلات offline (409) عند تمرير العميل base_version. idempotent.
v61_fields_row_version.sql
# v62: حارس سباق دورة حياة الحقل — فهرس فريد جزئيّ على (field_id) حيث
# season_id IS NULL، يسدّ ثغرة NULL≠NULL في UNIQUE(field_id, season_id) فيمنع
# تكرار دورة الحياة قبل-البذر. ينظّف تكرارات سابقة ثمّ يُنشئ الفهرس. idempotent.
v62_field_lifecycle_null_season_guard.sql
# v63: عمود seq (BIGINT IDENTITY) على events — مؤشّر إدراج تسلسليّ صارم يكسر
# تعادل occurred_at حتميّاً (يسدّ Projection Drift الذي اعترف به تعليق v60) +
# فهرسا ترتيب لإعادة البناء ومسار الأمر. append-only فالـseq لا يتغيّر. idempotent.
v63_events_seq_deterministic_order.sql
# v64: توسيع التزامن التفاؤليّ إلى seasons — عمّاد row_version يرفعه trigger
# (bump_row_version) على كلّ UPDATE (يغطّي كلّ مسارات كتابة الموسم: تحديث/محاكاة/
# إغلاق آليّ). update_season يقارن base_version ويردّ 409 عند التعارض. يكمّل v61.
v64_seasons_row_version.sql
# v65: تتبّع سلسلة الإمداد (farm-to-market) — جدولا harvest_lots (دفعة الحصاد،
# تربط field/season) وcustody_chain_events (سلسلة حيازة append-only: حصاد→تخزين→
# نقل→بيع + content_hash للتدقيق). RLS صريح لكلّ مستأجِر (نمط v50). يسدّ الفجوة
# التنافسيّة (xFarm/Farmonaut) فوق input-traceability القائم. idempotent.
v65_harvest_traceability.sql
# v66: سجلّ قرارات الإرسال المحروسة (dispatch_decisions) — سجلّ تدقيق
# decision→execution لكلّ guarded-dispatch (محظور/بانتظار موافقة/جاهز) مع خروقات
# الحظر/التحذير والموافقات، ويعمل كطابور أوامر يستهلكه الـactuator
# (exec_status='queued'). tenant-isolated عبر _sahool_apply_tenant_rls (v9). idempotent.
v66_dispatch_decisions.sql
# v67: تصليب موزِّع القرار — idempotency_key (مفتاح لاتكرار حتميّ) + فهرس فريد جزئيّ
# على الحالات الحيّة (queued/dispatched) يمنع الإطلاق المزدوج، وتوسيع exec_status
# بـdispatched/failed لإغلاق دورة الحياة (مستهلِك+سجلّ). يعدّل dispatch_decisions (v66).
# دورة الحياة محروسة في core/dispatch_lifecycle. idempotent.
v67_dispatch_hardening.sql
# v68: سجلّ التنفيذ append-only (execution_ledger) — يُغلِق الحلقة ويقيسها: لكلّ قرار
# مُسلَّم قيدٌ بنتيجته (executed/failed) + content_hash للتدقيق (مرآة custody_chain v65).
# مرجع متبادَل مع dispatch_decisions (decision_id) وfarm_ledger الماليّ. tenant-isolated
# عبر _sahool_apply_tenant_rls (v9). append-only. idempotent.
v68_execution_ledger.sql
# v69: سجلّ سياسات القرار (decision_policies) — حوكمة معلنة قابلة للتهيئة لكلّ مستأجِر:
# لكلّ سياسة نطاق (scope) وأثر (effect: auto_block/require_approvals/water_cap_pct)
# وأولويّة، تُستشار في core/policy_registry ضمن مسار القرار الموحّد والموزِّع. تكمّل
# الحواجز لا تستبدلها. tenant-isolated عبر _sahool_apply_tenant_rls (v9). idempotent.
v69_decision_policies.sql
# v70: عزل كتابة المستأجرين (WITH CHECK) — دفاع عمق على RLS. v9 صار يعرّف السياسة
# بـWITH CHECK (يمنع INSERT/UPDATE عابر المستأجرين تحت سياق مستأجِر، ويُبقي كتابة
# النظام/الهجرات بلا سياق). هذا الملفّ يَنشُر التغيير: يُعيد تطبيق _sahool_apply_tenant_rls
# على كلّ جدول يحمل سياسة tenant_isolation (حلقة v56 لا تلمس المُمَأسَسة). idempotent.
v70_rls_with_check_propagate.sql

# v71: MEDIUM-009 (تدقيق) — RLS على field_lifecycle_transitions (عزل عبر الأب
# field_lifecycle بالمفتاح الأجنبيّ). weather_automation_locations يبقى عالميّاً
# عمداً (مجدوِل عابر للمستأجرين — يُعالَج بدور خدميّ، لا RLS). idempotent.
v71_rls_missing_tables.sql

# v72: HIGH-002 (شهادة) — RLS على event_outbox (عزل عبر FK للأب events). يُمكَّن لأنّ
# المرسِل صار على دور sahool_jobs (BYPASSRLS) عبر JOBS_DATABASE_URL. idempotent.
v72_event_outbox_rls.sql

# v73: HIGH-002 (شهادة) — RLS على weather_automation_locations (عزل عبر field_id→fields،
# NULL=عالميّ) وweather_automation_cache (يرث رؤية موقعه). يُمكَّن لأنّ مجدوِل الطقس صار
# على دور sahool_jobs (BYPASSRLS). idempotent.
v73_weather_automation_rls.sql

# v74: طبقة ذكاء الطقس — جداول التراكب/الإشارات/التنبيهات (field_id TEXT لا UUID،
# RLS+FORCE بسياسة current_setting، PK مع tenant_id، قيود الدرجات). weather_grid/
# weather_forecasts عالميّة بلا tenant (موثّق). idempotent.
v74_weather_intelligence.sql

# v75: FOES (نظام تنفيذ العمليّات الزراعيّة) — جدول work_orders: أمر عمل من توصية
# (ري/تسميد/رشّ/فحص/حصاد) بآلة حالات (planned→…→verified). field_id نصّ، RLS+FORCE.
v75_work_orders.sql

# v76: تخزين Kc الدائم — جدول crop_kc_timeseries: معاملات Kc الثلاثيّة المُشتقّة
# (FAO-56) لكلّ حقل×محصول×موسم×سيناريو، للمقارنة التاريخيّة. field_id نصّ، RLS+FORCE
# بسياسة current_setting، UNIQUE(tenant,field,crop,season,scenario) للـupsert. idempotent.
v76_crop_kc_timeseries.sql

# v77: تخزين + تدقيق التوصية — جدول recommendations: يحفظ كلّ توصية مُولَّدة مع شرحها
# (provenance + cross_reference)، PK بديل UUID + rec_id نصّ مفهرس، field_id نصّ،
# RLS+FORCE بسياسة current_setting. يُصدِر RECOMMENDATION_CREATED عبر outbox. idempotent.
v77_recommendations.sql

# v78: إدامة رأس القرار — جدول decision_record: يُدِيم كلّ قرار مُتّخذ (decision_id،
# نوعه، حقله، قيمته الكاملة JSONB، ثقته) كرأس سلسلة النَّسَب (Decision→Outcome→Evidence).
# يُصدِر DECISION_RECORDED عبر outbox. RLS إلزاميّ معزول بالمستأجر. idempotent.
v78_decision_record.sql

# v79: إدامة نتيجة القرار — جدول outcome_record: يُدِيم قياس النتيجة (مُخطَّط/مرصود/مقاييس)
# مربوطاً بـdecision_id (ربط ليّن مفهرس بلا FK صلب، يحفظ RLS) — يُغلق حلقة Decision→Outcome
# (P0-1). يُصدِر OUTCOME_MEASURED عبر outbox. RLS إلزاميّ معزول بالمستأجر. idempotent.
v79_outcome_record.sql

# v80: معايرة إقليميّة مُدارة DB-backed — جدول calibration_override: يُدِيم قيم المعايرة
# المُتحقَّقة لكلّ مستأجِر×منطقة (UNIQUE للـupsert، override_values JSONB، مصدر provenance
# إلزاميّ) بدل تعديل _REGION_OVERRIDES في الكود — عكوسيّ (DELETE يعيد الوراثة). يُصدِر
# CALIBRATION_OVERRIDE_SET عبر outbox. RLS+FORCE معزول بالمستأجر. idempotent.
v80_calibration_override.sql

# v81: مخزن إزالة التكرار العنقوديّ للـactuator — جدول actuator_command_dedup: يجعل dedup
# الأوامر cluster-safe (فحص-وتثبيت ذرّيّ عبر القاعدة بدل dict محلّيّ per-replica) ⇒ يمنع
# التنفيذ المزدوج على المضخّات/الصمّامات. يُستهلَك خلف علم ACTUATOR_IDEMPOTENCY_MODE
# (local→shadow→cluster، إغلاق مرن). RLS+FORCE معزول بالمستأجر. idempotent.
v81_actuator_command_dedup.sql

# v82: توحيد نَسَب التنفيذ (PR #396) — جدول lineage_link: جسر ربط يُضيف معرّفاً عالميّاً
# lin_ **فوق** المعرّفات القائمة (dec_/disp_/command_id) دون إعادة تسمية — صفّ لكلّ
# (lineage_id↔مرجع) يجمع decision/dispatch/command/execution/outcome في سلسلة واحدة.
# UNIQUE(tenant,ref_type,ref_id) للربط مرّةً، فهرس (tenant,lineage_id) للجلب. يُستهلَك خلف
# علم FEATURE_UNIFIED_LINEAGE (إغلاق مرن). يُصدِر LINEAGE_LINKED. RLS+FORCE معزول. idempotent.
v82_lineage_link.sql

# v83: إيصالات تسليم الإشعار — جدول notification_delivery: يُدِيم حالة كلّ تسليم
# (queued|sent|failed|delivered) لكلّ مستأجِر×تنبيه×قناة (UNIQUE(tenant,alert_key,channel)
# للـupsert، error provenance) — يُغلق دورة الإشعار (تتبّع «هل وصل؟» + إعادة محاولة + تدقيق).
# تُحدِّثه نقطة POST في موجِّه notifications. يُصدِر NOTIFICATION_DELIVERED. RLS+FORCE معزول. idempotent.
v83_notification_delivery.sql

# v84: سجلّ تدقيق المعايرة — جدول calibration_audit append-only (لا UPDATE/DELETE): يُدوّن كلّ
# تغيير على calibration_override (override_set/reverted/adaptation_applied) مع old_values/new_values
# JSONB + actor + source_ar، فيصبح «من غيّر المعايرة ومتى وما القيم قبل/بعد» متتبَّعاً ومدقَّقاً.
# تُدرِجه نقاط المعايرة الثلاث (best-effort داخل savepoint). يُصدِر CALIBRATION_AUDIT_RECORDED.
# فهرس (tenant,region,created_at DESC). RLS+FORCE معزول. idempotent.
v84_calibration_audit.sql

# v85: سجلّ تدقيق NL-GIS — جدول nl_gis_audit append-only (لا UPDATE/DELETE): يُدوّن كلّ استعلام
# لُغة-طبيعيّة قراءة-فقط (النصّ الخام/النيّة المُصنَّفة/الخانات/المصدر المُستدعى/حالة النتيجة).
# read_only=TRUE دائماً. الخانات من النصّ فقط (لا تلفيق). تُدرِجه نقطة /api/v1/nl-gis/query
# (best-effort داخل savepoint). فهرس (tenant,created_at DESC). RLS+FORCE معزول. idempotent.
v85_nl_gis_audit.sql

# v86: trigger تجميع الدليل التراكمي — جدول calibration_evidence يُحدَّثه trigger على
# outcome_record (AFTER INSERT): يُدِيم sample_count/success_count/success_rate/
# evidence_level مُسبَقاً ⇒ GET /evidence قراءة O(1) بدل إعادة الحساب O(n). RLS+FORCE،
# فهرس فريد تعبيريّ COALESCE(crop_type). idempotent. (دُمِج من مرفق مع تصحيحَي صحّة:
# فهرس فريد بدل قيد UNIQUE تعبيريّ، والنجاح من عمود success القانونيّ.)
v86_calibration_evidence_trigger.sql
v87_audit_log_tenant.sql
