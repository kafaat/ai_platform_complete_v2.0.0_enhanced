# 🚧 سجلّ الفجوات الحيّ (Gap Registry)

> سجلّ حيّ بالحالة. كلّ صفّ يحوي **مصدراً** (`file:line` أو `#PR`) و**حالة**
> (`open` / `fixed` / `verified`). المصدر الأساسيّ:
> [`../../SAHOOL_PRODUCTION_GAP_REPORT_v1.md`](../../SAHOOL_PRODUCTION_GAP_REPORT_v1.md) (تحليل ساكن
> READ-ONLY — «مؤشّر ≠ إثبات»: `fixed` = عُولِج في الكود؛ `verified` = أُكِّد حيّاً).
>
> **تنبيه دقّة:** تقرير الفجوات سجّل بعض البنود مُصلَحةً في الكود (#349–#363) لكنّها لم تُؤكَّد
> حيّاً بعد؛ نُبقيها `fixed` (لا `verified`) التزاماً بحدّ الصدق.

| ID | العنوان | المجال/الخدمة | المصدر | الحالة |
|---|---|---|---|---|
| C1/C2 | التوصية تُولَّد + تُخزَّن وتُدقَّق وتُربَط بالشرح (جدول v77 + `RECOMMENDATION_CREATED` + `GET /{rec_id}`) | platform/التوصيات | `SAHOOL_PRODUCTION_GAP_REPORT_v1.md:19` (#350)؛ أصل: `:116-117` | fixed (يحتاج تأكيداً حيّاً) |
| C5 | NDVI الحقيقيّ معلوماتيّ لا يُغيّر صلاحيّة القرار | platform/الحالة القانونيّة | `api/field_state_projection.py` (`_apply_ndvi_threshold_gating`)؛ فرع `claude/c5-ndvi-threshold-flag` | **closed (implemented, gated, calibration absent)** — علم `APPLY_NDVI_THRESHOLDS` (default off) يُعلن `insufficient_field_calibration` صراحةً؛ لا عتبات معايَرة ⇒ لا اختلاق. تفعيل فعليّ يحتاج معايرة ميدانيّة (بطاقة محصول). |
| H2 | **٧** اشتراكات NATS بلا ناشر (لا ٨) — تصنيفها «ناشر مفقود متوقَّع» لا «اشتراك ميّت»: تطابق `EVENT_EMOJI` وأنواع الأحداث (`api/main.py:1670`)، فالإصلاح = بناء ناشرين عبر outbox لا تقليم الاشتراكات (قرار معماريّ). الموضوعان `satellite.*.computed`/`sahool.events.>` لهما ناشرون (ليسا يتيمين). `weather.forecast.updated` مُعالَج خلف راية `WEATHER_GRID_PIPELINE_ENABLED` (OFF). | notification/الأحداث | فرع `claude/h2-nats-publishers-flag`؛ `api/event_bus.py` (`nats_publishers_enabled`) · `api/main.py:_start_outbox_worker`؛ `agents/notification/agent.py:340-346` | **closed (implemented, gated, env-unverified ON)** — علم `FEATURE_NATS_PUBLISHERS` (default off) يحرس تشغيل OutboxWorker؛ OFF ⇒ `record_decision_only` (الأحداث في outbox، لا تُفقَد) + إعلان السجلّ؛ ON ⇒ `publish_event`. ON يحتاج NATS حيّاً (env-unverified). الاشتراكات الخدميّة الـ8 تبقى يتيمة (خدماتها غير مُسلَّمة). |
| H4 | ET0 Hargreaves مُكرَّر بقيم Ra متعارضة — وُحِّد في `core/engines/et0.py` | platform/الأغرونوميا الكمّيّة | `SAHOOL_PRODUCTION_GAP_REPORT_v1.md:23` (#351/#356)؛ تأكيد #457 | ✅ fixed + مؤكَّد باختبارات انحدار (#457؛ متبقٍّ موثَّق: إعادتان عبر-خدمات weather_server/wofost) |
| H5 | احتياج الريّ بصيغتين (مع/بلا ملوحة) — **وُحِّد (#464)** ثمّ **رُبِط بالحالة الكنسيّة (sweep):** قرار المستخدم «بلا ملوحة افتراضيّاً، قابلة للإدخال». الملوحة **مفتاح اختياريّ off-افتراضيّ** في `compute_irrigation`/`water_balance`، تُفعَّل عبر `salinity_decision` عند تحليل موثوق. **الربط الكنسيّ:** `_apply_canonical_salinity` يحقن كتلة `water.salinity` خلف `FEATURE_CANONICAL_SALINITY` (default OFF) — OFF تُعلِن التعطيل، ON+موثوق ⇒ Ks فعليّة (إعادة استخدام `salinity_decision`/`salinity_stress_ks`، لا تكرار). | platform/الريّ | #464؛ فرع `claude/actionable-gaps-sweep`؛ `api/field_state_projection.py` (`_apply_canonical_salinity`) · `core/salinity_policy.py` · `core/engines/fao56.py`؛ أصل: `SAHOOL_PRODUCTION_GAP_REPORT_v1.md:38,131` | ✅ fixed (موحّد + سياسة تفعيل + **ربط كنسيّ off-default**؛ ECw/الغسيل غير مُدخَل ⇒ معلَن None) |
| NATS-DEADEND | ناشر `sahool.weather.field.overlay.completed` بلا مشترِك (وجده المفتّش) | notification/weather-grid | فرع `claude/actionable-gaps-sweep`؛ `services/weather-polygon-worker/src/main.py:160` | **declared/resolved for inspector (سقالة مقصودة، لا عطل)** — المسار محروس بـ`WEATHER_GRID_PIPELINE_ENABLED` (OFF)؛ النشر لمشترِك مستقبليّ. عولج ضجيج المفتّش بإدراجه كـdeclared future subject موثّق، لا بمستهلك وهمي. قرار H2: لا مستهلك مُلفَّق. لا تغيير سلوك تشغيل. |
| H4-residual | إعادتان عبر-خدمات لـRa/ET0 (weather_server MCP · wofost R&D) | platform/الأغرونوميا الكمّيّة | فرع `claude/actionable-gaps-sweep`؛ `services/mcp_servers/weather_server.py` · `wofost_real/wofost_engine.py`؛ مصدر كنسيّ `core/engines/et0.py` | **declared (٣/٥ موحّدة؛ المتبقّيان محجوبان بعزل خدمات مقصود)** — مرجع توثيقيّ لمصدر الحقيقة الكنسيّ؛ التوحيد الكامل يحتاج نقل `core→shared` (خارج النطاق). لا تغيير حسابيّ. |
| INSPECT-CI | لا بوّابة CI ساكنة تثبّت ضمانات RLS/router/migration | platform/CI | فرع `claude/actionable-gaps-sweep`؛ `.github/workflows/ci.yml` (`Platform Structure Inspector`) · `tools/sahool_inspector.py` | **fixed** — وظيفة CI ساكنة تشغّل المفتّش (exit 1 على FAIL فقط)؛ تثبّت RLS (22/0)·routers (142/0)·migrations (105/105) ⇒ تأكيد ساكن مستمرّ لـC1/C2/H6. لا WARN بعد تثبيت 2026-06-24 (ل): NATS future subject مُعلن، ونقاط reference العامة لا تُصعَّد لأنها لا تمس DB/tenant. |
| H6 | عتبات الملوحة/pH/الحرارة مُكرَّرة — وُحِّدت في `core/thresholds.py` | platform/العتبات | `SAHOOL_PRODUCTION_GAP_REPORT_v1.md:21` (#352)؛ أصل: `:132` | fixed (يحتاج تأكيداً حيّاً) |
| C4/M1 | الموبايل: بنية push (FCM/APNs) + عميل WebSocket | mobile/الإشعارات | فرع `claude/c4-m1-mobile-push-flag`؛ `agents/notification/agent.py` (`push_decision`/`_record_push_fallback`) | **closed (implemented, gated, env-unverified ON)** — backend: علم `FEATURE_MOBILE_PUSH` (default off) + سجلّ احتياطيّ دائم `notification_delivery` بدل الإسقاط الصامت (`create_notification_record`). ON يحتاج FCM حيّاً + tokens (env-unverified). WebSocket منفَّذ أصلاً في الوكيل. عميل Flutter UI يبقى منفصلاً. |
| SAM2 | خادم استدلال SAM2 يحتاج GPU (opt-in)؛ بدونه 503 صادق | field-segmentation/sam2 | `docker-compose.v9.yml:1351` (profile=gpu)؛ `services/sam2-inference/main.py:74`؛ [`docs/SAM2_DEPLOYMENT.md`](../../docs/SAM2_DEPLOYMENT.md) | **implemented-gated-but-env-unverified** (محروس بـprofile=gpu؛ يحتاج GPU حيّاً — لا production-ready) — انظر [`../decisions/bundle-c-status.md`](../decisions/bundle-c-status.md) |
| C-Embeddings/RAG | Field Embeddings / RAG | R&D/knowledge | `services/qdrant-seed/` · `services/local-ai-rag/` · `api/decision_explainer.py:38`؛ [`../decisions/bundle-c-status.md`](../decisions/bundle-c-status.md) | **closed (implemented-as-optional-services, deployment opt-in)** — RAG لا يدخل مسار قرار إنتاجيّ بلا حراسة (مُتحقَّق)؛ إثراء شرح opt-in. لا حاجة `FEATURE_CONSERVATIVE_RAG`. |
| C-Ensemble | Multi-engine Ensemble | R&D | `core/engines/fusion.py` (fusion مؤشّرات، شيء مختلف)؛ [`../decisions/bundle-c-status.md`](../decisions/bundle-c-status.md) | **open (concept-only / not implemented)** — تصويت محرّكات متعدّدة غير منفَّذ؛ fusion.py للمؤشّرات الطيفيّة ليس هو. |
| C-Foundation | نماذج أساس (Prithvi/DINOv3) | R&D | لا كود؛ [`../decisions/bundle-c-status.md`](../decisions/bundle-c-status.md) | **not started** (رهان R&D مقترح — لا استيراد/أوزان/استدلال) |
| C-ISOXML | Machine Integration (ISOXML) | precision-ag | [`../decisions/strategy.md`](../decisions/strategy.md) (أُوقِف بصدق)؛ بديل `api/prescription_shapefile.py` (#456) | **deferred by design (#456)** (مُلاءمة اليمن — Shapefile كافٍ، لا John Deere/Trimble) |
| MAP-QA | افتراض MapLibre/WebGL ينتظر بوّابة QA حيّة (Playwright) | frontend/الخرائط | بوّابة Playwright #441 (`d23eb6b`)؛ سويت [`tests/`](../../frontend/) | **verified 2026-06-25** — 10 passed / 3 skipped (@visual صرف). إصلاح race condition Terra Draw: `data-draw-ready` + `waitForSelector` في الاختبار. |
| TERRAIN | `TerrainView3D` يحتاج مسار `/terrain` خادميّ | frontend/الخرائط | [`frontend/src/components/maphub/TerrainView3D.tsx`](../../frontend/src/components/maphub/TerrainView3D.tsx) | open (P2) |
| IND-SRC | مصدر مؤشّرات الموبايل الصحيح (`getFieldIndicators`) | mobile | #445 (`a7909e6`)؛ [`mobile/sahool_app/lib/services/api_service.dart`](../../mobile/sahool_app/lib/services/api_service.dart) | fixed |
| MERGE | دمج/انقسام الحقول ذرّيّاً (سدّ خطر البيانات الثلاثيّة) | platform/الحقول | #443 (`2456d2b`)؛ [`api/routers/fields.py`](../../services/sahool-platform/api/routers/fields.py)؛ اختبار [`tests_v9/test_fields_merge_split_atomic.py`](../../tests_v9/test_fields_merge_split_atomic.py) | fixed |
| NDVI-MOB | مسار سلسلة NDVI في الموبايل (404) | mobile | #444 (`9e00d0a`)؛ [`mobile/sahool_app/lib/screens/satellite_screen.dart`](../../mobile/sahool_app/lib/screens/satellite_screen.dart) | fixed |
| WUE | لا كفاءة استخدام مياه مُجمَّعة على فترة (الدفتر يوميّ فقط) | platform/المياه | #477 (`f01c468`)؛ `api/water_efficiency.py` + `routers/water_ledger.py` (`GET …/water-efficiency`) | **fixed (water-balance)** — WUE من التوازن المائيّ (ETc مقابل المُورَّد) + over_application؛ الغلّة خارج النطاق (لا حلقة)؛ needs_data/needs_irrigation_data عند النقص. |
| DISPATCH-BRIDGE | الحلقة المغلقة (قرار→تنفيذ فيزيائيّ) — لا جسر يستهلك `dispatch_decisions[queued]` ⇒ MQTT | actuator/الحلقة المغلقة | فرع `claude/dispatch-actuator-bridge`؛ `services/actuator-service/main.py` (`dispatch_consumer_loop`/`_dispatch_one`/`_device_belongs_to_tenant`) | **fixed (simulation-first, double-gated)** — الحلقة كانت مغلقة عبر الإنسان (إخطار)؛ الجسر **اختياريّ** خلف `FEATURE_DISPATCH_ACTUATOR` (default OFF) **+** `ACTUATOR_MODE=real` (وإلّا محاكاة). HIGH/CRITICAL/NULL لا تُؤتمت؛ حارس عزل جهاز (fail-closed)؛ مطالبة ذرّيّة (لا إطلاق مزدوج)؛ «نُشِر≠نُفِّذ». مراجعة عدائيّة: ٥ ادّعاءات تصمد. حدود مُعلَنة: تعافي `dispatched` العالق (لا TTL). |
| ACTUATOR-SAFETY | الافتراضيّ غير الآمن: `ACTUATOR_MODE` يُرجّح `real` عند عدم ضبطه (مسارات automation/manual غير محروسة بعلم) | actuator/السلامة الفيزيائيّة | فرع `claude/actuator-safety-hardening`؛ `shared/actuator_mode.py` · `services/actuator-service/main.py` (أعلام per-path + `/safety-status`) | **fixed (implemented-gated-fail-safe)** — `resolve_actuator_mode` الافتراضيّ صار `simulation` (real يتطلّب opt-in صريحاً)؛ `send_mqtt_command` نقطة اختناق واحدة + دفاع بالعمق: `FEATURE_AUTOMATION_RULES_ACTUATION`/`FEATURE_MANUAL_ACTUATOR_COMMANDS`/`FEATURE_DISPATCH_ACTUATOR` كلّها default-OFF. `/command`→403 صريح؛ `/safety-status` (لا أسرار)؛ تحذير إقلاع. كسر واعٍ (يعكس PR #394). مراجعة عدائيّة: ٧ ادّعاءات تصمد، لا ثغرة (شامل `_compensate`). |
| SUSTAIN | لا درجة استدامة مُفسَّرة لكلّ حقل (إشارات تربة/مياه مبعثرة) | platform/الاستدامة | فرع `claude/field-sustainability-index`؛ `api/field_sustainability.py` + `routers/reports.py` (`GET …/sustainability`) | **fixed (reuse-only)** — تجميع تربة+مياه يُعيد استخدام `salinity_class`/`water_stress_class` الكنسيّين (لا حساب)؛ المغذّيات `needs_data` بصدق (NPK غير مقيس)؛ بلا كربون؛ بُعد غائب يُستبعَد + إعادة تسوية. قراءة فقط، لا تغيير قرار. |

## فجوات جلسة 2026-06-27 (تكامل الأرشيف)

| ID | العنوان | المجال/الخدمة | المصدر | الحالة |
|---|---|---|---|---|
| RLS-WITHCHECK | سياسات كتابة tenant بـUSING-only (مسار الكتابة العابر للمستأجر غير محروس) | platform/الأمن | #487 (`24e7923`)؛ `migrations/v122_rls_with_check_session_unification.sql` · `scripts/security/validate_rls_write_policies.py` | **fixed** — backfill WITH CHECK مدفوع بالكتالوج + `sahool_effective_tenant_id()`؛ بوّابة Security Scan تحجب أيّ ترحيل لاحق يُرجِع سياسة كتابة بـUSING-only أو app.tenant_id دون app.current_tenant. |
| MAIN-DECOMP | `main.py` يتضخّم (~١٤٢ تضمين راوتر يدويّ) ⇒ دَيْن تقنيّ | platform/البنية | #491 (`2198525`)؛ `api/main.py` (حلقة `pkgutil.iter_modules`) · `test_router_decomposition_guard.py` | **fixed** — تسجيل تلقائيّ لكلّ راوتر في `api/routers/`؛ `service_proxy`/`gis_cloud_native` يُستورَدان متأخّراً (دورة/بادئة). حارس + مفتّش يتحقّقان عبر `app.routes`. |
| PHASE9-12-AUTH | ٦٤ نقطة POST للمراحل 9-12 (autonomy/federation/learning/marketplace) بلا أيّ مصادقة (جلبها الأرشيف) | platform/الأمن | #497 (`e09ce27`)؛ `api/service_token_auth.py` · `api/phase{9,10,11,12}_*.py` (`dependencies=[Depends(_require_service_token)]`) · `tests_v9/test_endpoint_auth_coverage.py` | **fixed** — حارس توكن خدمة على مستوى الراوتر (خدمة-لخدمة) عبر وحدة مستقلّة تتفادى دورة استيراد api.main؛ عُلِّم حارس تغطية المصادقة اكتشاف تبعيّات مستوى-الراوتر. يحلّ `test_mutating_endpoints_authn_guard`. |
| COMPAT-GW | نقاط توافقيّة `@app` في main.py تكسر حارس تفكيك الراوترات | platform/البنية | #497؛ `api/routers/compat_gateway.py` · `tests_v9/test_endpoint_auth_coverage.py` (PUBLIC_ALLOWLIST) | **fixed** — نُقلت ٧ نقاط (مسابر صحّة عامّة + تمرير vegetation/raster يُفوّض المصادقة) إلى راوتر مستقلّ + أُدرِجت في القائمة العامّة بتبرير. |
| ARCHIVE-INTEGR | تكامل لقطة الأرشيف الكامل (٤ مراحل / ٥ طلبات حسب المجال) | كامل المنصّة | #488-#497 (`6ce8849`…`e09ce27`) | **fixed + CI-verified** — كلّ بوّابات CI الـ١١ + سير production-gates الـ٨ خضراء عند الدمج النهائيّ. حدود مُعلَنة: أُسقط اختبارا raster-internal متعارضان مع راستر #484؛ سير production-gates جديد على الريبو. |

## فجوات جلسة 2026-06-27 (ب) — تصلّب + اعتماد

| ID | العنوان | المجال/الخدمة | المصدر | الحالة |
|---|---|---|---|---|
| RLS-V123 | نمط v122 يستبدل USING (footgun) — قد يُسقِط شروط سياسة مركّبة بلا WITH CHECK | platform/الأمن | #499 (`be1bd56`)؛ `migrations/v123_rls_with_check_preserve_using.sql` · `tests/security/test_phase23_rls_preserve_using.py` | **fixed (preventive)** — v123 خَلَف حافظ لـUSING (idempotent/fail-closed)؛ بلا انحدار فعليّ (user_self محميّة، الباقي tenant صرف). |
| ROUTER-REGISTRY | main.py مركز تسجيل الراوترات (دَيْن تقنيّ) | platform/البنية | #501 (`e230ecb`)؛ `api/router_registry.py` · `tests/test_router_registry_contract.py` | **fixed (extraction-only)** — `register_routers(app)` مُستخرَجة؛ main بلا include مباشر؛ router wiring 146/0؛ بلا نقل router_groups فيزيائيّ (مؤجَّل C2-C5). |
| CLAUDE-LOCAL | `.claude/settings.local.json` متعقَّب في git (ملفّ محلّيّ) | repo/نظافة | #500 (`488a7c7`)؛ `.gitignore` | **fixed** — أُزيل من التعقّب + .gitignore (الباني يستثني .claude أصلاً). |
| PROD-CERT | لا اعتماد إنتاجيّ موثَّق بالأدلّة (static مقابل runtime/soak) | platform/الاعتماد | #502 (`95dc750`)؛ `FINAL_PRODUCTION_READINESS_REPORT.md` · `PRODUCTION_CERTIFICATION_MATRIX.md` | **partial (GO ساكن/CI · NO-GO إنتاجيّ معلّق)** — smoke/env_doctor/soak على بيئة حيّة لم تُنفَّذ (مالك Ops/SRE)؛ مُعلَّمة PENDING بصدق لا تلفيق. |

## ملاحظات

- **مصادر [حيّ] تنتظر التشغيل:** R6 (البوّابيّ)، H1 (التفويض فاشل-مفتوح، قرار)، OFFLINE
  (مزامنة Flutter كاملة) — انظر ملحق التحقّق المعماريّ في
  [`../../SAHOOL_PRODUCTION_GAP_REPORT_v1.md`](../../SAHOOL_PRODUCTION_GAP_REPORT_v1.md):44-74.
- **الأداة القابلة للتشغيل:** [`../../tools/sahool_inspector.py`](../../tools/sahool_inspector.py)
  (RLS coverage / router wiring / NATS subjects / endpoint authz / migration manifest).
- **تأكيد حيّ 2026-06-25:** `POST /api/v1/fields` → 201 Created ✅؛ `GET /api/v1/fields` → 200 OK ✅ — بعد إضافة `planting_date` (v103) وإصلاح asyncpg.
