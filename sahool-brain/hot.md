# 🔥 التركيز الحاليّ (Hot)

> **آخر تحديث:** 2026-06-28 · رأس `main`: [`51d650c`](../) (#551 مُدمج) · انظر [`log.md`](log.md) مدخل (ي).
> ⚠ **تنبيه:** `main` أُعيد ضبطه بدفع مباشر من المالك أثناء الجلسة فمُحيت ٦ PRs مدموجة؛ أُعيد جوهرها
> عبر #550/#551 مع حفظ مساري CDSE. **لا تبنِ على `main` أثناء دفع مباشر متزامن.**

## عمل هذه الجلسة (raster: #550 → #554)

سلسلة تحصين + تفكيك لخدمة الراستر على رأس `main` الحاليّ (التفاصيل + الأسباب في
[`log.md`](log.md) مدخل (ي) و[`decisions/ledger.md`](decisions/ledger.md)):

- **#550 (`2359cea`)** — استرجاع تحصينات raster: قناع `cog_writer` (إصلاح **جذر** الشرائط الداكنة من
  المصدر) + تعقيم `str(e)` + `cloud_pct`/قناع SCL في CDSE + سقالة `register_routers` — فرع واحد مدمج.
- **#551 (`51d650c`)** — تفكيك مسارات raster: ٤٥ `@app` → **١٠ وحدات `routers/`** (محفوظ السلوك، ٤٩
  مساراً ثابتة، CDSE محفوظة)؛ `main.py` ٣٠٠٥→١٦٢٥ سطراً.
- **قيد المراجعة:** #552 (واجهة CDSE — حذف `date=latest`) · #553 (nginx `/api/raster/` للواجهة 3003)
  · #554 (وثيقة مقارنة `v9↔fixed`).
- **صدق:** ادّعاء IDOR رُفِض (المسار يرفع 503 fail-closed أصلاً)؛ قناع CDSE SCL يحتاج تحقّقاً ميدانيّاً.

## أعلى الفجوات المفتوحة الآن

(السجلّ الكامل + المصادر في [`gaps/registry.md`](gaps/registry.md))

| ID | العنوان | الحالة |
|---|---|---|
| C1/C2 | التوصية تُولَّد بلا تخزين/تدقيق كامل لربط الشرح بـ`rec_id` | open (جزئيّ — v77 موجود) |
| C5 | NDVI الحقيقيّ معلوماتيّ لا يُغيّر صلاحيّة القرار | open (معماريّ — يحتاج تحقّقاً ميدانيّاً) |
| H2 | اشتراكات NATS يتيمة (بلا ناشر) | open |
| C4/M1 | الموبايل: بنية push/FCM + عميل WebSocket | open (يحتاج بيئة Flutter) |
| SAM2 | خادم الاستدلال يحتاج GPU (opt-in)، 503 صادق بدونه | open (تشغيليّ) |
| CI-MIRROR | `ci.yml` فقد مرآة `mirror.gcr.io` ⇒ رفرفة Docker Hub تُعطّل Integration Tests | open (يُعاد) |
| CDSE-SCL | قناع غيوم SCL بكسليّ في CDSE | fixed (يحتاج تحقّقاً ميدانيّاً) |
| — | TerrainView3D `/terrain` (مسار خادميّ) | open (P2) |

## ماذا بعد؟

- **عاجل:** إعادة مرآة `mirror.gcr.io` إلى `ci.yml` (CI-MIRROR) — توقف رفرفة Docker Hub.
- دمج #552/#553/#554 عند الاخضرار + تحقّق ميدانيّ لقناع CDSE SCL.
- ربط/تقليم اشتراكات NATS اليتيمة (H2/M5).
- توحيد ET0/الريّ (H4/H5) — قرار أغرونوميّ.
- بنية push للموبايل (C4/M1).
