# 🔥 التركيز الحاليّ (Hot)

> **آخر تحديث:** 2026-06-28 · رأس `main`: [`7a36511`](../) (#563 مُدمج) · انظر [`log.md`](log.md) مدخل (ك).
> ⚠ **درس تشغيليّ سابق:** `main` أُعيد ضبطه بدفع مباشر من المالك (محا #544–#549)؛ أُعيد جوهرها عبر
> #550/#551. **لا تبنِ على `main` أثناء دفع مباشر متزامن؛ وحّد الاسترجاع في فرع واحد سريع الدمج.**

## عمل هذه الجلسة (إكمال raster/CDSE + تفكيك ٤ خدمات + MapHub/WS)

التفاصيل + الأسباب في [`log.md`](log.md) مدخل (ك) و[`decisions/ledger.md`](decisions/ledger.md):

- **مدموج (#552–#563، ١٢ PR):**
  - إكمال raster: واجهة CDSE (#552) · nginx `/api/raster/` بلا `auth_request` (#553) · وثيقة `v9↔fixed` (#554) · العقل (#555) · **إعادة مرآة `mirror.gcr.io`** (#556) · **تفكيك `auth`** ٢٧→٩ (#557).
  - **قصّ CDSE على المضلّع** (#558) + **تطبيع تاريخ CDSE** الفارغ (#559).
  - **تفكيك ٤ خدمات متجانسة** (#560–#563): odoo-bridge 14 · video-processor 12 · vegetation 12 · supervisor 14 — محفوظ السلوك، عدد المسارات ثابت. #563 احتاج مساعِد `supervisor_route_source.py` (حارس مصدر بعد انتقال `/agent/*`).
- **مفتوح (#564، قيد CI):** MapHub→`cdse-tiles` (إصلاح 404) + nginx `^~ /api/raster/` (جذر اعتراض regex `.png`) + WebSocket الإشعارات (`python-jose` المفقود + توصيف + `websockets<14`) + احتياط جلب الهندسة من DB.
- **صدق:** كلّ تفكيك مُتحقَّق (`import main` + ثبات العدد + الحارس + ruff)؛ مسار CDSE الحيّ (قصّ + قناع SCL) يحتاج تشغيل CDSE حقيقيّاً — `fixed` لا `verified`.

## أعلى الفجوات المفتوحة الآن

(السجلّ الكامل + المصادر في [`gaps/registry.md`](gaps/registry.md))

| ID | العنوان | الحالة |
|---|---|---|
| C1/C2 | التوصية تُولَّد بلا تخزين/تدقيق كامل لربط الشرح بـ`rec_id` | open (جزئيّ — v77 موجود) |
| MAP-QA | افتراض MapLibre/WebGL ينتظر بوّابة QA حيّة (Playwright) | open (البوّابة مُنشأة، تنتظر تشغيلاً) |
| CDSE-CLIP/SCL | قصّ المضلّع + قناع غيوم SCL في CDSE | fixed (يحتاج تحقّقاً ميدانيّاً بتشغيل CDSE) |
| MAPHUB-CDSE / NOTIF-WS | MapHub→cdse-tiles + nginx + WebSocket الإشعارات | PR #564 (مفتوح، قيد CI) |
| C5·H2·H5·C4-M1·SAM2·TERRAIN | (مُغلَقة) NDVI/NATS/الريّ/موبايل push/GPU/terrain | **deferred / by-design** (بيئة أو تحقّق ميدانيّ أو قرار زراعيّ) |

## ماذا بعد؟

- **عاجل:** دمج #564 عند اخضراره (MapHub/CDSE/WS) — ثمّ تحقّق ميدانيّ بتشغيل CDSE حقيقيّ.
- **تنظيف (واجهة GitHub):** حذف الفروع العالقة `frontend-cdse-hide-date` · `fix-cdse-clip-to-field` (الوكيل لا يملك حذف الفروع؛ الوسيط يرفض حذف المرجع).
- **متاح عند الرغبة (لم يُطلَب):** تفكيك بقيّة `main.py` المتجانسة المتبقّية إن وُجدت · ربط ناشري NATS عبر outbox (H2) — قرار معماريّ.
- الفجوات المؤجّلة الستّ تُعاد إلى `open` متى توفّر مُمكِّنها (GPU/Flutter/تحقّق ميدانيّ/قرار زراعيّ).
