pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-xdist>=3.5.0
pytest-cov>=5.0.0
httpx>=0.27.0
anyio>=4.0.0
factory-boy>=3.3.0
freezegun>=1.4.0
respx>=0.20.2
ruff>=0.4.0
black>=24.0.0
sqlparse>=0.5.0
asyncpg>=0.30.0,<0.32.0
redis>=5.0.0
python-jose[cryptography]>=3.4.0
# تبعيّات وقت تشغيل تستوردها الاختبارات (كانت مفقودة ⇒ ModuleNotFoundError في CI):
# test_tts/test_auth_service تستوردان pydantic؛ auth يستخدم bcrypt وEmailStr؛
# الخدمات تستورد prometheus_client.
pydantic[email]>=2.8.0
bcrypt>=4.1.0
prometheus-client>=0.20.0
PyJWT>=2.13.0
# test_otp يُحمّل services/auth/main.py الذي يستورد pyotp (MFA/TOTP، تبعيّة
# مُعلَنة أصلاً في services/auth/requirements.txt) ⇒ بدونها يفشل جمع الاختبار
# في CI بـModuleNotFoundError: No module named 'pyotp'.
pyotp>=2.9.0
# sahool_ai/cookbook يقرأ model_catalog.yaml عبر PyYAML (تبعيّة أساسيّة للحزمة)
# ⇒ كان جمع test_cookbook يفشل بـ ModuleNotFoundError: yaml في CI.
PyYAML>=6.0
# raster-service/change_detection يستخدم numpy (test_change_detection يختبره).
numpy>=1.24.0
hypothesis>=6.100.0

# notification agent imports nats.aio during mobile push tests
nats-py>=2.3.0

# ── UNIT-TEST-DORMANCY-01: إيقاظ ٤٧٩ اختبار وحدة ─────────────────────────────
# هذه الثلاث كانت مستبعَدة عمداً (#590) كي تبقى بيئة `-m unit` «دنيا». المبرّر
# **سقط**: كلّ ما كان ناقصاً هو `scipy` و`pillow`، وscipy مُعلَنة أصلاً في
# `services/sahool-platform/api/requirements.txt:20`. القياس على هذا الملفّ:
#   بدونها  → 2999 نجح · 369 تخطٍّ (كلّ ملفّ خامد يُحتسَب تخطّياً واحداً)
#   معها    → 3478 نجح ·  26 تخطٍّ · **صفر فشل**
# أي أنّ ٤٧٩ اختباراً لم تكن تُنفَّذ في بوّابة الدمج قطّ.
#
# كلّ إصدار مثبَّت على **إصدار الإنتاج** لا على نطاق مفتوح — درس
# APP-ROUTES-EMPTY-01 (#686): `fastapi>=0.115.0` بلا سقف حلّ إلى 0.140.13، وفيها
# `include_router` يُدرِج غلاف `_IncludedRouter` بلا `.path` بدل تسطيح المسارات،
# فيفشل ١١ اختباراً على إصدار **لا تُختبَر عليه المنصّة**. رقِّ هنا والإنتاج معاً.
fastapi==0.136.3  # = api/requirements.txt:6
scipy==1.13.1  # = api/requirements.txt:20 — api/trial_engine.py يستورد scipy.stats
# pillow: مطلوبة لاختبار واحد مُثبَت بالقياس — إسقاطها وحدها يُفشِل
# test_segmentation_frontend_contract_20260702::test_segment_request_accepts_frontend_field_names
pillow>=10.0.0  # = services/field-segmentation/requirements.txt:10

# V6-HARD-IMPORTS-REACH-THE-UNIT-TIER-01: دلتا «إغلاق الفجوات الستّ» أدخلت
# استيرادين **علويّين غير مؤجَّلين** في سلسلة استيرادٍ يبلغها جناح الوحدة:
#   shapely  ← shared/precision_agriculture/trial_spatial.py (هندسة قطع RCBD)
#   rasterio ← services/raster-service/trial_zonal_outcomes.py، ويستوردها
#              raster_processing_runtime.py الذي كان يُستورَد **بلا rasterio** قبلها
# فسقط اختباران كانا يمرّان على `main`: `test_gap_closure_v6::…_spatial_rcbd_…`
# و`test_raster_latest_not_stale_after_rehydrate_v2` — الأوّل بـ`shapely>=2` مطلوبة،
# والثاني بـ`ModuleNotFoundError: rasterio` أثناء تسجيل الراوترات.
#
# كلتاهما مُعلَنة في requirements الخدمة (`api/requirements.txt:27` ·
# `raster-service/requirements.txt:7`)، فالنقص في **بيئة الاختبار** لا في الإنتاج.
# مثبَّتتان على إصدار الإنتاج نفسه — درس APP-ROUTES-EMPTY-01: نطاقٌ مفتوح يحلّ إلى
# إصدارٍ لا تُختبَر عليه المنصّة. و`pip-audit` عليهما قبل الإضافة ⇒ `No known
# vulnerabilities found` (بروتوكول التبعيّات في CLAUDE.md).
shapely==2.0.6  # = services/sahool-platform/api/requirements.txt:27
rasterio>=1.3.0  # = services/raster-service/requirements.txt:7

# JSON-SCHEMAS-WITH-NO-VALIDATOR-01: خمسة عشر ملفّ `*.schema.json` في الشجرة كانت
# بلا مُتحقِّق واحد — المكتبة غير مثبَّتة ولا مُعلَنة ولا مستورَدة في أيّ سطر. المُتحقِّق
# الموحَّد (`scripts/ci/schema_validation_guard.py`) يفشل مُغلَقاً بدونها، لا يتخطّى صامتاً.
jsonschema>=4.20.0
