pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-xdist>=3.5.0
pytest-cov>=5.0.0
httpx>=0.27.0
anyio>=4.0.0
factory-boy>=3.3.0
freezegun>=1.4.0
respx>=0.20.2
ruff>=0.4.0
black>=24.0.0
sqlparse>=0.5.0
asyncpg>=0.30.0,<0.32.0
redis>=5.0.0
python-jose[cryptography]>=3.4.0
# تبعيّات وقت تشغيل تستوردها الاختبارات (كانت مفقودة ⇒ ModuleNotFoundError في CI):
# test_tts/test_auth_service تستوردان pydantic؛ auth يستخدم bcrypt وEmailStr؛
# الخدمات تستورد prometheus_client.
pydantic[email]>=2.8.0
bcrypt>=4.1.0
prometheus-client>=0.20.0
PyJWT>=2.13.0
# test_otp يُحمّل services/auth/main.py الذي يستورد pyotp (MFA/TOTP، تبعيّة
# مُعلَنة أصلاً في services/auth/requirements.txt) ⇒ بدونها يفشل جمع الاختبار
# في CI بـModuleNotFoundError: No module named 'pyotp'.
pyotp>=2.9.0
# sahool_ai/cookbook يقرأ model_catalog.yaml عبر PyYAML (تبعيّة أساسيّة للحزمة)
# ⇒ كان جمع test_cookbook يفشل بـ ModuleNotFoundError: yaml في CI.
PyYAML>=6.0
# raster-service/change_detection يستخدم numpy (test_change_detection يختبره).
numpy>=1.24.0
hypothesis>=6.100.0

# notification agent imports nats.aio during mobile push tests
nats-py>=2.3.0

# ── إيقاظ الاختبارات الخامدة (قياس: 2999 ⇒ 3478 ناجح، 369 ⇒ 26 متخطٍّ، صفر فشل) ──
# كان استبعاد fastapi مقصوداً وموثّقاً في requirements-field-forms.txt: وجودها يوقظ
# اختبارات تبني api.main عبر TestClient «وتفشل في البيئة الدنيا». الشرط الثاني هو ما
# تآكل: نمت هذه البيئة تدريجيّاً (prometheus_client · pyotp · numpy · nats-py …) حتّى
# لم يبقَ ناقصاً إلّا scipy وpillow، فصار الاستبعاد يُخمِد ~479 اختبار وحدة بلا سبب.
# منها 12 ملفّاً في tests_v9 تفحص app.routes — أي حرّاس ميزانية المسارات والموضع
# القانونيّ وتفكيك main.py — كانت خارج الوظيفة الحاجبة.
# scipy ليست توسيعاً للسطح: api/requirements.txt:20 يثبّتها أصلاً لـcore.spatial.
fastapi>=0.115.0
scipy>=1.13.1
pillow>=10.3.0
