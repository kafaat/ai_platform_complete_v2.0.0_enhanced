# إكمال الأكواد الناقصة والجزئيّة (المنطق القابل للإكمال)

أكملتُ الفجوات الأربع التي هي **منطق/بنية قابلة للإكمال** دون جهازك. تركتُ
ما يحتاج بنية تشغيليّة حيّة (موضّح في النهاية بصدق).

## ما أُكمِل
### ١. تسلسل الحالة (persistence-ready) — كان مفقوداً
- `CanonicalFieldState.to_dict()` + `from_dict()` (round-trip مُختبَر)
- يُمكّن: الحفظ في farm_memory/timeline + المقارنة الموسميّة + replay/audit
- الإصدارات (schema/fusion_strategy) مُضمّنة للتفسير عبر ترقيات النموذج

### ٢. مصفوفة الأسبقيّة الصريحة (arbitration matrix) — كانت ad-hoc
استبدلتُ منطق "الملوحة فقط" بمصفوفة شاملة `ARBITRATION_PRECEDENCE` (5 مستويات):
| الأولويّة | القيد | الحالة | القاعدة |
|-----------|-------|--------|---------|
| ١ | ملوحة حرجة | salinity_limited | SAL-SOIL-03 |
| ٢ | حرارة حادّة | heat_limited | HEAT-01 |
| ٣ | اتّجاه هابط | trend_warning | MON-TREND-01 |
| ٤ | حيويّة منخفضة | vigor_stressed | RS-VIGOR-01 |
| ٥ | حيويّة جيّدة | vigor_led | RS-VIGOR-02 |
- المبدأ: القيود البنيويّة طويلة الأمد تتقدّم على القرائن اللحظيّة
- مُختبَر: ملوحة تتقدّم على حرارة؛ حرارة تحكم عند غياب ملوحة؛ سليم → vigor_led
- صريحة وقابلة للتدقيق (لا emergent behavior)

### ٣. تكاليف المدخلات المفصّلة — كانت غير مبنيّة
- `EconomicContext.input_costs` (بذور/سماد/عمالة/ريّ) + `area_ha`
- `assess_economics` يحسب: الإجمالي + التكلفة لكلّ هكتار + التفصيل
- **صدق**: الأرقام من المستخدم فقط — لا أخترع أسعاراً يمنيّة لا أملكها

### ٤. نيّة المزارع المؤثّرة — كانت تُخزَّن فقط
- الهدف يُعدّل القرار فعليّاً (لا metadata):
  - minimize_cost → تفضيل تدخّلات منخفضة التكلفة
  - water_saving → ضبط الريّ على Kc بدقّة
  - risk_reduction → تدخّل وقائي مبكر مفضّل
  - maximize_yield → لا تأخير في معالجة الإجهاد

## التحقّق
- 529/529 roadmap (+7) · 0 خطأ ترجمة · offline 34/0
- لا انحدار: تغيير مصفوفة الأسبقيّة لم يكسر اختبار الملوحة القديم

## ما لم يُكمَل — ولماذا (صدق صارم)
هذه **بنية تشغيليّة حيّة** تحتاج جهازك/بياناتك، إكمالها الآن = واجهات على فراغ:
1. **حفظ الحالة الفعلي في قاعدة البيانات**: to_dict جاهز، لكن الكتابة في
   PostgreSQL/Timescale تحتاج قاعدة حيّة على جهازك.
2. **endpoint حيّ (POST /field-intelligence)**: المنطق جاهز، لكن تشغيله يحتاج
   الخدمة قائمة.
3. **محوّلات HTTP**: نقاط الحقن جاهزة، الربط الحيّ يحتاج الخدمات.
4. **IoT**: لا مستشعرات فعليّة لديك بعد.
5. **أسعار/تكاليف فعليّة**: البنية جاهزة، الأرقام من سوقك المحلّي (لا أخترعها).

## ملاحظة صدق
أكملتُ كلّ ما هو **منطق صرف** (تسلسل/مصفوفة/تكاليف/نيّة) بإعادة استخدام
الموجود. لم أكمل البنية الحيّة (DB/endpoint/IoT) لأنّها تحتاج جهازك — وبناء
واجهات فارغة وهمٌ لا إكمال. مصفوفة الأسبقيّة الآن صريحة كما طلبت المراجعات.
