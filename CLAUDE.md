# دليل المساهمة للوكلاء (Agent / Contributor Guidance)

إرشادات تشغيليّة دائمة لجلسات التطوير المستقبليّة. تُتّبع تلقائيّاً.

## قبل الدفع — شغّل بروتوكول البوّابات

[`docs/runbooks/CI_GATES_AND_PRE_PUSH_PROTOCOL.md`](docs/runbooks/CI_GATES_AND_PRE_PUSH_PROTOCOL.md)
يحمل كتلة واحدة قابلة للّصق تُعيد إنتاج **كلّ** بوّابة حاجبة محلّيّاً، ثمّ يشرح أصناف
الفشل المتكرّرة بأسبابها المقيسة: انحراف المصنوعات المولَّدة (`git add` **قبل** إعادة
التوليد) · ضريبة تسجيل السكربتات والاختبارات المعماريّة الجديدة · نطاق `ruff` (كامل
الشجرة) مقابل `bandit` (`services/ bots/ agents/`) · صنفا حلّ التعارض · فخاخ رموز
الخروج في السكربتات · `Capability-Impact` بعد الدفع · حرّاس الدماغ · ترميز لغة الآلة · **مراسي الأسطر في
`platform_extraction_map.json`** (وثيقة سياسة لا يُعيد `--fix` توليدها) · **حزمة الإصدار
تُبنى آخِراً** (تُجزّئ كلّ ما سبقها، وبعض خطوات `--check` تكتب أثناء الفحص) · **§٢ تُغطّي
أقلّيّة البوّابات** (الأكثريّة خارجها — اشتقّ الباقي من مساراتك المُعدَّلة، والنسبةُ
المقيسة في §٣.١٧ بسِمَتها إلى SHA قياسِها لا مطلقةً).

**§٢ صارت سكربتاً — شغّله بدل نسخ الكتلة:**

```bash
bash scripts/ci/preflight.sh --fast   # ~٣٠ث · ١٥ حارساً ثابتاً (بلا جناح)
bash scripts/ci/preflight.sh          # ~٧د  · يضيف المكنسة والجناحين
bash scripts/ci/preflight.sh --full   # ~١٢د · يضيف bandit وpip-audit ولغة الآلة
```

يطبع أيضاً سطر `Capability-Impact` **مشتقّاً** (لا من جدول يدويّ)، ويُعلن عدد البوّابات
**المتخطّاة** — لأنّ أخضره يعني «ما قِيس مرّ» لا «CI ستخضرّ» (§٣.١٨–§٣.٢١).

القاعدة: **لا تدفع قبل تشغيل §٢ منه.** كلّ صنف موصوف هناك أسقط بناءً حقيقيّاً في هذا
المستودع، وكلّها تُقاس محلّيّاً في دقائق.

**عند فشل بوّابة، ابدأ من [`docs/runbooks/GUARD_CATALOGUE.md`](docs/runbooks/GUARD_CATALOGUE.md)** — كتالوج **مولَّد** (لا يُحرَّر يدويّاً) يقول لكلّ حارس: ما يفرضه · أين يحجب (workflow → job) · العطل الذي وُجِد لأجله بكلمات كاتبه · والاختبار الذي يجب أن يحمرّ إن عُطِّل. ابحث فيه باسم السكربت في رسالة الفشل. **وهو أيضاً مصدرُ العدد الوحيد** — يطبع في رأسه كم حارساً يحجب وكم منها مُثبَتة بالتكذيب، مُشتقّاً من الشجرة لا منقولاً.

> **ولا تُكرَّر أعدادُ الكتالوج هنا.** كانت هذه الفقرة تنقل أعداداً يدويّة فانحرفت عن المصدر الذي تصفه. المرجعُ المُلزِم هو الكتالوج المولَّد، ويُكشَف انجرافُه **باختباره** لا بنسخ قيمه إلى هذه الوثيقة. واللقطاتُ العدديّة للتأريخ الجنائيّ موضعُها [`sahool-brain/reports/guard_surface_ledger.md`](sahool-brain/reports/guard_surface_ledger.md) — مقيَّدةً بـSHA وتاريخ، لا في وثيقة سياسةٍ حيّة.

## الاختبارات — ابدأ بـ`pytest -m unit`

- **وقبله عند تعديل ضيّق:** `bash scripts/ci/test_impact.sh --plan` يشتقّ الاختبارات التي يمسّها تغييرك من **محرّك أثر القدرات نفسه** الذي تُشغّله البوّابة الحاجبة (لا نموذج ثانٍ). ويطبع سقفه المقيس بدل أن يُخفيه: **٤٨٪ من حالات الوحدة في ملفّات لا يبلغها أيّ مخروط** فتعمل دائماً، و**٤٢٪ فقط من ملفّات المصدر قابلة للربط** — فيُصعِّد إلى الجناح الكامل على ~٥٨٪ من تغييرات المصدر بدل أن يُضيّق تخميناً. تسريع محلّيّ لا بوّابة؛ لا يستدعيه أيّ workflow ويحرس ذلك اختبار عقده. تفصيله §٣.٢٣.
- **الافتراضيّ للتغذية الراجعة السريعة محليّاً:** `pytest -m unit`. هذه اختبارات منطق صرف بلا خدمات، وعليها تُبنى بوّابة CI (وظيفة *Unit Tests*: `pytest -v -m unit --cov=services` + أرضيّة تغطية `--cov-fail-under=43`). الأرضيّة راتشِت يصعد ولا ينزل (20 → 40 → 42 → 43)؛ المقيس حاليّاً ~47%. راجع `docs/testing/coverage_ratchet.md`.
- **العلامات (markers) في `pytest.ini`:** `unit` / `integration` / `security` / `slow` / `mcp`، و`testpaths = tests_v9`.
- **احتفظ بـ`-m integration` لِما بعد رفع الخدمات/PostGIS** (تتطلّب Postgres+PostGIS وRedis قيد التشغيل). لا تُشغّلها كافتراضيّ.
- **قبل تسجيل طفرة في `guard_mutation_registry.json`:** الاختبار الذي تُسمّيه الطفرة يجب أن يعمل **في كلّ بيئة**. اختبارٌ يحمل `skipif` (مثل المشروط بوجود SHA في التاريخ) **يُتخطّى في استنساخ CI بعمق ١**، فتصير الطفرة صمتاً لا تكذيباً — و`guard_mutation_guard` يُصنّفها `STABLE_WRONG_TEST`. مفروض بـ`test_no_registered_mutation_names_a_test_that_can_be_skipped`. تفصيله §٣.٢٥د.
- **حارس تفكيك الراوترات:** `services/sahool-platform/tests/test_router_decomposition_guard.py` (مُعلَّم `unit`) يمنع انحدار تفكيك `main.py` إلى `api/routers/` — أبقِه أخضر عند تعديل نقاط `/api/v1/*`.

## التبعيّات — افحص الثغرات قبل أيّ إضافة أو ترقية

- **قبل** إضافة أو ترقية أيّ شيء في `requirements*.txt`، شغّل فحص ثغرات/استشارة تبعيّات (`pip-audit -r <file>`) محليّاً. الفشل المتأخّر في CI مكلِف.
- **بوّابة CI (*Security Scan*):**
  - `pip-audit` يحجب الدمج على **١٩ ملفّ متطلّبات** (توسعة C1: كان أربعة فقط فتفلت خدمات كاملة) — منها `services/sahool-platform/api/requirements.txt` و`services/auth/requirements.txt` و`services/guardrails-engine/requirements.txt` و`requirements_real.txt`. القائمة القانونيّة في `.github/workflows/ci.yml` تحت `pip-audit (gating — critical path)`؛ لا تُقلَّص.
  - استثناء واحد موثَّق: `--ignore-vuln PYSEC-2026-1325` (`ecdsa` — تبعيّة عبوريّة لـ`python-jose`، صنّفها صانعوها WONTFIX، ومسارنا يوقّع عبر خلفيّة `cryptography`). تُشغَّل محلّيّاً بنفس الراية وإلّا ظهرت نتيجة حمراء كاذبة.
  - `bandit -r services/ bots/ agents/ --severity-level high` يحجب على HIGH (الباقي إرشاديّ، لا يحجب).
- **مثال واقعيّ حديث:** `python-multipart` 0.0.27 حمل ثغرة CVE حجبت CI حتى رُفِع إلى `0.0.31` في المسار الحرج. افحص أوّلاً تتجنّب التكرار.

## مسارات المنصّة — الموضع القانونيّ وميزانية النطاق

- **`services/sahool-platform/api/main.py` خالٍ من المسارات بالعقد.** `scripts/ci/p1_main_decomposition_guard.py` يرفض **أيّ** مُزخرِف مسار فيه ويحدّ عدد أسطره. كلّ مسار جديد يذهب إلى `api/routers/`.
- **مسارات البنية/الـprovenance** (`/healthz` · `/readyz` · `/metrics` · `/runtime-identity`) موضعها القانونيّ **`api/routers/platform_health.py`**، مُعلَنة كبيانات في `docs/architecture/platform_route_placement_contract.json` (`required_source` + `forbidden_sources` + `required_function` + `uniqueness`) ومفروضة بـ`scripts/ci/platform_route_placement_guard.py` — حارس مستقلّ يعمل بلا pytest داخل وظيفة `platform-route-budget`، ويفشل برسالة تسمّي الملفّ الصحيح.
- **الميزانية:** الراتشِت يحدّ **مسارات النطاق فقط** (`domain ≤ 629`). مسارات البنية تُستثنى عبر allowlist صريحة (method + مسار مُطبَّع، لا substring) بينما يبقى **الجرد الخام ظاهراً** (631 خام − 4 بنية = 627 نطاق، الهامش ٢). التصنيف يقول ما هو المسار؛ خريطة الموضع تقول أين ينتمي — والاستثناء من الميزانية **لا** يرخّص إعلانه في `main.py`.
- **لا ترفع السقف ولا تحذف نقطة provenance** لحلّ تعارض ميزانية؛ صنّفها بصدق أو ضعها في خدمتها.

## الدماغ المعرفيّ (Knowledge Brain)

قاعدة معرفة Markdown يصونها الوكيل ذاتيّاً في `sahool-brain/` — هُب **رابط لا مكرّر** يربط المصادر
القائمة (docs/adr · MANIFEST · compose · تقرير الفجوات · decision_record) ويضيف الناقص (كتالوج خدمات،
سجلّ فجوات حيّ، لقطة تركيز، سجلّ جلسات، بروتوكول صيانة).

- **بداية الجلسة:** اقرأ `sahool-brain/hot.md` + `sahool-brain/index.md` + الفجوات الحرجة المفتوحة في `sahool-brain/gaps/registry.md`.
- **نهاية الجلسة:** حدّث `hot.md`؛ ألحِق `log.md`؛ حدّث حالات `gaps/registry.md`؛ أضف قرارات الجلسة (SHA + سبب) إلى `decisions/ledger.md`.
- **القواعد الصارمة:** لا معلومة بلا مصدر (`path:line`/`#PR`) · لا فجوة بلا مصدر + حالة · لا قرار بلا سبب + PR/SHA · لا تحديث بلا سطر في `log.md`.

نقطة الدخول: [`sahool-brain/index.md`](sahool-brain/index.md) · المقدّمة والقواعد: [`sahool-brain/README.md`](sahool-brain/README.md).
