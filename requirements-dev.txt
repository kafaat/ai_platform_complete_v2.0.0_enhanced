# SAHOOL v9.1.0 — Development & Testing Dependencies
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-xdist>=3.5.0
pytest-cov>=5.0.0
httpx>=0.27.0
anyio>=4.0.0
factory-boy>=3.3.0
freezegun>=1.4.0
respx>=0.20.2
ruff>=0.4.0
black>=24.0.0
isort>=5.13.0
mypy>=1.10.0
bandit[toml]>=1.7.0
safety>=3.2.0
sqlparse>=0.5.0
# M9/M12 FIX: مسار الترحيل الموثّق (alembic upgrade head) يحتاجهما، ولم يكونا
# معلنَين في أيّ ملف متطلّبات ⇒ ModuleNotFoundError لمن يتبع التوثيق.
alembic>=1.13.0
SQLAlchemy>=2.0.0
asyncpg>=0.30.0,<0.32.0
redis>=5.0.0
# ── تبعيّات وقت تشغيل تحتاجها اختبارات الخدمة (وإلّا ModuleNotFoundError) ──
# FIX: كانت مفقودة فيفشل جمع الاختبارات بـ"No module named 'jose'"
python-jose[cryptography]>=3.4.0
fastapi==0.115.0
pydantic==2.8.2
cryptography>=42.0.0
