# SAHOOL v9 — requirements_real.txt
# الحزم الحقيقية للخدمات

# ── API Framework ─────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.30.6
pydantic==2.13.4

# ── HTTP Client (Sentinel Hub + Open-Meteo) ────
httpx>=0.27.0
aiofiles>=23.0

# ── Database ───────────────────────────────────
asyncpg>=0.30.0,<0.32.0
# ── Messaging ──────────────────────────────────
nats-py>=2.3.0

# ── Cache ──────────────────────────────────────
redis>=5.0.0

# ── Email (async — FIXED) ─────────────────────
aiosmtplib>=3.0.0

# ── Remote Sensing (Sentinel Hub) ─────────────
sentinelhub>=3.10.0        # Sentinel Hub Python SDK
# shapely>=2.0.0           # لتحويل الأشكال الهندسية

# ── Scientific Computing ───────────────────────
numpy>=1.24.0

# ── Machine Learning (AGB Model) ──────────────
scikit-learn>=1.4.0        # Random Forest

# ── Geospatial (Satellite Tiles) ──────────────
# rio-cogeo>=3.6.0         # Cloud-Optimized GeoTIFF (للمرحلة القادمة)
# rasterio>=1.3.9          # Raster processing

# ── Monitoring ─────────────────────────────────
prometheus-client>=0.19.0

# ── Testing ────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
