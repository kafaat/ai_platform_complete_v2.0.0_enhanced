# SAHOOL v9.1.0
.env
.env.*
!.env.example
__pycache__/
*.pyc
*.pyo
node_modules/
dist/
build/
# Playwright E2E (تقارير/نتائج/متصفّحات/لقطات) — لا تُلتزَم
playwright-report/
test-results/
.playwright/
playwright/.cache/
**/playwright-report/
**/test-results/
.dart_tool/
.flutter-plugins
*.log
*.key
*.pem
*.crt
volumes/
data/
backups/
.coverage
htmlcov/
.pytest_cache/
build/
.claude/worktrees/
.claude/settings.local.json

# AlertManager rendered config (contains live secrets — never commit)
monitoring/alertmanager.rendered.yml

# SQLite lite-store artifact (self-created at runtime/tests)
services/sahool-platform/storage/sahool.db
services/sahool-platform/storage/*.db

# لقطات إصدار مؤرشفة (git archive) — قابلة للتوليد، لا تُلتزَم في التاريخ
/sahool_main_*.zip
artifacts/
.claude/scheduled_tasks.lock

# Hypothesis example database (test cache; never commit — trips security audit on BYPASSRLS constants)
.hypothesis/

# Functional runtime probe evidence is produced live, never committed (keeps the repo at the honest baseline)
runtime-verification/functional_evidence/
