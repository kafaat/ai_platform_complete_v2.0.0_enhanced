# تتبّع idempotency عبر السلسلة كاملةً — فحص فعلي

المراجعة محقّة: operation_id في العميل بلا قيمة إن أسقطته السلسلة. تتبّعتُ
المسار فعليّاً واكتشفتُ الصورة الحقيقيّة + فجوة حرجة أصلحتُها.

## اكتشاف معماري مهمّ (تصحيح بصدق)
تتبّعتُ المسار، فوجدتُ أنّ البنية **ليست** كما افترضتِ المراجعة:
```
المُفترَض: موبايل → WS → supervisor → command_store → actuator
الواقع:    موبايل → WS /ws/notifications (استقبال فقط، لا أوامر)
           الأوامر: automation_rules (خلفيّة) → MQTT → actuator → firmware
```
**الموبايل لا يرسل أوامر actuator عبر WS حاليّاً** (لا واجهة تحكّم ري بعد).
`WebSocketService.send()` يُستخدم داخليّاً (pong) فقط. لذا سيناريو "الري يعمل
مرّتين من إعادة إرسال الموبايل" **لا يمكن أن يحدث اليوم** (لا مسار أمر من الموبايل).

→ إصلاح P1 السابق (operation_id في العميل) **دفاعي/مستقبلي** صحيح كنمط، لكن
  ليس حلّاً لخطر قائم حاليّاً. كنتُ سأبالغ لو ادّعيتُ غير ذلك.

## الفجوة الحرجة الحقيقيّة (نقطة المراجعة #3 — مُصلَحة)
المسار الفعلي للأمر الفيزيائي: `actuator → MQTT → firmware → صمّام`.
- MQTT QoS 1 = "at least once" → **قد يسلّم الأمر مرّتين**
- الfirmware يتحقّق من HMAC (أصالة) لكن **بلا حماية تكرار**
- السيناريو: `ينفّذ → ACK يُفقَد → إعادة إرسال → ينفّذ مرّتين` (صمّام يُفتح مرّتين)

**الإصلاح** (في الfirmware): `lastCmdTs` — يرفض الأمر بنفس الطابع الزمني
(تكرار). الآن تشغيل الصمّام **idempotent فيزيائيّاً**.

## السلسلة بعد الفحص
| الطبقة | idempotency | الحالة |
|--------|-------------|--------|
| الموبايل (WS) | operation_id | ✅ دفاعي (لا مسار أمر حاليّاً) |
| command_store (الخلفيّة) | ON CONFLICT command_id | ✅ موجود سابقاً |
| actuator (MQTT) | HMAC موقّع | ✅ أصالة (لا تكرار — يعتمد على الfirmware) |
| **firmware (الصمّام)** | **lastCmdTs** | ✅ **مُصلَح الآن** (النقطة الحرجة) |

## فجوة الطابور (نقطة المراجعة — جزئيّاً)
- الطابور **مُقيّد** (100) ✓ — لا نموّ لانهائي
- لكنّه **in-memory** (يُفقَد عند إعادة التشغيل) — لكنّه قناة إشعارات لا أوامر،
  فالأثر منخفض. الحلّ المستقبلي (P2): تخزين Hive.

## التحقّق
- 659/659 roadmap · توازن أقواس firmware مؤكّد (28/28, 154/154, 24/24)

## ملاحظة صدق
- **صحّحتُ افتراضاً**: تتبّع المسار كشف أنّ الموبايل لا يرسل أوامر actuator
  (قناة إشعارات). لم أدّعِ أنّ P1 السابق حلّ خطراً قائماً — هو دفاعي مستقبلي.
- **الفجوة الحقيقيّة** (تكرار الأمر الفيزيائي في الfirmware) **مُصلَحة** ومُتحقَّقة
  بنيويّاً. هذه أخطر نقطة (صمّام/مضخّة فيزيائيّة).
- `lastCmdTs` حماية بسيطة (آخر ts واحد) — تكفي لتكرار MQTT المتتالي. لانقطاع
  طويل مع أوامر متعدّدة، نافذة أوسع (آخر N معرّفات) أقوى — تحسين مستقبلي.
- **لم أشغّل الfirmware** (يحتاج ESP32 + Arduino) — تحقّق بنيوي فقط. اختبرها
  على جهاز فعلي.
